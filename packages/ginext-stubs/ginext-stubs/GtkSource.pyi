# Auto-generated by ginext-stubgen from GtkSource-5.gir.
# Do not edit by hand.
# mypy: disable-error-code="override,type-arg,misc,valid-type"
from __future__ import annotations

import os
from collections.abc import Iterator
from contextlib import AbstractContextManager
from typing import Any, Awaitable, Callable, ClassVar, Concatenate, Generic, Final, Literal, NamedTuple, ParamSpec as _ParamSpec, Self, TypeVar, override, overload
from enum import IntEnum, IntFlag
from ginext import GLib
from ginext import GObject
from ginext import Gdk
from ginext import GdkPixbuf
from ginext import Gio
from ginext import Gtk
from ginext import Pango
from ginext.overlay.registrar import OverlayRegistrar as _OverlayRegistrar
from ginext.private import GIMeta as _GIMeta

overlay: _OverlayRegistrar
__version__: tuple[int, ...]
def __getattr__(name: str) -> Any: ...

_SigO = TypeVar('_SigO')
_SigP = _ParamSpec('_SigP')
_SigR = TypeVar('_SigR')

class SignalConnection:
    handler_id: int
    signal_name: str
    source: Any
    callback: Callable[..., Any]
    after: bool
    once: bool
    owner: Any
    is_connected: bool
    def disconnect(self) -> None: ...
    def blocked(self) -> AbstractContextManager[SignalConnection]: ...

class Signal(Generic[_SigO, _SigP, _SigR]):
    def __call__(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...
    def connect(self, handler: Callable[Concatenate[_SigO, _SigP], _SigR], *, after: bool = ..., once: bool = ..., owner: Any = ...) -> SignalConnection: ...
    def connect_after(self, handler: Callable[Concatenate[_SigO, _SigP], _SigR]) -> SignalConnection: ...
    def emit(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...
    def disconnect(self, connection: SignalConnection) -> None: ...
class SignalMethod(Signal[_SigO, _SigP, _SigR]):
    def __call__(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...

class DetailedSignal(Signal[_SigO, _SigP, _SigR]):
    def __call__(self, detail: str | GObject.Property) -> 'Signal[_SigO, _SigP, _SigR]': ...

class AnnotationStyle(IntEnum):
    NONE = 0
    WARNING = 1
    ERROR = 2
    ACCENT = 3

class BackgroundPatternType(IntEnum):
    NONE = 0
    GRID = 1

class BracketMatchType(IntEnum):
    NONE = 0
    OUT_OF_RANGE = 1
    NOT_FOUND = 2
    FOUND = 3

class ChangeCaseType(IntEnum):
    LOWER = 0
    UPPER = 1
    TOGGLE = 2
    TITLE = 3

class CompletionActivation(IntEnum):
    NONE = 0
    INTERACTIVE = 1
    USER_REQUESTED = 2

class CompletionColumn(IntEnum):
    ICON = 0
    BEFORE = 1
    TYPED_TEXT = 2
    AFTER = 3
    COMMENT = 4
    DETAILS = 5

class CompressionType(IntEnum):
    NONE = 0
    GZIP = 1

class FileLoaderError(IntEnum):
    TOO_BIG = 0
    ENCODING_AUTO_DETECTION_FAILED = 1
    CONVERSION_FALLBACK = 2

class FileSaverError(IntEnum):
    INVALID_CHARS = 0
    EXTERNALLY_MODIFIED = 1

class FileSaverFlags(IntFlag):
    NONE = 0
    IGNORE_INVALID_CHARS = 1
    IGNORE_MODIFICATION_TIME = 2
    CREATE_BACKUP = 4

class GutterRendererAlignmentMode(IntEnum):
    CELL = 0
    FIRST = 1
    LAST = 2

class NewlineType(IntEnum):
    LF = 0
    CR = 1
    CR_LF = 2

class SmartHomeEndType(IntEnum):
    DISABLED = 0
    BEFORE = 1
    AFTER = 2
    ALWAYS = 3

class SortFlags(IntFlag):
    NONE = 0
    CASE_SENSITIVE = 1
    REVERSE_ORDER = 2
    REMOVE_DUPLICATES = 4
    FILENAME = 8

class SpaceLocationFlags(IntFlag):
    NONE = 0
    LEADING = 1
    INSIDE_TEXT = 2
    TRAILING = 4
    ALL = 7

class SpaceTypeFlags(IntFlag):
    NONE = 0
    SPACE = 1
    TAB = 2
    NEWLINE = 4
    NBSP = 8
    ALL = 15

class ViewGutterPosition(IntEnum):
    LINES = -30
    MARKS = -20

SchedulerCallback = Callable[[int], bool]
class Annotation(GObject.Object):
    """
    Represents an annotation added to :class:`View`, it has a :attr:`Annotation.line` property,
    :attr:`Annotation.description`, icon and a style.

    It will be displayed always at the end of a line.

    If the style is GTK_SOURCE_ANNOTATION_STYLE_NONE it will use the same color as :class:`SpaceDrawer`.
    """
    gimeta: ClassVar[_GIMeta]
    description: str
    icon: Gio.Icon
    line: int
    style: AnnotationStyle | int
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_description(self) -> str: ...
    def get_icon(self) -> Gio.Icon | None: ...
    def get_line(self) -> int: ...
    def get_style(self) -> AnnotationStyle: ...

class AnnotationClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class AnnotationProvider(GObject.Object):
    """
    It is used to provide annotations and display them on :class:`View` and also populate
    :class:`HoverDisplay` when the user hovers over an annotation.

    You can subclass this object and implement :meth:`AnnotationProvider.populate_hover_async` and
    :meth:`AnnotationProvider.populate_hover_finish` or connect to ``AnnotationProvider::populate``
    and call :meth:`AnnotationProvider.populate` or do it asynchronously.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def add_annotation(self, annotation: Annotation) -> None:
        """
        Add an annotation to the provider.
        """
    @overload
    def populate_hover_async(self, annotation: Annotation, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: None = ...) -> Awaitable[bool]:
        """
        Used to populate the :class:`HoverDisplay` asynchronously, use
        :meth:`AnnotationProvider.populate_hover` to do it synchronously.
        """
    @overload
    def populate_hover_async(self, annotation: Annotation, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        Used to populate the :class:`HoverDisplay` asynchronously, use
        :meth:`AnnotationProvider.populate_hover` to do it synchronously.
        """
    def populate_hover_finish(self, result: Gio.AsyncResult) -> bool:
        """
        Finishes populating the :class:`HoverDisplay` asynchronously.
        """
    def remove_all(self) -> None:
        """
        Removes all annotations from the provider.
        """
    def remove_annotation(self, annotation: Annotation) -> bool:
        """
        Remove an annotation from the provider.
        """
    def do_populate_hover_async(self, annotation: Annotation, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback | None = ...) -> None:
        """
        Used to populate the :class:`HoverDisplay` asynchronously, use
        :meth:`AnnotationProvider.populate_hover` to do it synchronously.
        """
    def do_populate_hover_finish(self, result: Gio.AsyncResult) -> bool:
        """
        Finishes populating the :class:`HoverDisplay` asynchronously.
        """
    changed: Signal[Self, [], None]
    def do_changed(self) -> None: ...

class AnnotationProviderClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    populate_hover_async: Any
    populate_hover_finish: Any

class Annotations(GObject.Object):
    """
    Use this object to manage :class:`Annotation`s. Each :class:`View` has a single annotation
    manager and it is guaranteed to be the same for the lifetime of :class:`View`.

    Add :class:`AnnotationProvider`s with :meth:`Annotations.add_provider` to
    display all the annotations added to each :class:`AnnotationProvider`.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def add_provider(self, provider: AnnotationProvider) -> None:
        """
        Adds a new annotation provider.
        """
    def remove_provider(self, provider: AnnotationProvider) -> bool:
        """
        Removes a provider.
        """
    changed: Signal[Self, [], None]
    def do_changed(self) -> None: ...

class AnnotationsClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class Buffer(Gtk.TextBuffer):
    """
    Subclass of :class:`Gtk.TextBuffer`.

    A :class:`GtkSource.Buffer` object is the model for :class:`View` widgets.
    It extends the :class:`Gtk.TextBuffer` class by adding features useful to display
    and edit source code such as syntax highlighting and bracket matching.

    To create a :class:`GtkSource.Buffer` use :meth:`GtkSource.Buffer.new` or
    :meth:`GtkSource.Buffer.new_with_language`. The second form is just a convenience
    function which allows you to initially set a :class:`Language`. You can also
    directly create a :class:`View` and get its :class:`Buffer` with
    :meth:`Gtk.TextView.get_buffer`.

    The highlighting is enabled by default, but you can disable it with
    :meth:`Buffer.set_highlight_syntax`.

    # Context Classes:

    It is possible to retrieve some information from the syntax highlighting
    engine. The default context classes that are applied to regions of a
    :class:`GtkSource.Buffer`:

     - **comment**: the region delimits a comment;
     - **no-spell-check**: the region should not be spell checked;
     - **path**: the region delimits a path to a file;
     - **string**: the region delimits a string.

    Custom language definition files can create their own context classes,
    since the functions like :meth:`Buffer.iter_has_context_class` take
    a string parameter as the context class.

    :class:`GtkSource.Buffer` provides an API to access the context classes:
    :meth:`Buffer.iter_has_context_class`,
    :meth:`Buffer.get_context_classes_at_iter`,
    :meth:`Buffer.iter_forward_to_context_class_toggle` and
    :meth:`Buffer.iter_backward_to_context_class_toggle`.

    And the ``GtkSource.Buffer::highlight-updated`` signal permits to be notified
    when a context class region changes.

    Each context class has also an associated :class:`Gtk.TextTag` with the name
    ``gtksourceview:context-classes:<name>``. For example to
    retrieve the :class:`Gtk.TextTag` for the string context class, one can write:
    .. code-block:: c

        GtkTextTagTable *tag_table;
        GtkTextTag *tag;

        tag_table = gtk_text_buffer_get_tag_table (buffer);
        tag = gtk_text_tag_table_lookup (tag_table, "gtksourceview:context-classes:string");
    .. code-block:: python

        buffer = GtkSource.Buffer()

        tag_table = buffer.get_tag_table()
        tag = tag_table.lookup(name="gtksourceview:context-classes:string")

    The tag must be used for read-only purposes.

    Accessing a context class via the associated :class:`Gtk.TextTag` is less
    convenient than the :class:`GtkSource.Buffer` API, because:

     - The tag doesn't always exist, you need to listen to the
       ``Gtk.TextTagTable::tag-added`` and ``Gtk.TextTagTable::tag-removed`` signals.
     - Instead of the ``GtkSource.Buffer::highlight-updated`` signal, you can listen
       to the ``Gtk.TextBuffer::apply-tag`` and ``Gtk.TextBuffer::remove-tag`` signals.

    A possible use-case for accessing a context class via the associated
    :class:`Gtk.TextTag` is to read the region but without adding a hard dependency on the
    :class:`GtkSource.View` library (for example for a spell-checking library that wants to
    read the no-spell-check region).
    """
    gimeta: ClassVar[_GIMeta]
    highlight_matching_brackets: bool
    highlight_syntax: bool
    implicit_trailing_newline: bool
    language: Language
    loading: bool
    style_scheme: StyleScheme
    def __init__(self, *, highlight_matching_brackets: bool = ..., highlight_syntax: bool = ..., implicit_trailing_newline: bool = ..., language: Language = ..., style_scheme: StyleScheme = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_with_language(cls, language: Language) -> Self:
        """
        Creates a new source buffer using the highlighting patterns in ``language``.

        This is equivalent to creating a new source buffer with
        a new tag table and then calling :meth:`Buffer.set_language`.
        """
    def backward_iter_to_source_mark(self, iter: Gtk.TextIter, category: str | None = ...) -> tuple[bool, Gtk.TextIter]:
        """
        Moves ``iter`` to the position of the previous :class:`Mark` of the given
        category.

        Returns ``True`` if ``iter`` was moved. If ``category`` is NULL, the
        previous source mark can be of any category.
        """
    def change_case(self, case_type: ChangeCaseType | int, start: Gtk.TextIter, end: Gtk.TextIter) -> None:
        """
        Changes the case of the text between the specified iterators.

        Since 5.4, this function will update the position of ``start`` and
        ``end`` to surround the modified text.
        """
    def create_source_mark(self, name: str | None, category: str, where: Gtk.TextIter) -> Mark:
        """
        Creates a source mark in the ``buffer`` of category ``category``.

        A source mark is a :class:`Gtk.TextMark` but organized into categories.
        Depending on the category a pixbuf can be specified that will be displayed
        along the line of the mark.

        Like a :class:`Gtk.TextMark`, a :class:`Mark` can be anonymous if the
        passed ``name`` is ``None``.  Also, the buffer owns the marks so you
        shouldn't unreference it.

        Marks always have left gravity and are moved to the beginning of
        the line when the user deletes the line they were in.

        Typical uses for a source mark are bookmarks, breakpoints, current
        executing instruction indication in a source file, etc..
        """
    def ensure_highlight(self, start: Gtk.TextIter, end: Gtk.TextIter) -> None:
        """
        Forces buffer to analyze and highlight the given area synchronously.

        **Note**:

        This is a potentially slow operation and should be used only
        when you need to make sure that some text not currently
        visible is highlighted, for instance before printing.
        """
    def forward_iter_to_source_mark(self, iter: Gtk.TextIter, category: str | None = ...) -> tuple[bool, Gtk.TextIter]:
        """
        Moves ``iter`` to the position of the next :class:`Mark` of the given
        ``category``.

        Returns ``True`` if ``iter`` was moved. If ``category`` is NULL, the
        next source mark can be of any category.
        """
    def get_context_classes_at_iter(self, iter: Gtk.TextIter) -> list[str]:
        """
        Get all defined context classes at ``iter``.

        See the :class:`Buffer` description for the list of default context classes.
        """
    def get_highlight_matching_brackets(self) -> bool:
        """
        Determines whether bracket match highlighting is activated for the
        source buffer.
        """
    def get_highlight_syntax(self) -> bool:
        """
        Determines whether syntax highlighting is activated in the source
        buffer.
        """
    def get_implicit_trailing_newline(self) -> bool: ...
    def get_language(self) -> Language | None:
        """
        Returns the :class:`Language` associated with the buffer,
        see :meth:`Buffer.set_language`.

        The returned object should not be unreferenced by the user.
        """
    def get_loading(self) -> bool: ...
    def get_markup(self, start: Gtk.TextIter, end: Gtk.TextIter) -> str:
        """
        Returns the text in the specified range converting any text formatting
        to equivalent Pango markup tags.
        This allows the styled text to be displayed in other widgets that support
        Pango markup, such as :class:`GtkLabel.`

        For very long ranges this function can take long enough that you could
        potentially miss frame renderings.
        """
    def get_source_marks_at_iter(self, iter: Gtk.TextIter, category: str | None = ...) -> list[Mark]:
        """
        Returns the list of marks of the given category at ``iter``.

        If ``category`` is ``None`` it returns all marks at ``iter``.
        """
    def get_source_marks_at_line(self, line: int, category: str | None = ...) -> list[Mark]:
        """
        Returns the list of marks of the given category at ``line``.

        If ``category`` is ``None``, all marks at ``line`` are returned.
        """
    def get_style_scheme(self) -> StyleScheme | None:
        """
        Returns the :class:`StyleScheme` associated with the buffer,
        see :meth:`Buffer.set_style_scheme`.

        The returned object should not be unreferenced by the user.
        """
    def iter_backward_to_context_class_toggle(self, iter: Gtk.TextIter, context_class: str) -> tuple[bool, Gtk.TextIter]:
        """
        Moves backward to the next toggle (on or off) of the context class.

        If no matching context class toggles are found, returns ``False``, otherwise ``True``.
        Does not return toggles located at ``iter``, only toggles after ``iter``. Sets
        ``iter`` to the location of the toggle, or to the end of the buffer if no
        toggle is found.

        See the :class:`Buffer` description for the list of default context classes.
        """
    def iter_forward_to_context_class_toggle(self, iter: Gtk.TextIter, context_class: str) -> tuple[bool, Gtk.TextIter]:
        """
        Moves forward to the next toggle (on or off) of the context class.

        If no matching context class toggles are found, returns ``False``, otherwise ``True``.
        Does not return toggles located at ``iter``, only toggles after ``iter``. Sets
        ``iter`` to the location of the toggle, or to the end of the buffer if no
        toggle is found.

        See the :class:`Buffer` description for the list of default context classes.
        """
    def iter_has_context_class(self, iter: Gtk.TextIter, context_class: str) -> bool:
        """
        Check if the class ``context_class`` is set on ``iter``.

        See the :class:`Buffer` description for the list of default context classes.
        """
    def join_lines(self, start: Gtk.TextIter, end: Gtk.TextIter) -> None:
        """
        Joins the lines of text between the specified iterators.
        """
    def remove_source_marks(self, start: Gtk.TextIter, end: Gtk.TextIter, category: str | None = ...) -> None:
        """
        Remove all marks of ``category`` between ``start`` and ``end`` from the buffer.

        If ``category`` is NULL, all marks in the range will be removed.
        """
    def set_highlight_matching_brackets(self, highlight: bool) -> None:
        """
        Controls the bracket match highlighting function in the buffer.

        If activated, when you position your cursor over a bracket character
        (a parenthesis, a square bracket, etc.) the matching opening or
        closing bracket character will be highlighted.
        """
    def set_highlight_syntax(self, highlight: bool) -> None:
        """
        Controls whether syntax is highlighted in the buffer.

        If ``highlight`` is ``True``, the text will be highlighted according to the syntax
        patterns specified in the :class:`Language` set with :meth:`Buffer.set_language`.

        If ``highlight`` is ``False``, syntax highlighting is disabled and all the
        :class:`Gtk.TextTag` objects that have been added by the syntax highlighting engine
        are removed from the buffer.
        """
    def set_implicit_trailing_newline(self, implicit_trailing_newline: bool) -> None:
        """
        Sets whether the ``buffer`` has an implicit trailing newline.

        If an explicit trailing newline is present in a :class:`Gtk.TextBuffer`, :class:`Gtk.TextView`
        shows it as an empty line. This is generally not what the user expects.

        If ``implicit_trailing_newline`` is ``True`` (the default value):
         - when a :class:`FileLoader` loads the content of a file into the ``buffer``,
           the trailing newline (if present in the file) is not inserted into the
           ``buffer``.
         - when a :class:`FileSaver` saves the content of the ``buffer`` into a file, a
           trailing newline is added to the file.

        On the other hand, if ``implicit_trailing_newline`` is ``False``, the file's
        content is not modified when loaded into the ``buffer``, and the ``buffer``'s
        content is not modified when saved into a file.
        """
    def set_language(self, language: Language | None = ...) -> None:
        """
        Associates a :class:`Language` with the buffer.

        Note that a :class:`Language` affects not only the syntax highlighting, but
        also the [context classes](./class.Buffer.html#context-classes). If you want to disable just the
        syntax highlighting, see :meth:`Buffer.set_highlight_syntax`.

        The buffer holds a reference to ``language``.
        """
    def set_style_scheme(self, scheme: StyleScheme | None = ...) -> None:
        """
        Sets a :class:`StyleScheme` to be used by the buffer and the view.

        Note that a :class:`StyleScheme` affects not only the syntax highlighting,
        but also other :class:`View` features such as highlighting the current line,
        matching brackets, the line numbers, etc.

        Instead of setting a ``None`` ``scheme``, it is better to disable syntax
        highlighting with :meth:`Buffer.set_highlight_syntax`, and setting the
        :class:`StyleScheme` with the "classic" or "tango" ID, because those two
        style schemes follow more closely the GTK theme (for example for the
        background color).

        The buffer holds a reference to ``scheme``.
        """
    def sort_lines(self, start: Gtk.TextIter, end: Gtk.TextIter, flags: SortFlags | int, column: int) -> None:
        """
        Sort the lines of text between the specified iterators.
        """
    def do_bracket_matched(self, iter: Gtk.TextIter, state: BracketMatchType | int) -> None: ...
    bracket_matched: Signal[Self, [Gtk.TextIter | None, BracketMatchType], None]
    cursor_moved: Signal[Self, [], None]
    highlight_updated: Signal[Self, [Gtk.TextIter, Gtk.TextIter], None]
    source_mark_updated: Signal[Self, [Gtk.TextMark], None]
    def do_cursor_moved(self) -> None: ...
    def do_highlight_updated(self, start: Gtk.TextIter, end: Gtk.TextIter) -> None: ...
    def do_source_mark_updated(self, mark: Gtk.TextMark) -> None: ...

class BufferClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.TextBufferClass
    bracket_matched: Any
    _reserved: list[int]

class Completion(GObject.Object):
    """
    Main Completion Object.

    The completion system helps the user when they writes some text,
    such as words, command names, functions, and suchlike. Proposals can
    be shown, to complete the text the user is writing. Each proposal can
    contain an additional piece of information (for example
    documentation), that is displayed when the "Details" button is
    clicked.

    Proposals are created via a :class:`CompletionProvider`. There can
    be for example a provider to complete words (see :class:`CompletionWords`),
    another provider for the completion of
    function names, etc. To add a provider, call
    :meth:`Completion.add_provider`.

    The :class:`CompletionProposal` interface represents a proposal.

    If a proposal contains extra information (see
    :data:`GtkSource.CompletionColumn.DETAILS`), it will be
    displayed in a supplemental details window, which appears when
    the "Details" button is clicked.

    Each :class:`View` object is associated with a :class:`Completion`
    instance. This instance can be obtained with
    :meth:`View.get_completion`. The :class:`View` class contains also the
    ``View::show-completion`` signal.

    A same :class:`CompletionProvider` object can be used for several
    :class:`GtkSource.Completion`'s.
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Gtk.TextView
    page_size: int
    remember_info_visibility: bool
    select_on_show: bool
    show_icons: bool
    view: View
    def __init__(self, *, page_size: int = ..., remember_info_visibility: bool = ..., select_on_show: bool = ..., show_icons: bool = ..., view: View = ..., **kwargs: Any) -> None: ...
    def add_provider(self, provider: CompletionProvider) -> None:
        """
        Adds a :class:`CompletionProvider` to the list of providers to be queried
        for completion results.
        """
    def block_interactive(self) -> None: ...
    def get_buffer(self) -> Buffer:
        """
        Gets the connected :class:`View`'s :class:`Buffer`
        """
    def get_page_size(self) -> int: ...
    def get_view(self) -> View:
        """
        Gets the :class:`View` that owns the :class:`Completion`.
        """
    def remove_provider(self, provider: CompletionProvider) -> None:
        """
        Removes a :class:`CompletionProvider` previously added with
        :meth:`Completion.add_provider`.
        """
    def set_page_size(self, page_size: int) -> None: ...
    def unblock_interactive(self) -> None: ...
    @staticmethod
    def fuzzy_highlight(haystack: str, casefold_query: str) -> Pango.AttrList | None:
        """
        This will add ``<b>`` tags around matched characters in ``haystack``
        based on ``casefold_query``.
        """
    @staticmethod
    def fuzzy_match(haystack: str | None, casefold_needle: str) -> tuple[bool, int]:
        """
        This helper function can do a fuzzy match for you giving a haystack and
        casefolded needle.

        Casefold your needle using :func:`GLib.utf8_casefold` before
        running the query.

        Score will be set with the score of the match upon success. Otherwise,
        it will be set to zero.
        """
    hide: SignalMethod[Self, [], None]
    provider_added: Signal[Self, [CompletionProvider], None]
    provider_removed: Signal[Self, [CompletionProvider], None]
    show: SignalMethod[Self, [], None]
    def do_hide(self) -> None: ...
    def do_provider_added(self, provider: CompletionProvider) -> None: ...
    def do_provider_removed(self, provider: CompletionProvider) -> None: ...
    def do_show(self) -> None: ...

class CompletionCell(Gtk.Widget, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Widget for single cell of completion proposal.

    The :class:`GtkSource.CompletionCell` widget provides a container to display various
    types of information with the completion display.

    Each proposal may consist of multiple cells depending on the complexity of
    the proposal. For example, programming language proposals may contain a cell
    for the "left-hand-side" of an operation along with the "typed-text" for a
    function name and "parameters". They may also optionally set an icon to
    signify the kind of result.

    A :class:`CompletionProvider` should implement the
    :meth:`CompletionProvider.do_display` virtual function to control
    how to convert data from their :class:`CompletionProposal` to content for
    the :class:`GtkSource.CompletionCell`.
    """
    gimeta: ClassVar[_GIMeta]
    column: CompletionColumn | int
    markup: str
    paintable: Gdk.Paintable
    text: str
    widget: Gtk.Widget
    def __init__(self, *, column: CompletionColumn | int = ..., markup: str = ..., paintable: Gdk.Paintable = ..., text: str = ..., widget: Gtk.Widget = ..., **kwargs: Any) -> None: ...
    def get_column(self) -> CompletionColumn: ...
    def get_widget(self) -> Gtk.Widget | None:
        """
        Gets the child :class:`GtkWidget`, if any.
        """
    def set_gicon(self, gicon: Gio.Icon) -> None: ...
    def set_icon_name(self, icon_name: str) -> None: ...
    def set_markup(self, markup: str) -> None: ...
    def set_paintable(self, paintable: Gdk.Paintable) -> None: ...
    def set_text(self, text: str | None = ...) -> None:
        """
        Sets the text for the column cell. Use ``None`` to unset.
        """
    def set_text_with_attributes(self, text: str, attrs: Pango.AttrList) -> None: ...
    def set_widget(self, child: Gtk.Widget) -> None: ...

class CompletionCellClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.WidgetClass

class CompletionClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class CompletionContext(GObject.Object, Gio.ListModel):
    """
    The context of a completion.

    :class:`GtkSource.CompletionContext` contains information about an attept to display
    completion proposals to the user based on typed text in the :class:`View`.

    When typing, :class:`Completion` may use registered
    :class:`CompletionProvider` to determine if there may be results which
    could be displayed. If so, a :class:`GtkSource.CompletionContext` is created with
    information that is provided to the :class:`CompletionProvider` to populate
    results which might be useful to the user.

    :class:`CompletionProvider` are expected to provide :class:`Gio.ListModel` with
    :class:`CompletionProposal` which may be joined together in a list of
    results for the user. They are also responsible for how the contents are
    displayed using :class:`CompletionCell` which allows for some level of
    customization.
    """
    gimeta: ClassVar[_GIMeta]
    busy: bool
    completion: Completion
    empty: bool
    def __init__(self, *, completion: Completion = ..., **kwargs: Any) -> None: ...
    def get_activation(self) -> CompletionActivation:
        """
        Gets the mode for which the context was activated.
        """
    def get_bounds(self) -> tuple[bool, Gtk.TextIter, Gtk.TextIter]:
        """
        Gets the bounds for the completion, which is the beginning of the
        current word (taking break characters into account) to the current
        insertion cursor.

        If ``begin`` is non-``None``, it will be set to the start position of the
        current word being completed.

        If ``end`` is non-``None``, it will be set to the insertion cursor for the
        current word being completed.
        """
    def get_buffer(self) -> Buffer | None:
        """
        Gets the underlying buffer used by the context.

        This is a convenience function to get the buffer via the :class:`GtkSource.Completion`
        property.
        """
    def get_busy(self) -> bool:
        """
        Gets the "busy" property. This is set to ``True`` while the completion
        context is actively fetching proposals from registered
        :class:`GtkSource.CompletionProvider`'s.
        """
    def get_completion(self) -> Completion | None:
        """
        Gets the :class:`GtkSource.Completion` that created the context.
        """
    def get_empty(self) -> bool:
        """
        Checks if any proposals have been provided to the context.

        Out of convenience, this function will return ``True`` if ``self`` is ``None``.
        """
    def get_language(self) -> Language | None:
        """
        Gets the language of the underlying buffer, if any.
        """
    def get_proposals_for_provider(self, provider: CompletionProvider) -> Gio.ListModel | None:
        """
        Gets the :class:`GListModel` associated with the provider.

        You can connect to :class:`GtkSource.CompletionContext`::model-changed to receive
        notifications about when the model has been replaced by a new model.
        """
    def get_view(self) -> View | None:
        """
        Gets the text view for the context.
        """
    def get_word(self) -> str:
        """
        Gets the word that is being completed up to the position of the insert mark.
        """
    def list_providers(self) -> Gio.ListModel:
        """
        Gets the providers that are associated with the context.
        """
    def set_proposals_for_provider(self, provider: CompletionProvider, results: Gio.ListModel | None = ...) -> None:
        """
        This function allows providers to update their results for a context
        outside of a call to :meth:`CompletionProvider.populate_async`.

        This can be used to immediately return results for a provider while it does
        additional asynchronous work. Doing so will allow the completions to
        update while the operation is in progress.
        """
    provider_model_changed: Signal[Self, [CompletionProvider, Gio.ListModel | None], None]
    def do_provider_model_changed(self, provider: CompletionProvider, model: Gio.ListModel | None) -> None: ...

class CompletionContextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class CompletionProposal:
    """
    Interface for completion proposals.

    This interface is used to denote that an object is capable of being
    a completion proposal for :class:`Completion`.

    Currently, no method or functions are required but additional methods
    may be added in the future. Proposals created by
    :class:`GtkSource.CompletionProvider` can use :func:`GObject.IMPLEMENT_INTERFACE` to
    implement this with ``None`` for the interface init function.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def get_typed_text(self) -> str | None:
        """
        Gets the typed-text for the proposal, if supported by the implementation.

        Implementing this virtual-function is optional, but can be useful to allow
        external tooling to compare results.
        """
    def do_get_typed_text(self) -> str | None:
        """
        Gets the typed-text for the proposal, if supported by the implementation.

        Implementing this virtual-function is optional, but can be useful to allow
        external tooling to compare results.
        """

class CompletionProposalInterface:
    gimeta: ClassVar[_GIMeta]
    parent_iface: GObject.TypeInterface
    get_typed_text: Any

class CompletionProvider:
    """
    Completion provider interface.

    You must implement this interface to provide proposals to :class:`Completion`.

    In most cases, implementations of this interface will want to use
    :meth:`CompletionProvider.do_populate_async` to asynchronously populate the results
    to avoid blocking the main loop.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def activate(self, context: CompletionContext, proposal: CompletionProposal) -> None:
        """
        This function requests ``proposal`` to be activated by the
        :class:`GtkSource.CompletionProvider`.

        What the provider does to activate the proposal is specific to that
        provider. Many providers may choose to insert a :class:`GtkSource.Snippet` with
        edit points the user may cycle through.

        See also: :class:`Snippet`, :class:`SnippetChunk`, :meth:`View.push_snippet`
        """
    def display(self, context: CompletionContext, proposal: CompletionProposal, cell: CompletionCell) -> None:
        """
        This function requests that the :class:`GtkSource.CompletionProvider` prepares
        ``cell`` to display the contents of ``proposal``.

        Based on ``cells`` column type, you may want to display different information.

        This allows for columns of information among completion proposals
        resulting in better alignment of similar content (icons, return types,
        method names, and parameter lists).
        """
    def get_priority(self, context: CompletionContext) -> int:
        """
        This function should return the priority of ``self`` in ``context``.

        The priority is used to sort groups of completion proposals by
        provider so that higher priority providers results are shown
        above lower priority providers.

        Higher value indicates higher priority.
        """
    def get_title(self) -> str | None:
        """
        Gets the title of the completion provider, if any.

        Currently, titles are not displayed in the completion results, but may be
        at some point in the future when non-``None``.
        """
    def is_trigger(self, iter: Gtk.TextIter, ch: str) -> bool:
        """
        This function is used to determine if a character inserted into the text
        editor should cause a new completion request to be triggered.

        An example would be period '.' which might indicate that the user wants
        to complete method or field names of an object.

        This method will only trigger when text is inserted into the :class:`GtkTextBuffer`
        while the completion list is visible and a proposal is selected. Incremental
        key-presses (like shift, control, or alt) are not triggerable.
        """
    def key_activates(self, context: CompletionContext, proposal: CompletionProposal, keyval: int, state: Gdk.ModifierType | int) -> bool:
        """
        This function is used to determine if a key typed by the user should
        activate ``proposal`` (resulting in committing the text to the editor).

        This is useful when using languages where convention may lead to less
        typing by the user. One example may be the use of "." or "-" to expand
        a field access in the C programming language.
        """
    def list_alternates(self, context: CompletionContext, proposal: CompletionProposal) -> list[CompletionProposal] | None:
        """
        Providers should return a list of alternates to ``proposal`` or ``None`` if
        there are no alternates available.

        This can be used by the completion view to allow the user to move laterally
        through similar proposals, such as overrides of methods by the same name.
        """
    @overload
    def populate_async(self, context: CompletionContext, cancellable: Gio.Cancellable | None = ..., callback: None = ...) -> Awaitable[Gio.ListModel]:
        """
        Asynchronously requests that the provider populates the completion
        results for ``context``.

        For providers that would like to populate a :class:`Gio.ListModel` while those
        results are displayed to the user,
        :meth:`CompletionContext.set_proposals_for_provider` may be used
        to reduce latency until the user sees results.
        """
    @overload
    def populate_async(self, context: CompletionContext, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        Asynchronously requests that the provider populates the completion
        results for ``context``.

        For providers that would like to populate a :class:`Gio.ListModel` while those
        results are displayed to the user,
        :meth:`CompletionContext.set_proposals_for_provider` may be used
        to reduce latency until the user sees results.
        """
    def populate_finish(self, result: Gio.AsyncResult) -> Gio.ListModel:
        """
        Completes an asynchronous operation to populate a completion provider.
        """
    def refilter(self, context: CompletionContext, model: Gio.ListModel) -> None:
        """
        This function can be used to filter results previously provided to
        the :class:`CompletionContext` by the :class:`GtkSource.CompletionProvider`.

        This can happen as the user types additional text onto the word so
        that previously matched items may be removed from the list instead of
        generating new :class:`Gio.ListModel` of results.
        """
    def do_activate(self, context: CompletionContext, proposal: CompletionProposal) -> None:
        """
        This function requests ``proposal`` to be activated by the
        :class:`GtkSource.CompletionProvider`.

        What the provider does to activate the proposal is specific to that
        provider. Many providers may choose to insert a :class:`GtkSource.Snippet` with
        edit points the user may cycle through.

        See also: :class:`Snippet`, :class:`SnippetChunk`, :meth:`View.push_snippet`
        """
    def do_display(self, context: CompletionContext, proposal: CompletionProposal, cell: CompletionCell) -> None:
        """
        This function requests that the :class:`GtkSource.CompletionProvider` prepares
        ``cell`` to display the contents of ``proposal``.

        Based on ``cells`` column type, you may want to display different information.

        This allows for columns of information among completion proposals
        resulting in better alignment of similar content (icons, return types,
        method names, and parameter lists).
        """
    def do_get_priority(self, context: CompletionContext) -> int:
        """
        This function should return the priority of ``self`` in ``context``.

        The priority is used to sort groups of completion proposals by
        provider so that higher priority providers results are shown
        above lower priority providers.

        Higher value indicates higher priority.
        """
    def do_get_title(self) -> str | None:
        """
        Gets the title of the completion provider, if any.

        Currently, titles are not displayed in the completion results, but may be
        at some point in the future when non-``None``.
        """
    def do_is_trigger(self, iter: Gtk.TextIter, ch: str) -> bool:
        """
        This function is used to determine if a character inserted into the text
        editor should cause a new completion request to be triggered.

        An example would be period '.' which might indicate that the user wants
        to complete method or field names of an object.

        This method will only trigger when text is inserted into the :class:`GtkTextBuffer`
        while the completion list is visible and a proposal is selected. Incremental
        key-presses (like shift, control, or alt) are not triggerable.
        """
    def do_key_activates(self, context: CompletionContext, proposal: CompletionProposal, keyval: int, state: Gdk.ModifierType | int) -> bool:
        """
        This function is used to determine if a key typed by the user should
        activate ``proposal`` (resulting in committing the text to the editor).

        This is useful when using languages where convention may lead to less
        typing by the user. One example may be the use of "." or "-" to expand
        a field access in the C programming language.
        """
    def do_list_alternates(self, context: CompletionContext, proposal: CompletionProposal) -> list[CompletionProposal] | None:
        """
        Providers should return a list of alternates to ``proposal`` or ``None`` if
        there are no alternates available.

        This can be used by the completion view to allow the user to move laterally
        through similar proposals, such as overrides of methods by the same name.
        """
    def do_populate_async(self, context: CompletionContext, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback | None = ...) -> None:
        """
        Asynchronously requests that the provider populates the completion
        results for ``context``.

        For providers that would like to populate a :class:`Gio.ListModel` while those
        results are displayed to the user,
        :meth:`CompletionContext.set_proposals_for_provider` may be used
        to reduce latency until the user sees results.
        """
    def do_populate_finish(self, result: Gio.AsyncResult) -> Gio.ListModel:
        """
        Completes an asynchronous operation to populate a completion provider.
        """
    def do_refilter(self, context: CompletionContext, model: Gio.ListModel) -> None:
        """
        This function can be used to filter results previously provided to
        the :class:`CompletionContext` by the :class:`GtkSource.CompletionProvider`.

        This can happen as the user types additional text onto the word so
        that previously matched items may be removed from the list instead of
        generating new :class:`Gio.ListModel` of results.
        """

class CompletionProviderInterface:
    gimeta: ClassVar[_GIMeta]
    parent_iface: GObject.TypeInterface
    get_title: Any
    get_priority: Any
    is_trigger: Any
    key_activates: Any
    populate_async: Any
    populate_finish: Any
    refilter: Any
    display: Any
    activate: Any
    list_alternates: Any

class CompletionSnippets(GObject.Object, CompletionProvider):
    """
    A :class:`CompletionProvider` for the completion of snippets.

    The :class:`GtkSource.CompletionSnippets` is an example of an implementation of
    the :class:`CompletionProvider` interface. The proposals are snippets
    registered with the :class:`SnippetManager`.
    """
    gimeta: ClassVar[_GIMeta]
    priority: int
    title: str
    def __init__(self, *, priority: int = ..., title: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...

class CompletionSnippetsClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class CompletionWords(GObject.Object, CompletionProvider):
    """
    A :class:`CompletionProvider` for the completion of words.

    The :class:`GtkSource.CompletionWords` is an example of an implementation of
    the :class:`CompletionProvider` interface. The proposals are words
    appearing in the registered :class:`Gtk.TextBuffer`s.
    """
    gimeta: ClassVar[_GIMeta]
    minimum_word_size: int
    priority: int
    proposals_batch_size: int
    scan_batch_size: int
    title: str
    def __init__(self, *, minimum_word_size: int = ..., priority: int = ..., proposals_batch_size: int = ..., scan_batch_size: int = ..., title: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def register(self, buffer: Gtk.TextBuffer) -> None:
        """
        Registers ``buffer`` in the ``words`` provider.
        """
    def unregister(self, buffer: Gtk.TextBuffer) -> None:
        """
        Unregisters ``buffer`` from the ``words`` provider.
        """

class CompletionWordsClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class Encoding:
    """
    Character encoding.

    The :class:`GtkSource.Encoding` boxed type represents a character encoding. It is used
    for example by :class:`GtkSource.File`. Note that the text in GTK widgets is always
    encoded in UTF-8.
    """
    gimeta: ClassVar[_GIMeta]
    def copy(self) -> Encoding:
        """
        Used by language bindings.
        """
    def free(self) -> None:
        """
        Used by language bindings.
        """
    def get_charset(self) -> str:
        """
        Gets the character set of the :class:`GtkSource.Encoding`, such as "UTF-8" or
        "ISO-8859-1".
        """
    def get_name(self) -> str:
        """
        Gets the name of the :class:`GtkSource.Encoding` such as "Unicode" or "Western".
        """
    def to_string(self) -> str: ...
    @staticmethod
    def get_all() -> list[Encoding]:
        """
        Gets all encodings.
        """
    @staticmethod
    def get_current() -> Encoding:
        """
        Gets the :class:`GtkSource.Encoding` for the current locale.

        See also :func:`GLib.get_charset`.
        """
    @staticmethod
    def get_default_candidates() -> list[Encoding]:
        """
        Gets the list of default candidate encodings to try when loading a file.

        See :meth:`FileLoader.set_candidate_encodings`.

        This function returns a different list depending on the current locale (i.e.
        language, country and default encoding). The UTF-8 encoding and the current
        locale encoding are guaranteed to be present in the returned list.
        """
    @staticmethod
    def get_from_charset(charset: str) -> Encoding | None:
        """
        Gets a :class:`GtkSource.Encoding` from a character set such as "UTF-8" or
        "ISO-8859-1".
        """
    @staticmethod
    def get_utf8() -> Encoding: ...

class File(GObject.Object):
    """
    On-disk representation of a :class:`Buffer`.

    A :class:`GtkSource.File` object is the on-disk representation of a :class:`Buffer`.
    With a :class:`GtkSource.File`, you can create and configure a :class:`FileLoader`
    and :class:`FileSaver` which take by default the values of the
    :class:`GtkSource.File` properties (except for the file loader which auto-detect some
    properties). On a successful load or save operation, the :class:`GtkSource.File`
    properties are updated. If an operation fails, the :class:`GtkSource.File` properties
    have still the previous valid values.
    """
    gimeta: ClassVar[_GIMeta]
    compression_type: CompressionType | int
    encoding: Encoding
    location: Gio.File
    newline_type: NewlineType | int
    read_only: bool
    def __init__(self, *, location: Gio.File = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def check_file_on_disk(self) -> None:
        """
        Checks synchronously the file on disk, to know whether the file is externally
        modified, or has been deleted, and whether the file is read-only.

        :class:`GtkSource.File` doesn't create a :class:`Gio.FileMonitor` to track those properties, so
        this function needs to be called instead. Creating lots of :class:`Gio.FileMonitor`'s
        would take lots of resources.

        Since this function is synchronous, it is advised to call it only on local
        files. See :meth:`File.is_local`.
        """
    def get_compression_type(self) -> CompressionType: ...
    def get_encoding(self) -> Encoding:
        """
        The encoding is initially ``None``. After a successful file loading or saving
        operation, the encoding is non-``None``.
        """
    def get_location(self) -> Gio.File | None: ...
    def get_newline_type(self) -> NewlineType: ...
    def is_deleted(self) -> bool:
        """
        Returns whether the file has been deleted. If the
        :attr:`File.location` is ``None``, returns ``False``.

        To have an up-to-date value, you must first call
        :meth:`File.check_file_on_disk`.
        """
    def is_externally_modified(self) -> bool:
        """
        Returns whether the file is externally modified. If the
        :attr:`File.location` is ``None``, returns ``False``.

        To have an up-to-date value, you must first call
        :meth:`File.check_file_on_disk`.
        """
    def is_local(self) -> bool:
        """
        Returns whether the file is local. If the :attr:`File.location` is ``None``,
        returns ``False``.
        """
    def is_readonly(self) -> bool:
        """
        Returns whether the file is read-only. If the
        :attr:`File.location` is ``None``, returns ``False``.

        To have an up-to-date value, you must first call
        :meth:`File.check_file_on_disk`.
        """
    def set_location(self, location: Gio.File | None = ...) -> None:
        """
        Sets the location.
        """

class FileClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class FileLoader(GObject.Object):
    """
    Load a file into a :class:`GtkSource.Buffer`.

    A :class:`GtkSource.FileLoader` object permits to load the contents of a :class:`Gio.File` or a
    :class:`Gio.InputStream` into a :class:`Buffer`.

    A file loader should be used only for one load operation, including errors
    handling. If an error occurs, you can reconfigure the loader and relaunch the
    operation with :meth:`FileLoader.load_async`.

    Running a :class:`GtkSource.FileLoader` is an undoable action for the
    :class:`Buffer`.

    After a file loading, the buffer is reset to the contents provided by the
    :class:`Gio.File` or :class:`Gio.InputStream`, so the buffer is set as “unmodified”, that is,
    :meth:`Gtk.TextBuffer.set_modified` is called with ``False``. If the contents isn't
    saved somewhere (for example if you load from stdin), then you should
    probably call :meth:`Gtk.TextBuffer.set_modified` with ``True`` after calling
    :meth:`FileLoader.load_finish`.
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Buffer
    file: File
    input_stream: Gio.InputStream
    location: Gio.File
    def __init__(self, *, buffer: Buffer = ..., file: File = ..., input_stream: Gio.InputStream = ..., location: Gio.File = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_from_stream(cls, buffer: Buffer, file: File, stream: Gio.InputStream) -> Self:
        """
        Creates a new :class:`GtkSource.FileLoader` object. The contents is read from ``stream``.
        """
    def get_buffer(self) -> Buffer: ...
    def get_compression_type(self) -> CompressionType: ...
    def get_encoding(self) -> Encoding: ...
    def get_file(self) -> File: ...
    def get_input_stream(self) -> Gio.InputStream | None: ...
    def get_location(self) -> Gio.File | None: ...
    def get_newline_type(self) -> NewlineType: ...
    @overload
    def load_async(self, io_priority: int, cancellable: Gio.Cancellable | None = ..., progress_callback: Gio.FileProgressCallback | None = ..., callback: None = ...) -> Awaitable[bool]:
        """
        Loads asynchronously the file or input stream contents into the :class:`Buffer`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this
        function.
        """
    @overload
    def load_async(self, io_priority: int, cancellable: Gio.Cancellable | None = ..., progress_callback: Gio.FileProgressCallback | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        Loads asynchronously the file or input stream contents into the :class:`Buffer`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this
        function.
        """
    def load_finish(self, result: Gio.AsyncResult) -> bool:
        """
        Finishes a file loading started with :meth:`FileLoader.load_async`.

        If the contents has been loaded, the following :class:`File` properties will
        be updated: the location, the encoding, the newline type and the compression
        type.
        """
    def set_candidate_encodings(self, candidate_encodings: list[Encoding]) -> None:
        """
        Sets the candidate encodings for the file loading.

        The encodings are tried in the same order as the list.

        For convenience, ``candidate_encodings`` can contain duplicates. Only the first
        occurrence of a duplicated encoding is kept in the list.

        By default the candidate encodings are (in that order in the list):

        1. If set, the :class:`File`'s encoding as returned by :meth:`File.get_encoding`.
        2. The default candidates as returned by :func:`Encoding.get_default_candidates`.
        """

class FileLoaderClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class FileSaver(GObject.Object):
    """
    Save a :class:`Buffer` into a file.

    A :class:`GtkSource.FileSaver` object permits to save a :class:`Buffer` into a
    :class:`Gio.File`.

    A file saver should be used only for one save operation, including errors
    handling. If an error occurs, you can reconfigure the saver and relaunch the
    operation with :meth:`FileSaver.save_async`.
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Buffer
    compression_type: CompressionType | int
    encoding: Encoding
    file: File
    flags: FileSaverFlags | int
    location: Gio.File
    newline_type: NewlineType | int
    def __init__(self, *, buffer: Buffer = ..., compression_type: CompressionType | int = ..., encoding: Encoding = ..., file: File = ..., flags: FileSaverFlags | int = ..., location: Gio.File = ..., newline_type: NewlineType | int = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_with_target(cls, buffer: Buffer, file: File, target_location: Gio.File) -> Self:
        """
        Creates a new :class:`GtkSource.FileSaver` object with a target location.

        When the file saving is finished successfully, ``target_location`` is set to the ``file``'s
        :attr:`File.location` property. If an error occurs, the previous valid
        location is still available in :class:`File`.

        This constructor is suitable for a "save as" operation, or for saving a new
        buffer for the first time.
        """
    def get_buffer(self) -> Buffer: ...
    def get_compression_type(self) -> CompressionType: ...
    def get_encoding(self) -> Encoding: ...
    def get_file(self) -> File: ...
    def get_flags(self) -> FileSaverFlags: ...
    def get_location(self) -> Gio.File: ...
    def get_newline_type(self) -> NewlineType: ...
    @overload
    def save_async(self, io_priority: int, cancellable: Gio.Cancellable | None = ..., progress_callback: Gio.FileProgressCallback | None = ..., callback: None = ...) -> Awaitable[bool]:
        """
        Saves asynchronously the buffer into the file.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.
        """
    @overload
    def save_async(self, io_priority: int, cancellable: Gio.Cancellable | None = ..., progress_callback: Gio.FileProgressCallback | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        Saves asynchronously the buffer into the file.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.
        """
    def save_finish(self, result: Gio.AsyncResult) -> bool:
        """
        Finishes a file saving started with :meth:`FileSaver.save_async`.

        If the file has been saved successfully, the following :class:`File`
        properties will be updated: the location, the encoding, the newline type and
        the compression type.

        Since the 3.20 version, :meth:`Gtk.TextBuffer.set_modified` is called with ``False``
        if the file has been saved successfully.
        """
    def set_compression_type(self, compression_type: CompressionType | int) -> None:
        """
        Sets the compression type. By default the compression type is taken from the
        :class:`GtkSource.File`.
        """
    def set_encoding(self, encoding: Encoding | None = ...) -> None:
        """
        Sets the encoding. If ``encoding`` is ``None``, the UTF-8 encoding will be set.

        By default the encoding is taken from the :class:`GtkSource.File`.
        """
    def set_flags(self, flags: FileSaverFlags | int) -> None: ...
    def set_newline_type(self, newline_type: NewlineType | int) -> None:
        """
        Sets the newline type. By default the newline type is taken from the
        :class:`GtkSource.File`.
        """

class FileSaverClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class Gutter(Gtk.Widget, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Gutter object for :class:`View`.

    The :class:`GtkSource.Gutter` object represents the left or right gutter of the text
    view. It is used by :class:`View` to draw the line numbers and
    :class:`Mark`s that might be present on a line. By packing
    additional :class:`GutterRenderer` objects in the gutter, you can extend the
    gutter with your own custom drawings.

    To get a :class:`GtkSource.Gutter`, use the :meth:`View.get_gutter` function.

    The gutter works very much the same way as cells rendered in a :class:`Gtk.TreeView`.
    The concept is similar, with the exception that the gutter does not have an
    underlying :class:`Gtk.TreeModel`. The builtin line number renderer is at position
    :data:`GtkSource.ViewGutterPosition.LINES` (-30) and the marks renderer is at
    :data:`GtkSource.ViewGutterPosition.MARKS` (-20). The gutter sorts the renderers
    in ascending order, from left to right. So the marks are displayed on the
    right of the line numbers.
    """
    gimeta: ClassVar[_GIMeta]
    view: View
    window_type: Gtk.TextWindowType | int
    def __init__(self, *, view: View = ..., window_type: Gtk.TextWindowType | int = ..., **kwargs: Any) -> None: ...
    def get_view(self) -> View: ...
    def insert(self, renderer: GutterRenderer, position: int) -> bool:
        """
        Insert ``renderer`` into the gutter. If ``renderer`` is yet unowned then gutter
        claims its ownership. Otherwise just increases renderer's reference count.
        ``renderer`` cannot be already inserted to another gutter.
        """
    def remove(self, renderer: GutterRenderer) -> None: ...
    def reorder(self, renderer: GutterRenderer, position: int) -> None:
        """
        Reorders ``renderer`` in ``gutter`` to new ``position``.
        """

class GutterClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.WidgetClass

class GutterLines(GObject.Object):
    """
    Collected information about visible lines.

    The :class:`GtkSource.GutterLines` object is used to collect information about
    visible lines.

    Use this from your ``GutterRenderer::query-data`` to collect the
    necessary information on visible lines. Doing so reduces the number of
    passes through the text btree allowing :class:`GtkSource.View` to reach more
    frames-per-second while performing kinetic scrolling.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def add_class(self, line: int, name: str) -> None:
        """
        Adds the class ``name`` to ``line``.

        ``name`` will be converted to a [alias``GLib``.Quark] as part of this process. A
        faster version of this function is available via
        :meth:`GutterLines.add_qclass` for situations where the [alias``GLib``.Quark] is
        known ahead of time.
        """
    def add_qclass(self, line: int, qname: GLib.Quark) -> None:
        """
        Adds the class denoted by ``qname`` to ``line``.

        You may check if a line has ``qname`` by calling
        :meth:`GutterLines.has_qclass`.

        You can remove ``qname`` by calling
        :meth:`GutterLines.remove_qclass`.
        """
    def get_buffer(self) -> Gtk.TextBuffer:
        """
        Gets the :class:`Gtk.TextBuffer` that the :class:`GtkSource.GutterLines` represents.
        """
    def get_first(self) -> int:
        """
        Gets the line number (starting from 0) for the first line that is
        user visible.
        """
    def get_iter_at_line(self, line: int) -> Gtk.TextIter:
        """
        Gets a :class:`GtkTextIter` for the current buffer at ``line``
        """
    def get_last(self) -> int:
        """
        Gets the line number (starting from 0) for the last line that is
        user visible.
        """
    def get_line_extent(self, line: int, mode: GutterRendererAlignmentMode | int) -> tuple[float, float]:
        """
        Gets the Y range for a line based on ``mode``.

        The value for ``y`` is relative to the renderers widget coordinates.
        """
    def get_line_yrange(self, line: int, mode: GutterRendererAlignmentMode | int) -> tuple[int, int]:
        """
        Gets the Y range for a line based on ``mode``.

        The value for ``y`` is relative to the renderers widget coordinates.
        """
    def get_view(self) -> Gtk.TextView:
        """
        Gets the :class:`Gtk.TextView` that the :class:`GtkSource.GutterLines` represents.
        """
    def has_any_class(self, line: int) -> bool:
        """
        Checks to see if the line has any GQuark classes set. This can be
        used to help renderer implementations avoid work if nothing has
        been set on the class.
        """
    def has_class(self, line: int, name: str) -> bool:
        """
        Checks to see if :meth:`GutterLines.add_class` was called with
        the ``name`` for ``line``.

        A faster version of this function is provided via
        :meth:`GutterLines.has_qclass` for situations where the quark
        is known ahead of time.
        """
    def has_qclass(self, line: int, qname: GLib.Quark) -> bool:
        """
        Checks to see if :meth:`GutterLines.add_qclass` was called with
        the quark denoted by ``qname`` for ``line``.
        """
    def is_cursor(self, line: int) -> bool:
        """
        Checks to see if ``line`` contains the insertion cursor.
        """
    def is_prelit(self, line: int) -> bool:
        """
        Checks to see if ``line`` is marked as prelit. Generally, this means
        the mouse pointer is over the line within the gutter.
        """
    def is_selected(self, line: int) -> bool:
        """
        Checks to see if the view had a selection and if that selection overlaps
        ``line`` in some way.
        """
    def remove_class(self, line: int, name: str) -> None:
        """
        Removes the class matching ``name`` from ``line``.

        A faster version of this function is available via
        :meth:`GutterLines.remove_qclass` for situations where the
        :class:`GQuark` is known ahead of time.
        """
    def remove_qclass(self, line: int, qname: GLib.Quark) -> None:
        """
        Reverses a call to :meth:`GutterLines.add_qclass` by removing
        the [alias``GLib``.Quark] matching ``qname``.
        """

class GutterLinesClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class GutterRenderer(Gtk.Widget, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Gutter cell renderer.

    A :class:`GtkSource.GutterRenderer` represents a column in a :class:`Gutter`. The
    column contains one cell for each visible line of the :class:`Gtk.TextBuffer`. Due to
    text wrapping, a cell can thus span multiple lines of the :class:`Gtk.TextView`. In
    this case, :class:`GutterRendererAlignmentMode` controls the alignment of
    the cell.

    The gutter renderer is a :class:`Gtk.Widget` and is measured using the normal widget
    measurement facilities. The width of the gutter will be determined by the
    measurements of the gutter renderers.

    The width of a gutter renderer generally takes into account the entire text
    buffer. For instance, to display the line numbers, if the buffer contains 100
    lines, the gutter renderer will always set its width such as three digits can
    be printed, even if only the first 20 lines are shown. Another strategy is to
    take into account only the visible lines.  In this case, only two digits are
    necessary to display the line numbers of the first 20 lines. To take another
    example, the gutter renderer for :class:`Mark`s doesn't need to take
    into account the text buffer to announce its width. It only depends on the
    icons size displayed in the gutter column.

    When the available size to render a cell is greater than the required size to
    render the cell contents, the cell contents can be aligned horizontally and
    vertically with :meth:`GutterRenderer.set_alignment_mode`.

    The cells rendering occurs using :meth:`Gtk.Widget.do_snapshot`. Implementations
    should use ``gtk_source_gutter_renderer_get_lines()`` to retrieve information
    about the lines to be rendered. To help with aligning content which takes
    into account the padding and alignment of a cell, implementations may call
    :meth:`GutterRenderer.align_cell` for a given line number with the
    width and height measurement of the content they width to render.
    """
    gimeta: ClassVar[_GIMeta]
    alignment_mode: GutterRendererAlignmentMode | int
    lines: GutterLines
    view: Gtk.TextView
    xalign: float
    xpad: int
    yalign: float
    ypad: int
    def __init__(self, *, alignment_mode: GutterRendererAlignmentMode | int = ..., xalign: float = ..., xpad: int = ..., yalign: float = ..., ypad: int = ..., **kwargs: Any) -> None: ...
    def align_cell(self, line: int, width: float, height: float) -> tuple[float, float]:
        """
        Locates where to render content that is ``width`` x ``height`` based on
        the renderers alignment and padding.

        The location will be placed into ``x`` and ``y`` and is relative to the
        renderer's coordinates.

        It is encouraged that renderers use this function when snappshotting
        to ensure consistent placement of their contents.
        """
    def get_alignment_mode(self) -> GutterRendererAlignmentMode:
        """
        Get the alignment mode.

        The alignment mode describes the manner in which the
        renderer is aligned (see :attr:`GutterRenderer.xalign` and
        :attr:`GutterRenderer.yalign`).
        """
    def get_buffer(self) -> Buffer | None:
        """
        Gets the :class:`Buffer` for which the gutter renderer is drawing.
        """
    def get_view(self) -> View:
        """
        Get the view associated to the gutter renderer
        """
    def get_xalign(self) -> float:
        """
        Gets the ``xalign`` property.

        This may be used to adjust where within the cell rectangle the renderer will draw.
        """
    def get_xpad(self) -> int:
        """
        Gets the ``xpad`` property.

        This may be used to adjust the cell rectangle that the renderer will use to draw.
        """
    def get_yalign(self) -> float:
        """
        Gets the ``yalign`` property.

        This may be used to adjust where within the cell rectangle the renderer will draw.
        """
    def get_ypad(self) -> int:
        """
        Gets the ``ypad`` property.

        This may be used to adjust the cell rectangle that the renderer will use to draw.
        """
    def set_alignment_mode(self, mode: GutterRendererAlignmentMode | int) -> None:
        """
        Set the alignment mode. The alignment mode describes the manner in which the
        renderer is aligned (see :attr:`GutterRenderer.xalign` and
        :attr:`GutterRenderer.yalign`).
        """
    def set_xalign(self, xalign: float) -> None:
        """
        Adjusts the ``xalign`` property.

        This may be used to adjust where within the cell rectangle the renderer will draw.
        """
    def set_xpad(self, xpad: int) -> None:
        """
        Adjusts the ``xpad`` property.

        This may be used to adjust the cell rectangle that the renderer will use to draw.
        """
    def set_yalign(self, yalign: float) -> None:
        """
        Adjusts the ``yalign`` property.

        This may be used to adjust where within the cell rectangle the renderer will draw.
        """
    def set_ypad(self, ypad: int) -> None:
        """
        Adjusts the ``ypad`` property.

        This may be used to adjust the cell rectangle that the renderer will use to draw.
        """
    def do_activate(self, iter: Gtk.TextIter, area: Gdk.Rectangle, button: int, state: Gdk.ModifierType | int, n_presses: int) -> None:
        """
        Emits the ``GutterRenderer::activate`` signal of the renderer. This is
        called from :class:`Gutter` and should never have to be called manually.
        """
    def do_begin(self, lines: GutterLines) -> None: ...
    def do_change_buffer(self, old_buffer: Buffer | None = ...) -> None:
        """
        This is called when the text buffer changes for ``renderer``.
        """
    def do_change_view(self, old_view: View | None = ...) -> None:
        """
        This is called when the text view changes for ``renderer``.
        """
    def do_end(self) -> None: ...
    def do_query_activatable(self, iter: Gtk.TextIter, area: Gdk.Rectangle) -> bool:
        """
        Get whether the renderer is activatable at the location provided. This is
        called from :class:`Gutter` to determine whether a renderer is activatable
        using the mouse pointer.
        """
    def do_query_data(self, lines: GutterLines, line: int) -> None: ...
    def do_snapshot_line(self, snapshot: Gtk.Snapshot, lines: GutterLines, line: int) -> None: ...
    activate: SignalMethod[Self, [Gtk.TextIter, Gdk.Rectangle, int, Gdk.ModifierType, int], None]
    query_activatable: SignalMethod[Self, [Gtk.TextIter, Gdk.Rectangle], bool]
    query_data: Signal[Self, [GObject.Object, int], None]

class GutterRendererClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.WidgetClass
    query_data: Any
    begin: Any
    snapshot_line: Any
    end: Any
    change_view: Any
    change_buffer: Any
    query_activatable: Any
    activate: Any
    _reserved: list[int]

class GutterRendererPixbuf(GutterRenderer, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Renders a pixbuf in the gutter.

    A :class:`GtkSource.GutterRendererPixbuf` can be used to render an image in a cell of
    :class:`Gutter`.
    """
    gimeta: ClassVar[_GIMeta]
    gicon: Gio.Icon
    icon_name: str
    paintable: Gdk.Paintable
    pixbuf: GdkPixbuf.Pixbuf
    def __init__(self, *, gicon: Gio.Icon = ..., icon_name: str = ..., paintable: Gdk.Paintable = ..., pixbuf: GdkPixbuf.Pixbuf = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_gicon(self) -> Gio.Icon:
        """
        Get the gicon of the renderer
        """
    def get_icon_name(self) -> str: ...
    def get_paintable(self) -> Gdk.Paintable | None:
        """
        Gets a :class:`Gdk.Paintable` that was set with
        :meth:`GutterRendererPixbuf.set_paintable`
        """
    def get_pixbuf(self) -> GdkPixbuf.Pixbuf:
        """
        Get the pixbuf of the renderer.
        """
    def overlay_paintable(self, paintable: Gdk.Paintable) -> None:
        """
        Allows overlaying a paintable on top of any other image that
        has been set for the pixbuf. This will be applied when the
        widget is next snapshot.
        """
    def set_gicon(self, icon: Gio.Icon | None = ...) -> None: ...
    def set_icon_name(self, icon_name: str | None = ...) -> None: ...
    def set_paintable(self, paintable: Gdk.Paintable | None = ...) -> None: ...
    def set_pixbuf(self, pixbuf: GdkPixbuf.Pixbuf | None = ...) -> None: ...

class GutterRendererPixbufClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GutterRendererClass
    _reserved: list[int]

class GutterRendererText(GutterRenderer, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Renders text in the gutter.

    A :class:`GtkSource.GutterRendererText` can be used to render text in a cell of
    :class:`Gutter`.
    """
    gimeta: ClassVar[_GIMeta]
    markup: str
    text: str
    def __init__(self, *, markup: str = ..., text: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def measure(self, text: str) -> tuple[int, int]:
        """
        Measures the text provided using the pango layout used by the
        :class:`GtkSource.GutterRendererText`.
        """
    def measure_markup(self, markup: str) -> tuple[int, int]:
        """
        Measures the pango markup provided using the pango layout used by the
        :class:`GtkSource.GutterRendererText`.
        """
    def set_markup(self, markup: str, length: int) -> None: ...
    def set_text(self, text: str, length: int) -> None: ...

class GutterRendererTextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GutterRendererClass
    _reserved: list[int]

class Hover(GObject.Object):
    """
    Interactive tooltips.

    :class:`GtkSource.Hover` allows a :class:`View` to provide contextual information.
    When enabled, if the user hovers over a word in the text editor, a series
    of registered :class:`HoverProvider` can populate a :class:`HoverDisplay`
    with useful information.

    To enable call :meth:`View.get_hover` and add :class:`HoverProvider`
    using :meth:`Hover.add_provider`. To disable, remove all registered
    providers with :meth:`Hover.remove_provider`.

    You can change how long to wait to display the interactive tooltip by
    setting the :attr:`Hover.hover-delay` property in milliseconds.
    """
    gimeta: ClassVar[_GIMeta]
    hover_delay: int
    def __init__(self, *, hover_delay: int = ..., **kwargs: Any) -> None: ...
    def add_provider(self, provider: HoverProvider) -> None: ...
    def remove_provider(self, provider: HoverProvider) -> None: ...

class HoverClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class HoverContext(GObject.Object):
    """
    Context for populating :class:`HoverDisplay` contents.

    :class:`GtkSource.HoverContext` contains information about the request to populate
    contents for a :class:`HoverDisplay`.

    It can be used to retrieve the :class:`View`, :class:`Buffer`, and
    :class:`Gtk.TextIter` for the regions of text which are being displayed.

    Use :meth:`HoverContext.get_bounds` to get the word that was
    requested. :meth:`HoverContext.get_iter` will get you the location
    of the pointer when the request was made.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def get_bounds(self) -> tuple[bool, Gtk.TextIter, Gtk.TextIter]:
        """
        Gets the current word bounds of the hover.

        If ``begin`` is non-``None``, it will be set to the start position of the
        current word being hovered.

        If ``end`` is non-``None``, it will be set to the end position for the
        current word being hovered.
        """
    def get_buffer(self) -> Buffer:
        """
        A convenience function to get the buffer.
        """
    def get_iter(self) -> tuple[bool, Gtk.TextIter]:
        """
        Gets the location of the pointer where the request was made.
        """
    def get_view(self) -> View: ...

class HoverContextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class HoverDisplay(Gtk.Widget, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    Display for interactive tooltips.

    :class:`GtkSource.HoverDisplay` is a :class:`Gtk.Widget` that may be populated with widgets
    to be displayed to the user in interactive tooltips. The children widgets
    are packed vertically using a :class:`Gtk.Box`.

    Implement the :class:`HoverProvider` interface to be notified of when
    to populate a :class:`GtkSource.HoverDisplay` on behalf of the user.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def append(self, child: Gtk.Widget) -> None: ...
    def insert_after(self, child: Gtk.Widget, sibling: Gtk.Widget) -> None: ...
    def prepend(self, child: Gtk.Widget) -> None: ...
    def remove(self, child: Gtk.Widget) -> None: ...

class HoverDisplayClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.WidgetClass

class HoverProvider:
    """
    Interface to populate interactive tooltips.

    :class:`GtkSource.HoverProvider` is an interface that should be implemented to extend
    the contents of a :class:`HoverDisplay`. This is typical in editors that
    interact external tooling such as those utilizing Language Server Protocol.

    If you can populate the :class:`HoverDisplay` synchronously, use
    :meth:`HoverProvider.do_populate`. Otherwise, interface implementations that
    may take additional time should use :meth:`HoverProvider.do_populate_async`
    to avoid blocking the main loop.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    @overload
    def populate_async(self, context: HoverContext, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: None = ...) -> Awaitable[bool]: ...
    @overload
    def populate_async(self, context: HoverContext, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None: ...
    def populate_finish(self, result: Gio.AsyncResult) -> bool: ...
    def do_populate(self, context: HoverContext, display: HoverDisplay) -> bool: ...
    def do_populate_async(self, context: HoverContext, display: HoverDisplay, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback | None = ...) -> None: ...
    def do_populate_finish(self, result: Gio.AsyncResult) -> bool: ...

class HoverProviderInterface:
    gimeta: ClassVar[_GIMeta]
    parent_iface: GObject.TypeInterface
    populate: Any
    populate_async: Any
    populate_finish: Any

class Indenter:
    """
    Auto-indentation interface.

    By default, :class:`View` can auto-indent as you type when
    :attr:`View.auto-indent` is enabled. The indentation simply copies the
    previous lines indentation.

    This can be changed by implementing :class:`GtkSource.Indenter` and setting the
    :attr:`View.indenter` property.

    Implementors of this interface should implement both
    :meth:`GtkSource.Indenter.do_is_trigger` and :meth:`GtkSource.Indenter.do_indent`.

    :meth:`GtkSource.Indenter.do_is_trigger` is called upon key-press to
    determine of the key press should trigger an indentation.  The default
    implementation of the interface checks to see if the key was
    :data:`Gdk.KEY_Return` or :data:`Gdk.KEY_KP_Enter` without ``GDK_SHIFT_MASK`` set.

    :meth:`GtkSource.Indenter.do_indent` is called after text has been
    inserted into :class:`Buffer` when
    :meth:`GtkSource.Indenter.do_is_trigger` returned ``True``. The :class:`Gtk.TextIter`
    is placed directly after the inserted character or characters.

    It may be beneficial to move the insertion mark using
    :meth:`Gtk.TextBuffer.select_range` depending on how the indenter changes
    the indentation.

    All changes are encapsulated within a single user action so that the
    user may undo them using standard undo/redo accelerators.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    def indent(self, view: View, iter: Gtk.TextIter) -> Gtk.TextIter:
        """
        This function should be implemented to alter the indentation of text
        within the view.

        ``view`` is provided so that the indenter may retrieve settings such as indentation and tab widths.

        ``iter`` is the location where the indentation was requested. This typically
        is after having just inserted a newline (\\n) character but can be other
        situations such as a manually requested indentation or reformatting.

        See :meth:`GtkSource.Indenter.do_is_trigger` for how to trigger indentation on
        various characters inserted into the buffer.

        The implementor of this function is expected to keep ``iter`` valid across
        calls to the function and should contain the location of the insert mark
        after calling this function.

        The default implementation for this virtual function will copy the
        indentation of the previous line.
        """
    def is_trigger(self, view: View, location: Gtk.TextIter, state: Gdk.ModifierType | int, keyval: int) -> bool:
        """
        This function is used to determine if a key pressed should cause the
        indenter to automatically indent.

        The default implementation of this virtual method will check to see
        if ``keyval`` is :data:`Gdk.KEY_Return` or :data:`Gdk.KEY_KP_Enter` and ``state`` does
        not have ``GDK_SHIFT_MASK`` set. This is to allow the user to avoid
        indentation when Shift+Return is pressed. Other indenters may want
        to copy this behavior to provide a consistent experience to users.
        """
    def do_indent(self, view: View, iter: Gtk.TextIter) -> Gtk.TextIter:
        """
        This function should be implemented to alter the indentation of text
        within the view.

        ``view`` is provided so that the indenter may retrieve settings such as indentation and tab widths.

        ``iter`` is the location where the indentation was requested. This typically
        is after having just inserted a newline (\\n) character but can be other
        situations such as a manually requested indentation or reformatting.

        See :meth:`GtkSource.Indenter.do_is_trigger` for how to trigger indentation on
        various characters inserted into the buffer.

        The implementor of this function is expected to keep ``iter`` valid across
        calls to the function and should contain the location of the insert mark
        after calling this function.

        The default implementation for this virtual function will copy the
        indentation of the previous line.
        """
    def do_is_trigger(self, view: View, location: Gtk.TextIter, state: Gdk.ModifierType | int, keyval: int) -> bool:
        """
        This function is used to determine if a key pressed should cause the
        indenter to automatically indent.

        The default implementation of this virtual method will check to see
        if ``keyval`` is :data:`Gdk.KEY_Return` or :data:`Gdk.KEY_KP_Enter` and ``state`` does
        not have ``GDK_SHIFT_MASK`` set. This is to allow the user to avoid
        indentation when Shift+Return is pressed. Other indenters may want
        to copy this behavior to provide a consistent experience to users.
        """

class IndenterInterface:
    """
    The virtual function table for :class:`GtkSource.Indenter`.
    """
    gimeta: ClassVar[_GIMeta]
    parent_iface: GObject.TypeInterface
    is_trigger: Any
    indent: Any

class Language(GObject.Object):
    """
    Represents a syntax highlighted language.

    A :class:`GtkSource.Language` represents a programming or markup language, affecting
    syntax highlighting and [context classes](./class.Buffer.html#context-classes).

    Use :class:`LanguageManager` to obtain a :class:`GtkSource.Language` instance, and
    :meth:`Buffer.set_language` to apply it to a :class:`Buffer`.
    """
    gimeta: ClassVar[_GIMeta]
    hidden: bool
    id: str
    name: str
    section: str
    def __init__(self, **kwargs: Any) -> None: ...
    def get_globs(self) -> list[str] | None:
        """
        Returns the globs associated to this language.

        This is just an utility wrapper around :meth:`Language.get_metadata` to
        retrieve the "globs" metadata property and split it into an array.
        """
    def get_hidden(self) -> bool:
        """
        Returns whether the language should be hidden from the user.
        """
    def get_id(self) -> str:
        """
        Returns the ID of the language.

        The ID is not locale-dependent.The returned string is owned by ``language``
        and should not be freed or modified.
        """
    def get_metadata(self, name: str) -> str | None: ...
    def get_mime_types(self) -> list[str] | None:
        """
        Returns the mime types associated to this language.

        This is just an utility wrapper around :meth:`Language.get_metadata` to
        retrieve the "mimetypes" metadata property and split it into an
        array.
        """
    def get_name(self) -> str:
        """
        Returns the localized name of the language.

        The returned string is owned by ``language`` and should not be freed
        or modified.
        """
    def get_section(self) -> str:
        """
        Returns the localized section of the language.

        Each language belong to a section (ex. HTML belongs to the
        Markup section).
        The returned string is owned by ``language`` and should not be freed
        or modified.
        """
    def get_style_fallback(self, style_id: str) -> str | None:
        """
        Returns the ID of the style to use if the specified ``style_id``
        is not present in the current style scheme.
        """
    def get_style_ids(self) -> list[str] | None:
        """
        Returns the ids of the styles defined by this ``language``.
        """
    def get_style_name(self, style_id: str) -> str | None:
        """
        Returns the name of the style with ID ``style_id`` defined by this ``language``.
        """

class LanguageClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class LanguageManager(GObject.Object):
    """
    Provides access to :class:`Language`s.

    :class:`GtkSource.LanguageManager` is an object which processes language description
    files and creates and stores :class:`Language` objects, and provides API to
    access them.

    Use :func:`LanguageManager.get_default` to retrieve the default
    instance of :class:`GtkSource.LanguageManager`, and
    :meth:`LanguageManager.guess_language` to get a :class:`Language` for
    given file name and content type.
    """
    gimeta: ClassVar[_GIMeta]
    language_ids: list[str]
    search_path: list[str]
    def __init__(self, *, search_path: list[str] = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def append_search_path(self, path: str) -> None:
        """
        Appends ``path`` to the list of directories where the ``manager`` looks for
        language files.

        See :meth:`LanguageManager.set_search_path` for details.
        """
    def get_language(self, id: str) -> Language | None:
        """
        Gets the :class:`Language` identified by the given ``id`` in the language
        manager.
        """
    def get_language_ids(self) -> list[str] | None:
        """
        Returns the ids of the available languages.
        """
    def get_search_path(self) -> list[str]:
        """
        Gets the list directories where ``lm`` looks for language files.
        """
    def guess_language(self, filename: str | bytes | os.PathLike[str] | os.PathLike[bytes] | None = ..., content_type: str | None = ...) -> Language | None:
        """
        Picks a :class:`Language` for given file name and content type,
        according to the information in lang files.

        Either ``filename`` or ``content_type`` may be ``None``. This function can be used as follows:

        .. code-block:: c

            :class:`GtkSource.Language` *lang;
            :class:`GtkSource.LanguageManager` *manager;
            lm = :meth:`GtkSource.LanguageManager.get_default`;
            lang = :meth:`GtkSource.LanguageManager.guess_language` (manager, filename, NULL);
            :meth:`GtkSource.Buffer.set_language` (buffer, lang);
        .. code-block:: python

            manager = GtkSource.LanguageManager.get_default()
            language = manager.guess_language(filename=filename, content_type=None)
            buffer.set_language(language=language)

        or

        .. code-block:: c

            :class:`GtkSource.Language` *lang = NULL;
            :class:`GtkSource.LanguageManager` *manager;
            gboolean result_uncertain;
            gchar *content_type;

            content_type = g_content_type_guess (filename, NULL, 0, &result_uncertain);
            if (result_uncertain)
              {
                g_free (content_type);
                content_type = NULL;
              }

            manager = :meth:`GtkSource.LanguageManager.get_default`;
            lang = :meth:`GtkSource.LanguageManager.guess_language` (manager, filename, content_type);
            :meth:`GtkSource.Buffer.set_language` (buffer, lang);

            g_free (content_type);
        .. code-block:: python

            content_type, uncertain = Gio.content_type_guess(filename=filename, data=None)
            if uncertain:
                content_type = None

            manager = GtkSource.LanguageManager.get_default()
            language = manager.guess_language(filename=filename, content_type=content_type)
            buffer.set_language(language=language)

        etc. Use :meth:`Language.get_mime_types` and :meth:`Language.get_globs`
        if you need full control over file -> language mapping.
        """
    def prepend_search_path(self, path: str) -> None:
        """
        Prepends ``path`` to the list of directories where the ``manager`` looks
        for language files.

        See :meth:`LanguageManager.set_search_path` for details.
        """
    def set_search_path(self, dirs: list[str] | None = ...) -> None:
        """
        Sets the list of directories where the ``lm`` looks for
        language files.

        If ``dirs`` is ``None``, the search path is reset to default.

        At the moment this function can be called only before the
        language files are loaded for the first time. In practice
        to set a custom search path for a :class:`GtkSource.LanguageManager`,
        you have to call this function right after creating it.

        Since :class:`GtkSource.View` 5.4 this function will allow you to provide
        paths in the form of "resource:///" URIs to embedded ``GResource``s.
        They must contain the path of a directory within the ``GResource``.
        """
    @staticmethod
    def get_default() -> LanguageManager:
        """
        Returns the default :class:`GtkSource.LanguageManager` instance.
        """

class LanguageManagerClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class Map(View, Gtk.Accessible, Gtk.AccessibleText, Gtk.Buildable, Gtk.ConstraintTarget, Gtk.Scrollable):
    """
    Widget that displays a map for a specific :class:`View`.

    :class:`GtkSource.Map` is a widget that maps the content of a :class:`View` into
    a smaller view so the user can have a quick overview of the whole document.

    This works by connecting a :class:`View` to to the :class:`GtkSource.Map` using
    the :attr:`Map.view` property or :meth:`Map.set_view`.

    :class:`GtkSource.Map` is a :class:`View` object. This means that you can add a
    :class:`GutterRenderer` to a gutter in the same way you would for a
    :class:`View`. One example might be a :class:`GutterRenderer` that shows
    which lines have changed in the document.

    Additionally, it is desirable to match the font of the :class:`GtkSource.Map` and
    the :class:`View` used for editing. Therefore, :attr:`Map.font-desc`
    should be used to set the target font. You will need to adjust this to the
    desired font size for the map. A 1pt font generally seems to be an
    appropriate font size. "Monospace 1" is the default. See
    :meth:`Pango.FontDescription.set_size` for how to alter the size of an existing
    :class:`Pango.FontDescription`.

    When FontConfig is available, :class:`GtkSource.Map` will try to use a bundled
    "block" font to make the map more legible.
    """
    gimeta: ClassVar[_GIMeta]
    font_desc: Pango.FontDescription
    view: View
    def __init__(self, *, font_desc: Pango.FontDescription = ..., view: View = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_view(self) -> View | None:
        """
        Gets the :attr:`Map.view` property, which is the view this widget is mapping.
        """
    def set_view(self, view: View) -> None:
        """
        Sets the view that ``map`` will be doing the mapping to.
        """

class MapClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: ViewClass
    _reserved: list[int]

class Mark(Gtk.TextMark):
    """
    Mark object for :class:`Buffer`.

    A :class:`GtkSource.Mark` marks a position in the text where you want to display
    additional info. It is based on :class:`Gtk.TextMark` and thus is still valid after
    the text has changed though its position may change.

    :class:`GtkSource.Mark`s are organized in categories which you have to set
    when you create the mark. Each category can have a priority, a pixbuf and
    other associated attributes. See :meth:`View.set_mark_attributes`.
    The pixbuf will be displayed in the margin at the line where the mark
    residents if the :attr:`View.show-line-marks` property is set to ``True``. If
    there are multiple marks in the same line, the pixbufs will be drawn on top
    of each other. The mark with the highest priority will be drawn on top.
    """
    gimeta: ClassVar[_GIMeta]
    category: str
    def __init__(self, *, category: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_category(self) -> str:
        """
        Returns the mark category.
        """
    def next(self, category: str | None = ...) -> Mark | None:
        """
        Returns the next :class:`GtkSource.Mark` in the buffer or ``None`` if the mark
        was not added to a buffer.

         If there is no next mark, ``None`` will be returned.

        If ``category`` is ``None``, looks for marks of any category.
        """
    def prev(self, category: str | None = ...) -> Mark | None:
        """
        Returns the previous :class:`GtkSource.Mark` in the buffer or ``None`` if the mark
        was not added to a buffer.

        If there is no previous mark, ``None`` is returned.

        If ``category`` is ``None``, looks for marks of any category
        """

class MarkAttributes(GObject.Object):
    """
    The source mark attributes object.

    :class:`GtkSource.MarkAttributes` is an object specifying attributes used by
    a :class:`View` to visually show lines marked with :class:`Mark`s
    of a specific category. It allows you to define a background color of a line,
    an icon shown in gutter and tooltips.

    The background color is used as a background of a line where a mark is placed
    and it can be set with :meth:`MarkAttributes.set_background`. To check
    if any custom background color was defined and what color it is, use
    :meth:`MarkAttributes.get_background`.

    An icon is a graphic element which is shown in the gutter of a view. An
    example use is showing a red filled circle in a debugger to show that a
    breakpoint was set in certain line. To get an icon that will be placed in
    a gutter, first a base for it must be specified and then
    :meth:`MarkAttributes.render_icon` must be called.
    There are several ways to specify a base for an icon:

    - :meth:`MarkAttributes.set_icon_name`
    - :meth:`MarkAttributes.set_gicon`
    - :meth:`MarkAttributes.set_pixbuf`

    Using any of the above functions overrides the one used earlier. But note
    that a getter counterpart of earlier used function can still return some
    value, but it is just not used when rendering the proper icon.

    To provide meaningful tooltips for a given mark of a category, you should
    connect to ``MarkAttributes::query-tooltip-text`` or
    ``MarkAttributes::query-tooltip-markup`` where the latter
    takes precedence.
    """
    gimeta: ClassVar[_GIMeta]
    background: Gdk.RGBA
    gicon: Gio.Icon
    icon_name: str
    pixbuf: GdkPixbuf.Pixbuf
    def __init__(self, *, background: Gdk.RGBA = ..., gicon: Gio.Icon = ..., icon_name: str = ..., pixbuf: GdkPixbuf.Pixbuf = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_background(self) -> tuple[bool, Gdk.RGBA]:
        """
        Stores background color in ``background``.
        """
    def get_gicon(self) -> Gio.Icon:
        """
        Gets a :class:`Gio.Icon` to be used as a base for rendered icon.

        Note that the icon can be ``None`` if it wasn't set earlier.
        """
    def get_icon_name(self) -> str:
        """
        Gets a name of an icon to be used as a base for rendered icon.

        Note that the icon name can be ``None`` if it wasn't set earlier.
        """
    def get_pixbuf(self) -> GdkPixbuf.Pixbuf:
        """
        Gets a :class:`GdkPixbuf.Pixbuf` to be used as a base for rendered icon.

        Note that the pixbuf can be ``None`` if it wasn't set earlier.
        """
    def get_tooltip_markup(self, mark: Mark) -> str:
        """
        Queries for a tooltip by emitting a ``MarkAttributes::query-tooltip-markup`` signal.

        The tooltip may contain a markup.
        """
    def get_tooltip_text(self, mark: Mark) -> str:
        """
        Queries for a tooltip by emitting a ``MarkAttributes::query-tooltip-text`` signal.

        The tooltip is a plain text.
        """
    def render_icon(self, widget: Gtk.Widget, size: int) -> Gdk.Paintable:
        """
        Renders an icon of given size.

        The base of the icon is set by the last call to one of:

        - :meth:`MarkAttributes.set_pixbuf`
        - :meth:`MarkAttributes.set_gicon`
        - :meth:`MarkAttributes.set_icon_name`

        ``size`` cannot be lower than 1.
        """
    def set_background(self, background: Gdk.RGBA) -> None:
        """
        Sets background color to the one given in ``background``.
        """
    def set_gicon(self, gicon: Gio.Icon) -> None:
        """
        Sets an icon to be used as a base for rendered icon.
        """
    def set_icon_name(self, icon_name: str) -> None:
        """
        Sets a name of an icon to be used as a base for rendered icon.
        """
    def set_pixbuf(self, pixbuf: GdkPixbuf.Pixbuf) -> None:
        """
        Sets a pixbuf to be used as a base for rendered icon.
        """
    query_tooltip_markup: Signal[Self, [Mark], str]
    query_tooltip_text: Signal[Self, [Mark], str]
    def do_query_tooltip_markup(self, mark: Mark) -> str: ...
    def do_query_tooltip_text(self, mark: Mark) -> str: ...

class MarkAttributesClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class MarkClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.TextMarkClass
    _reserved: list[int]

class PrintCompositor(GObject.Object):
    """
    Compose a :class:`Buffer` for printing.

    The :class:`GtkSource.PrintCompositor` object is used to compose a :class:`Buffer`
    for printing. You can set various configuration options to customize the
    printed output. :class:`GtkSource.PrintCompositor` is designed to be used with the
    high-level printing API of gtk+, i.e. :class:`Gtk.PrintOperation`.

    The margins specified in this object are the layout margins: they define the
    blank space bordering the printed area of the pages. They must not be
    confused with the "print margins", i.e. the parts of the page that the
    printer cannot print on, defined in the :class:`Gtk.PageSetup` objects. If the
    specified layout margins are smaller than the "print margins", the latter
    ones are used as a fallback by the :class:`GtkSource.PrintCompositor` object, so that
    the printed area is not clipped.
    """
    gimeta: ClassVar[_GIMeta]
    body_font_name: str
    buffer: Buffer
    footer_font_name: str
    header_font_name: str
    highlight_syntax: bool
    line_numbers_font_name: str
    n_pages: int
    print_footer: bool
    print_header: bool
    print_line_numbers: int
    tab_width: int
    wrap_mode: Gtk.WrapMode | int
    def __init__(self, *, body_font_name: str = ..., buffer: Buffer = ..., footer_font_name: str = ..., header_font_name: str = ..., highlight_syntax: bool = ..., line_numbers_font_name: str = ..., print_footer: bool = ..., print_header: bool = ..., print_line_numbers: int = ..., tab_width: int = ..., wrap_mode: Gtk.WrapMode | int = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_from_view(cls, view: View) -> Self:
        """
        Creates a new print compositor that can be used to print the buffer
        associated with ``view``.

        This constructor sets some configuration properties to make the
        printed output match ``view`` as much as possible.  The properties set are
        :attr:`PrintCompositor.tab-width`, :attr:`PrintCompositor.highlight-syntax`,
        :attr:`PrintCompositor.wrap-mode`, :attr:`PrintCompositor.body-font-name` and
        :attr:`PrintCompositor.print-line-numbers`.
        """
    def draw_page(self, context: Gtk.PrintContext, page_nr: int) -> None:
        """
        Draw page ``page_nr`` for printing on the the Cairo context encapsuled in ``context``.

        This method has been designed to be called in the handler of the ``Gtk.PrintOperation::draw_page`` signal
        as shown in the following example:

        .. code-block:: c

            // Signal handler for the GtkPrintOperation::draw_page signal

            static void
            draw_page (GtkPrintOperation *operation,
                       GtkPrintContext   *context,
                       gint               page_nr,
                       gpointer           user_data)
            {
                :class:`GtkSource.PrintCompositor` *compositor;

                compositor = GTK_SOURCE_PRINT_COMPOSITOR (user_data);

                :meth:`GtkSource.PrintCompositor.draw_page` (compositor,
                                                       context,
                                                       page_nr);
            }
        .. code-block:: python

            def on_draw_page(
                operation: Gtk.PrintOperation,
                context: Gtk.PrintContext,
                page_nr: int,
                compositor: GtkSource.PrintCompositor,
            ) -> None:
                \"\"\"Signal handler for draw-page that renders a single page.\"\"\"
                compositor.draw_page(context=context, page_nr=page_nr)
        """
    def get_body_font_name(self) -> str:
        """
        Returns the name of the font used to print the text body.

        The returned string must be freed with g_free().
        """
    def get_bottom_margin(self, unit: Gtk.Unit | int) -> float:
        """
        Gets the bottom margin in units of ``unit``.
        """
    def get_buffer(self) -> Buffer:
        """
        Gets the :class:`Buffer` associated with the compositor.

        The returned object reference is owned by the compositor object and
        should not be unreferenced.
        """
    def get_footer_font_name(self) -> str:
        """
        Returns the name of the font used to print the page footer.

        The returned string must be freed with g_free().
        """
    def get_header_font_name(self) -> str:
        """
        Returns the name of the font used to print the page header.

        The returned string must be freed with g_free().
        """
    def get_highlight_syntax(self) -> bool:
        """
        Determines whether the printed text will be highlighted according to the
        buffer rules.

        Note that highlighting will happen only if the buffer to print has highlighting activated.
        """
    def get_left_margin(self, unit: Gtk.Unit | int) -> float:
        """
        Gets the left margin in units of ``unit``.
        """
    def get_line_numbers_font_name(self) -> str:
        """
        Returns the name of the font used to print line numbers on the left margin.

        The returned string must be freed with g_free().
        """
    def get_n_pages(self) -> int:
        """
        Returns the number of pages in the document or <code>-1</code> if the
        document has not been completely paginated.
        """
    def get_pagination_progress(self) -> float:
        """
        Returns the current fraction of the document pagination that has been completed.
        """
    def get_print_footer(self) -> bool:
        """
        Determines if a footer is set to be printed for each page.

        A footer will be printed if this function returns ``True``
        **and** some format strings have been specified
        with :meth:`PrintCompositor.set_footer_format`.
        """
    def get_print_header(self) -> bool:
        """
        Determines if a header is set to be printed for each page.

        A header will be printed if this function returns ``True``
        **and** some format strings have been specified
        with :meth:`PrintCompositor.set_header_format`.
        """
    def get_print_line_numbers(self) -> int:
        """
        Returns the interval used for line number printing.

        If the value is 0, no line numbers will be printed. The default value is
        1 (i.e. numbers printed in all lines).
        """
    def get_right_margin(self, unit: Gtk.Unit | int) -> float:
        """
        Gets the right margin in units of ``unit``.
        """
    def get_tab_width(self) -> int:
        """
        Returns the width of tabulation in characters for printed text.
        """
    def get_top_margin(self, unit: Gtk.Unit | int) -> float:
        """
        Gets the top margin in units of ``unit``.
        """
    def get_wrap_mode(self) -> Gtk.WrapMode:
        """
        Gets the line wrapping mode for the printed text.
        """
    def ignore_tag(self, tag: Gtk.TextTag) -> None:
        """
        Specifies a tag whose style should be ignored when compositing the
        document to the printable page.
        """
    def paginate(self, context: Gtk.PrintContext) -> bool:
        """
        Paginate the document associated with the ``compositor``.

        In order to support non-blocking pagination, document is paginated in small chunks.
        Each time :meth:`PrintCompositor.paginate` is invoked, a chunk of the document
        is paginated. To paginate the entire document, :meth:`PrintCompositor.paginate`
        must be invoked multiple times.
        It returns ``True`` if the document has been completely paginated, otherwise it returns ``False``.

        This method has been designed to be invoked in the handler of the ``Gtk.PrintOperation::paginate`` signal,
        as shown in the following example:

        .. code-block:: c

            // Signal handler for the GtkPrintOperation::paginate signal

            static gboolean
            paginate (GtkPrintOperation *operation,
                      GtkPrintContext   *context,
                      gpointer           user_data)
            {
                :class:`GtkSource.PrintCompositor` *compositor;

                compositor = GTK_SOURCE_PRINT_COMPOSITOR (user_data);

                if (:meth:`GtkSource.PrintCompositor.paginate` (compositor, context))
                {
                    gint n_pages;

                    n_pages = :meth:`GtkSource.PrintCompositor.get_n_pages` (compositor);
                    gtk_print_operation_set_n_pages (operation, n_pages);

                    return TRUE;
                }

                return FALSE;
            }
        .. code-block:: python

            def on_paginate(
                operation: Gtk.PrintOperation,
                context: Gtk.PrintContext,
                compositor: GtkSource.PrintCompositor,
            ) -> bool:
                if compositor.paginate(context=context):
                    n_pages = compositor.get_n_pages()
                    operation.set_n_pages(n_pages=n_pages)
                    return True
                return False

        If you don't need to do pagination in chunks, you can simply do it all in the
        ``Gtk.PrintOperation::begin-print`` handler, and set the number of pages from there, like
        in the following example:

        .. code-block:: c

            // Signal handler for the GtkPrintOperation::begin-print signal

            static void
            begin_print (GtkPrintOperation *operation,
                         GtkPrintContext   *context,
                         gpointer           user_data)
            {
                :class:`GtkSource.PrintCompositor` *compositor;
                gint n_pages;

                compositor = GTK_SOURCE_PRINT_COMPOSITOR (user_data);

                while (!:meth:`GtkSource.PrintCompositor.paginate` (compositor, context));

                n_pages = :meth:`GtkSource.PrintCompositor.get_n_pages` (compositor);
                gtk_print_operation_set_n_pages (operation, n_pages);
            }
        .. code-block:: python

            def on_begin_print(
                operation: Gtk.PrintOperation,
                context: Gtk.PrintContext,
                compositor: GtkSource.PrintCompositor,
            ) -> None:
                # Paginate until complete
                while not compositor.paginate(context=context):
                    pass

                n_pages = compositor.get_n_pages()
                operation.set_n_pages(n_pages=n_pages)
        """
    def set_body_font_name(self, font_name: str) -> None:
        """
        Sets the default font for the printed text.

        ``font_name`` should be a
        string representation of a font description Pango can understand.
        (e.g. &quot;Monospace 10&quot;). See :func:`Pango.FontDescription.from_string`
        for a description of the format of the string representation.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_bottom_margin(self, margin: float, unit: Gtk.Unit | int) -> None:
        """
        Sets the bottom margin used by ``compositor``.
        """
    def set_footer_font_name(self, font_name: str | None = ...) -> None:
        """
        Sets the font for printing the page footer.

        If ``None`` is supplied, the default font (i.e. the one being used for the
        text) will be used instead.

        ``font_name`` should be a
        string representation of a font description Pango can understand.
        (e.g. &quot;Monospace 10&quot;). See :func:`Pango.FontDescription.from_string`
        for a description of the format of the string representation.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_footer_format(self, separator: bool, left: str | None = ..., center: str | None = ..., right: str | None = ...) -> None:
        """
        See :meth:`PrintCompositor.set_header_format` for more information
        about the parameters.
        """
    def set_header_font_name(self, font_name: str | None = ...) -> None:
        """
        Sets the font for printing the page header.

        If ``None`` is supplied, the default font (i.e. the one being used for the
        text) will be used instead.

        ``font_name`` should be a
        string representation of a font description Pango can understand.
        (e.g. &quot;Monospace 10&quot;). See :func:`Pango.FontDescription.from_string`
        for a description of the format of the string representation.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_header_format(self, separator: bool, left: str | None = ..., center: str | None = ..., right: str | None = ...) -> None:
        """
        Sets strftime like header format strings, to be printed on the
        left, center and right of the top of each page.

        The strings may include strftime(3) codes which will be expanded at print time.
        A subset of strftime() codes are accepted, see :meth:`GLib.DateTime.format`
        for more details on the accepted format specifiers.
        Additionally the following format specifiers are accepted:

        - :class:`N:` the page number
        - :class:`Q:` the page count.

        ``separator`` specifies if a solid line should be drawn to separate
        the header from the document text.

        If ``None`` is given for any of the three arguments, that particular
        string will not be printed.

        For the header to be printed, in
        addition to specifying format strings, you need to enable header
        printing with :meth:`PrintCompositor.set_print_header`.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_highlight_syntax(self, highlight: bool) -> None:
        """
        Sets whether the printed text will be highlighted according to the
        buffer rules.  Both color and font style are applied.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_left_margin(self, margin: float, unit: Gtk.Unit | int) -> None:
        """
        Sets the left margin used by ``compositor``.
        """
    def set_line_numbers_font_name(self, font_name: str | None = ...) -> None:
        """
        Sets the font for printing line numbers on the left margin.

        If ``None`` is supplied, the default font (i.e. the one being used for the
        text) will be used instead.

        ``font_name`` should be a
        string representation of a font description Pango can understand.
        (e.g. &quot;Monospace 10&quot;). See :func:`Pango.FontDescription.from_string`
        for a description of the format of the string representation.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_print_footer(self, print: bool) -> None:
        """
        Sets whether you want to print a footer in each page.

        The footer consists of three pieces of text and an optional line
        separator, configurable with
        :meth:`PrintCompositor.set_footer_format`.

        Note that by default the footer format is unspecified, and if it's
        empty it will not be printed, regardless of this setting.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_print_header(self, print: bool) -> None:
        """
        Sets whether you want to print a header in each page.

        The header consists of three pieces of text and an optional line
        separator, configurable with :meth:`PrintCompositor.set_header_format`.

        Note that by default the header format is unspecified, and if it's
        empty it will not be printed, regardless of this setting.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_print_line_numbers(self, interval: int) -> None:
        """
        Sets the interval for printed line numbers.

        If ``interval`` is 0 no numbers will be printed. If greater than 0, a number will be
        printed every ``interval`` lines (i.e. 1 will print all line numbers).

        Maximum accepted value for ``interval`` is 100.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_right_margin(self, margin: float, unit: Gtk.Unit | int) -> None:
        """
        Sets the right margin used by ``compositor``.
        """
    def set_tab_width(self, width: int) -> None:
        """
        Sets the width of tabulation in characters for printed text.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """
    def set_top_margin(self, margin: float, unit: Gtk.Unit | int) -> None:
        """
        Sets the top margin used by ``compositor``.
        """
    def set_wrap_mode(self, wrap_mode: Gtk.WrapMode | int) -> None:
        """
        Sets the line wrapping mode for the printed text.

        This function cannot be called anymore after the first call to the
        :meth:`PrintCompositor.paginate` function.
        """

class PrintCompositorClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class Region(GObject.Object):
    """
    Region utility.

    A :class:`GtkSource.Region` permits to store a group of subregions of a
    :class:`Gtk.TextBuffer`. :class:`GtkSource.Region` stores the subregions with pairs of
    :class:`Gtk.TextMark`'s, so the region is still valid after insertions and deletions
    in the :class:`Gtk.TextBuffer`.

    The :class:`Gtk.TextMark` for the start of a subregion has a left gravity, while the
    :class:`Gtk.TextMark` for the end of a subregion has a right gravity.

    The typical use-case of :class:`GtkSource.Region` is to scan a :class:`Gtk.TextBuffer` chunk by
    chunk, not the whole buffer at once to not block the user interface. The
    :class:`GtkSource.Region` represents in that case the remaining region to scan. You
    can listen to the ``Gtk.TextBuffer::insert-text`` and
    ``Gtk.TextBuffer::delete-range`` signals to update the :class:`GtkSource.Region`
    accordingly.

    To iterate through the subregions, you need to use a :class:`RegionIter`,
    for example:
    .. code-block:: c

        :class:`GtkSource.Region` *region;
        :class:`GtkSource.RegionIter` region_iter;

        :meth:`GtkSource.Region.get_start_region_iter` (region, &region_iter);

        while (!:meth:`GtkSource.RegionIter.is_end` (&region_iter))
        {
                GtkTextIter subregion_start;
                GtkTextIter subregion_end;

                if (!:meth:`GtkSource.RegionIter.get_subregion` (&region_iter,
                                                           &subregion_start,
                                                           &subregion_end))
                {
                        break;
                }

                // Do something useful with the subregion.

                :meth:`GtkSource.RegionIter.next` (&region_iter);
        }
    .. code-block:: python

        buffer: GtkSource.Buffer = GtkSource.Buffer()
        region: GtkSource.Region = GtkSource.Region(buffer=buffer)
        region_iter = region.get_start_region_iter()

        while not region_iter.is_end():
            success, start, end = region_iter.get_subregion()
            if not success:
                break

            # Do something useful with the subregion

            region_iter.next()
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Gtk.TextBuffer
    def __init__(self, *, buffer: Gtk.TextBuffer = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def add_region(self, region_to_add: Region | None = ...) -> None:
        """
        Adds ``region_to_add`` to ``region``.

        ``region_to_add`` is not modified.
        """
    def add_subregion(self, _start: Gtk.TextIter, _end: Gtk.TextIter) -> None:
        """
        Adds the subregion delimited by ``_start`` and ``_end`` to ``region``.
        """
    def get_bounds(self) -> tuple[bool, Gtk.TextIter, Gtk.TextIter]:
        """
        Gets the ``start`` and ``end`` bounds of the ``region``.
        """
    def get_buffer(self) -> Gtk.TextBuffer | None: ...
    def get_start_region_iter(self) -> RegionIter:
        """
        Initializes a :class:`RegionIter` to the first subregion of ``region``.

        If ``region`` is empty, ``iter`` will be initialized to the end iterator.
        """
    def intersect_region(self, region2: Region | None = ...) -> Region | None:
        """
        Returns the intersection between ``region1`` and ``region2``.

        ``region1`` and ``region2`` are not modified.
        """
    def intersect_subregion(self, _start: Gtk.TextIter, _end: Gtk.TextIter) -> Region | None:
        """
        Returns the intersection between ``region`` and the subregion delimited by
        ``_start`` and ``_end``.

        ``region`` is not modified.
        """
    def is_empty(self) -> bool:
        """
        Returns whether the ``region`` is empty.

        A ``None`` ``region`` is considered empty.
        """
    def subtract_region(self, region_to_subtract: Region | None = ...) -> None:
        """
        Subtracts ``region_to_subtract`` from ``region``.

        ``region_to_subtract`` is not modified.
        """
    def subtract_subregion(self, _start: Gtk.TextIter, _end: Gtk.TextIter) -> None:
        """
        Subtracts the subregion delimited by ``_start`` and ``_end`` from ``region``.
        """
    def to_string(self) -> str | None:
        """
        Gets a string represention of ``region``, for debugging purposes.

        The returned string contains the character offsets of the subregions. It
        doesn't include a newline character at the end of the string.
        """

class RegionClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class RegionIter:
    """
    An opaque datatype.

    Ignore all its fields and initialize the iter with :meth:`Region.get_start_region_iter`.
    """
    gimeta: ClassVar[_GIMeta]
    dummy1: int
    dummy2: int
    dummy3: int
    def get_subregion(self) -> tuple[bool, Gtk.TextIter, Gtk.TextIter]:
        """
        Gets the subregion at this iterator.
        """
    def is_end(self) -> bool: ...
    def next(self) -> bool:
        """
        Moves ``iter`` to the next subregion.
        """

class SearchContext(GObject.Object):
    """
    Search context.

    A :class:`GtkSource.SearchContext` is used for the search and replace in a
    :class:`Buffer`. The search settings are represented by a
    :class:`SearchSettings` object. There can be a many-to-many relationship
    between buffers and search settings, with the search contexts in-between: a
    search settings object can be shared between several search contexts; and a
    buffer can contain several search contexts at the same time.

    The total number of search occurrences can be retrieved with
    :meth:`SearchContext.get_occurrences_count`. To know the position of a
    certain match, use :meth:`SearchContext.get_occurrence_position`.

    The buffer is scanned asynchronously, so it doesn't block the user interface.
    For each search, the buffer is scanned at most once. After that, navigating
    through the occurrences doesn't require to re-scan the buffer entirely.

    To search forward, use :meth:`SearchContext.forward` or
    :meth:`SearchContext.forward_async` for the asynchronous version.
    The backward search is done similarly. To replace a search match, or all
    matches, use :meth:`SearchContext.replace` and
    :meth:`SearchContext.replace_all`.

    The search occurrences are highlighted by default. To disable it, use
    :meth:`SearchContext.set_highlight`. You can enable the search
    highlighting for several :class:`GtkSource.SearchContext`s attached to the
    same buffer. Moreover, each of those :class:`GtkSource.SearchContext`s can
    have a different text style associated. Use
    :meth:`SearchContext.set_match_style` to specify the :class:`Style`
    to apply on search matches.

    Note that the :attr:`SearchContext.highlight` and
    :attr:`SearchContext.match-style` properties are in the
    :class:`GtkSource.SearchContext` class, not :class:`SearchSettings`. Appearance
    settings should be tied to one, and only one buffer, as different buffers can
    have different style scheme associated (a :class:`SearchSettings` object
    can be bound indirectly to several buffers).

    The concept of "current match" doesn't exist yet. A way to highlight
    differently the current match is to select it.

    A search occurrence's position doesn't depend on the cursor position or other
    parameters. Take for instance the buffer "aaaa" with the search text "aa".
    The two occurrences are at positions [0:2] and [2:4]. If you begin to search
    at position 1, you will get the occurrence [2:4], not [1:3]. This is a
    prerequisite for regular expression searches. The pattern ".*" matches the
    entire line. If the cursor is at the middle of the line, you don't want the
    rest of the line as the occurrence, you want an entire line. (As a side note,
    regular expression searches can also match multiple lines.)

    In the :class:`GtkSource.View` source code, there is an example of how to use the
    search and replace API: see the tests/test-search.c file. It is a mini
    application for the search and replace, with a basic user interface.
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Buffer
    highlight: bool
    match_style: Style
    occurrences_count: int
    regex_error: GLib.Error
    settings: SearchSettings
    def __init__(self, *, buffer: Buffer = ..., highlight: bool = ..., match_style: Style = ..., settings: SearchSettings = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def backward(self, iter: Gtk.TextIter) -> tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]:
        """
        Synchronous backward search.

        It is recommended to use the asynchronous functions instead, to not block the user interface.
        However, if you are sure that the ``buffer`` is small, this function is more convenient to use.

        If the :attr:`SearchSettings.wrap-around` property is ``False``, this function
        doesn't try to wrap around.

        The ``has_wrapped_around`` out parameter is set independently of whether a match
        is found. So if this function returns ``False``, ``has_wrapped_around`` will have
        the same value as the :attr:`SearchSettings.wrap-around` property.
        """
    @overload
    def backward_async(self, iter: Gtk.TextIter, cancellable: Gio.Cancellable | None = ..., callback: None = ...) -> Awaitable[tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]]:
        """
        The asynchronous version of :meth:`SearchContext.backward`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.

        If the operation is cancelled, the ``callback`` will only be called if
        ``cancellable`` was not ``None``. The method takes
        ownership of ``cancellable``, so you can unref it after calling this function.
        """
    @overload
    def backward_async(self, iter: Gtk.TextIter, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        The asynchronous version of :meth:`SearchContext.backward`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.

        If the operation is cancelled, the ``callback`` will only be called if
        ``cancellable`` was not ``None``. The method takes
        ownership of ``cancellable``, so you can unref it after calling this function.
        """
    def backward_finish(self, result: Gio.AsyncResult) -> tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]:
        """
        Finishes a backward search started with
        :meth:`SearchContext.backward_async`.

        See the documentation of :meth:`SearchContext.backward` for more
        details.
        """
    def forward(self, iter: Gtk.TextIter) -> tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]:
        """
        Synchronous forward search.

        It is recommended to use the asynchronous functions instead, to not block the user interface.
        However, if you are sure that the ``buffer`` is small, this function is more convenient to use.

        If the :attr:`SearchSettings.wrap-around` property is ``False``, this function
        doesn't try to wrap around.

        The ``has_wrapped_around`` out parameter is set independently of whether a match
        is found. So if this function returns ``False``, ``has_wrapped_around`` will have
        the same value as the  :attr:`SearchSettings.wrap-around` property.
        """
    @overload
    def forward_async(self, iter: Gtk.TextIter, cancellable: Gio.Cancellable | None = ..., callback: None = ...) -> Awaitable[tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]]:
        """
        The asynchronous version of :meth:`SearchContext.forward`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.

        If the operation is cancelled, the ``callback`` will only be called if
        ``cancellable`` was not ``None``. The method takes
        ownership of ``cancellable``, so you can unref it after calling this function.
        """
    @overload
    def forward_async(self, iter: Gtk.TextIter, cancellable: Gio.Cancellable | None = ..., callback: Gio.AsyncReadyCallback = ...) -> None:
        """
        The asynchronous version of :meth:`SearchContext.forward`.

        See the :class:`Gio.AsyncResult` documentation to know how to use this function.

        If the operation is cancelled, the ``callback`` will only be called if
        ``cancellable`` was not ``None``. The method takes
        ownership of ``cancellable``, so you can unref it after calling this function.
        """
    def forward_finish(self, result: Gio.AsyncResult) -> tuple[bool, Gtk.TextIter, Gtk.TextIter, bool]:
        """
        Finishes a forward search started with :meth:`SearchContext.forward_async`.

        See the documentation of :meth:`SearchContext.forward` for more
        details.
        """
    def get_buffer(self) -> Buffer: ...
    def get_highlight(self) -> bool: ...
    def get_match_style(self) -> Style | None: ...
    def get_occurrence_position(self, match_start: Gtk.TextIter, match_end: Gtk.TextIter) -> int:
        """
        Gets the position of a search occurrence.

        If the buffer is not already fully scanned, the position may be unknown,
        and -1 is returned. If 0 is returned, it means that this part of the buffer
        has already been scanned, and that ``match_start`` and ``match_end`` don't delimit an occurrence.
        """
    def get_occurrences_count(self) -> int:
        """
        Gets the total number of search occurrences.

        If the buffer is not already fully scanned, the total number of occurrences is
        unknown, and -1 is returned.
        """
    def get_regex_error(self) -> GLib.Error | None:
        """
        Regular expression patterns must follow certain rules. If
        :attr:`SearchSettings.search-text` breaks a rule, the error can be
        retrieved with this function.

        The error domain is :class:`GLib.RegexError`.

        Free the return value with :meth:`GLib.Error.free`.
        """
    def get_settings(self) -> SearchSettings: ...
    def replace(self, match_start: Gtk.TextIter, match_end: Gtk.TextIter, replace: str, replace_length: int) -> bool:
        """
        Replaces a search match by another text. If ``match_start`` and ``match_end``
        doesn't correspond to a search match, ``False`` is returned.

        ``match_start`` and ``match_end`` iters are revalidated to point to the replacement
        text boundaries.

        For a regular expression replacement, you can check if ``replace`` is valid by
        calling :func:`GLib.Regex.check_replacement`. The ``replace`` text can contain
        backreferences.
        """
    def replace_all(self, replace: str, replace_length: int) -> int:
        """
        Replaces all search matches by another text.

        It is a synchronous function, so it can block the user interface.

        For a regular expression replacement, you can check if ``replace`` is valid by
        calling :func:`GLib.Regex.check_replacement`. The ``replace`` text can contain
        backreferences.
        """
    def set_highlight(self, highlight: bool) -> None:
        """
        Enables or disables the search occurrences highlighting.
        """
    def set_match_style(self, match_style: Style | None = ...) -> None:
        """
        Set the style to apply on search matches.

        If ``match_style`` is ``None``, default theme's scheme 'match-style' will be used.
        To enable or disable the search highlighting, use :meth:`SearchContext.set_highlight`.
        """

class SearchContextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class SearchSettings(GObject.Object):
    """
    Search settings.

    A :class:`GtkSource.SearchSettings` object represents the settings of a search. The
    search settings can be associated with one or several
    :class:`SearchContext`s.
    """
    gimeta: ClassVar[_GIMeta]
    at_word_boundaries: bool
    case_sensitive: bool
    regex_enabled: bool
    search_text: str
    visible_only: bool
    wrap_around: bool
    def __init__(self, *, at_word_boundaries: bool = ..., case_sensitive: bool = ..., regex_enabled: bool = ..., search_text: str = ..., visible_only: bool = ..., wrap_around: bool = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_at_word_boundaries(self) -> bool: ...
    def get_case_sensitive(self) -> bool: ...
    def get_regex_enabled(self) -> bool: ...
    def get_search_text(self) -> str | None:
        """
        Gets the text to search.

        The return value must not be freed.

        You may be interested to call :func:`utils_escape_search_text` after
        this function.
        """
    def get_visible_only(self) -> bool: ...
    def get_wrap_around(self) -> bool: ...
    def set_at_word_boundaries(self, at_word_boundaries: bool) -> None:
        """
        Change whether the search is done at word boundaries.

        If ``at_word_boundaries`` is ``True``, a search match must start and end a word.
        The match can span multiple words. See also :meth:`Gtk.TextIter.starts_word` and
        :meth:`Gtk.TextIter.ends_word`.
        """
    def set_case_sensitive(self, case_sensitive: bool) -> None:
        """
        Enables or disables the case sensitivity for the search.
        """
    def set_regex_enabled(self, regex_enabled: bool) -> None:
        """
        Enables or disables whether to search by regular expressions.

        If enabled, the :attr:`SearchSettings.search-text` property contains the
        pattern of the regular expression.

        :class:`SearchContext` uses :class:`GRegex` when regex search is enabled. See the
        `Regular expression syntax <https://developer.gnome.org/glib/stable/glib-regex-syntax.html>`_
        page in the GLib reference manual.
        """
    def set_search_text(self, search_text: str | None = ...) -> None:
        """
        Sets the text to search.

        If ``search_text`` is ``None`` or is empty, the search will be disabled. A copy of ``search_text``
        will be made, so you can safely free ``search_text`` after a call to this function.

        You may be interested to call :func:`utils_unescape_search_text` before
        this function.
        """
    def set_visible_only(self, visible_only: bool) -> None:
        """
        Enables or disables whether to exclude invisible text from the search.

        If enabled, only visible text will be searched.
        A search match may have invisible text interspersed.
        """
    def set_wrap_around(self, wrap_around: bool) -> None:
        """
        Enables or disables the wrap around search.

        If ``wrap_around`` is ``True``, the forward search continues at the beginning of the buffer
        if no search occurrences are found. Similarly, the backward search continues to search at
        the end of the buffer.
        """

class SearchSettingsClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass
    _reserved: list[int]

class Snippet(GObject.Object):
    """
    Quick insertion code snippets.

    The :class:`GtkSource.Snippet` represents a series of chunks that can quickly be
    inserted into the :class:`View`.

    Snippets are defined in XML files which are loaded by the
    :class:`SnippetManager`. Alternatively, applications can create snippets
    on demand and insert them into the :class:`View` using
    :meth:`View.push_snippet`.

    Snippet chunks can reference other snippet chunks as well as post-process
    the values from other chunks such as capitalization.
    """
    gimeta: ClassVar[_GIMeta]
    buffer: Gtk.TextBuffer
    description: str
    focus_position: int
    language_id: str
    name: str
    trigger: str
    def __init__(self, *, description: str = ..., language_id: str = ..., name: str = ..., trigger: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_parsed(cls, text: str) -> Self:
        """
        Parses the snippet formatted ``text`` into a series of chunks and adds them
        to a new :class:`GtkSource.Snippet`.
        """
    def add_chunk(self, chunk: SnippetChunk) -> None:
        """
        Appends ``chunk`` to the ``snippet``.

        This may only be called before the snippet has been expanded.
        """
    def copy(self) -> Snippet:
        """
        Does a deep copy of the snippet.
        """
    def get_context(self) -> SnippetContext | None:
        """
        Gets the context used for expanding the snippet.
        """
    def get_description(self) -> str:
        """
        Gets the description for the snippet.
        """
    def get_focus_position(self) -> int:
        """
        Gets the current focus for the snippet.

        This is changed as the user tabs through focus locations.
        """
    def get_language_id(self) -> str:
        """
        Gets the language-id used for the source snippet.

        The language identifier should be one that matches a
        source language :attr:`Language.id` property.
        """
    def get_n_chunks(self) -> int:
        """
        Gets the number of chunks in the snippet.

        Note that not all chunks are editable.
        """
    def get_name(self) -> str:
        """
        Gets the name for the snippet.
        """
    def get_nth_chunk(self, nth: int) -> SnippetChunk:
        """
        Gets the chunk at ``nth``.
        """
    def get_trigger(self) -> str | None:
        """
        Gets the trigger for the source snippet.

        A trigger is a word that can be expanded into the full snippet when
        the user presses Tab.
        """
    def set_description(self, description: str) -> None:
        """
        Sets the description for the snippet.
        """
    def set_language_id(self, language_id: str) -> None:
        """
        Sets the language identifier for the snippet.

        This should match the :attr:`Language.id` identifier.
        """
    def set_name(self, name: str) -> None:
        """
        Sets the name for the snippet.
        """
    def set_trigger(self, trigger: str) -> None:
        """
        Sets the trigger for the snippet.
        """

class SnippetChunk(GObject.InitiallyUnowned):
    """
    A chunk of text within the source snippet.

    The :class:`GtkSource.SnippetChunk` represents a single chunk of text that
    may or may not be an edit point within the snippet. Chunks that are
    an edit point (also called a tab stop) have the
    :attr:`SnippetChunk.focus-position` property set.
    """
    gimeta: ClassVar[_GIMeta]
    context: SnippetContext
    focus_position: int
    spec: str
    text: str
    text_set: bool
    tooltip_text: str
    def __init__(self, *, context: SnippetContext = ..., focus_position: int = ..., spec: str = ..., text: str = ..., text_set: bool = ..., tooltip_text: str = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def copy(self) -> SnippetChunk:
        """
        Copies the source snippet.
        """
    def get_context(self) -> SnippetContext:
        """
        Gets the context for the snippet insertion.
        """
    def get_focus_position(self) -> int:
        """
        Gets the :attr:`SnippetChunk.focus-position`.

        The focus-position is used to determine how many tabs it takes for the
        snippet to advanced to this chunk.

        A focus-position of zero will be the last focus position of the snippet
        and snippet editing ends when it has been reached.

        A focus-position of -1 means the chunk cannot be focused by the user.
        """
    def get_spec(self) -> str | None:
        """
        Gets the specification for the chunk.

        The specification is evaluated for variables when other chunks are edited
        within the snippet context. If the user has changed the text, the
        :attr:`SnippetChunk.text` and :attr:`SnippetChunk.text-set` properties
        are updated.
        """
    def get_text(self) -> str:
        """
        Gets the :attr:`SnippetChunk.text` property.

        The text property is updated when the user edits the text of the chunk.
        If it has not been edited, the :attr:`SnippetChunk.spec` property is
        returned.
        """
    def get_text_set(self) -> bool:
        """
        Gets the :attr:`SnippetChunk.text-set` property.

        This is typically set when the user has edited a snippet chunk.
        """
    def get_tooltip_text(self) -> str: ...
    def set_context(self, context: SnippetContext) -> None: ...
    def set_focus_position(self, focus_position: int) -> None:
        """
        Sets the :attr:`SnippetChunk.focus-position` property.

        The focus-position is used to determine how many tabs it takes for the
        snippet to advanced to this chunk.

        A focus-position of zero will be the last focus position of the snippet
        and snippet editing ends when it has been reached.

        A focus-position of -1 means the chunk cannot be focused by the user.
        """
    def set_spec(self, spec: str) -> None:
        """
        Sets the specification for the chunk.

        The specification is evaluated for variables when other chunks are edited
        within the snippet context. If the user has changed the text, the
        :attr:`SnippetChunk.text and` :attr:`SnippetChunk.text-set` properties
        are updated.
        """
    def set_text(self, text: str) -> None:
        """
        Sets the text for the snippet chunk.

        This is usually used by the snippet engine to update the text, but may
        be useful when creating custom snippets to avoid expansion of any
        specification.
        """
    def set_text_set(self, text_set: bool) -> None:
        """
        Sets the :attr:`SnippetChunk.text-set` property.

        This is typically set when the user has edited a snippet chunk by the
        snippet engine.
        """
    def set_tooltip_text(self, tooltip_text: str) -> None: ...

class SnippetChunkClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.InitiallyUnownedClass

class SnippetClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class SnippetContext(GObject.Object):
    """
    Context for expanding :class:`SnippetChunk`.

    This class is currently used primary as a hashtable. However, the longer
    term goal is to have it hold onto a ``GjsContext`` as well as other languages
    so that :class:`SnippetChunk` can expand themselves by executing
    script within the context.

    The :class:`Snippet` will build the context and then expand each of the
    chunks during the insertion/edit phase.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def clear_variables(self) -> None:
        """
        Removes all variables from the context.
        """
    def expand(self, input: str) -> str: ...
    def get_variable(self, key: str) -> str | None:
        """
        Gets the current value for a variable named ``key``.
        """
    def set_constant(self, key: str, value: str) -> None:
        """
        Sets a constatnt within the context.

        This is similar to a variable set with :meth:`SnippetContext.set_variable`
        but is expected to not change during use of the snippet.

        Examples would be the date or users name.
        """
    def set_line_prefix(self, line_prefix: str) -> None: ...
    def set_tab_width(self, tab_width: int) -> None: ...
    def set_use_spaces(self, use_spaces: bool) -> None: ...
    def set_variable(self, key: str, value: str) -> None:
        """
        Sets a variable within the context.

        This variable may be overridden by future updates to the
        context.
        """
    changed: Signal[Self, [], None]
    def do_changed(self) -> None: ...

class SnippetContextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class SnippetManager(GObject.Object):
    """
    Provides access to :class:`Snippet`.

    :class:`GtkSource.SnippetManager` is an object which processes snippet description
    files and creates :class:`Snippet` objects.

    Use :func:`SnippetManager.get_default` to retrieve the default
    instance of :class:`GtkSource.SnippetManager`.

    Use :meth:`SnippetManager.get_snippet` to retrieve snippets for
    a given snippets.
    """
    gimeta: ClassVar[_GIMeta]
    search_path: list[str]
    def __init__(self, *, search_path: list[str] = ..., **kwargs: Any) -> None: ...
    def get_search_path(self) -> list[str]:
        """
        Gets the list directories where ``self`` looks for snippet files.
        """
    def get_snippet(self, group: str | None, language_id: str | None, trigger: str) -> Snippet | None:
        """
        Queries the known snippets for the first matching ``group``, ``language_id``,
        and/or ``trigger``.

        If ``group`` or ``language_id`` are ``None``, they will be ignored.
        """
    def list_all(self) -> Gio.ListModel:
        """
        Gets a :class:`Gio.ListModel` of all snippets.

        This can be used to get an unfiltered list of all of the snippets
        known to the snippet manager.
        """
    def list_groups(self) -> list[str]:
        """
        List all the known groups within the snippet manager.

        The result should be freed with g_free(), and the individual strings are
        owned by ``self`` and should never be freed by the caller.
        """
    def list_matching(self, group: str | None = ..., language_id: str | None = ..., trigger_prefix: str | None = ...) -> Gio.ListModel:
        """
        Queries the known snippets for those matching ``group``, ``language_id``, and/or
        ``trigger_prefix``.

        If any of these are ``None``, they will be ignored when filtering the available snippets.

        The :class:`Gio.ListModel` only contains information about the available snippets until
        :meth:`Gio.ListModel.get_item` is called for a specific snippet. This helps reduce
        the number of :class:`GObject.Object`'s that are created at runtime to those needed by
        the calling application.
        """
    def set_search_path(self, dirs: list[str] | None = ...) -> None:
        """
        Sets the list of directories in which the :class:`GtkSource.SnippetManager` looks for
        snippet files.

        If ``dirs`` is ``None``, the search path is reset to default.

        At the moment this function can be called only before the
        snippet files are loaded for the first time. In practice
        to set a custom search path for a :class:`GtkSource.SnippetManager`,
        you have to call this function right after creating it.
        """
    @staticmethod
    def get_default() -> SnippetManager:
        """
        Returns the default :class:`GtkSource.SnippetManager` instance.
        """

class SnippetManagerClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class SpaceDrawer(GObject.Object):
    """
    Represent white space characters with symbols.

    :class:`GtkSource.SpaceDrawer` provides a way to visualize white spaces, by drawing
    symbols.

    Call :meth:`View.get_space_drawer` to get the :class:`GtkSource.SpaceDrawer`
    instance of a certain :class:`View`.

    By default, no white spaces are drawn because the
    :attr:`SpaceDrawer.enable-matrix` is ``False``.

    To draw white spaces, :meth:`SpaceDrawer.set_types_for_locations` can
    be called to set the :attr:`SpaceDrawer.matrix` property (by default all
    space types are enabled at all locations). Then call
    :meth:`SpaceDrawer.set_enable_matrix`.

    For a finer-grained method, there is also the :class:`Tag`'s
    :attr:`Tag.draw-spaces` property.

    # Example

    To draw non-breaking spaces everywhere and draw all types of trailing spaces
    except newlines:
    .. code-block:: c

        :meth:`GtkSource.SpaceDrawer.set_types_for_locations` (space_drawer,
                                                         GTK_SOURCE_SPACE_LOCATION_ALL,
                                                         GTK_SOURCE_SPACE_TYPE_NBSP);

        :meth:`GtkSource.SpaceDrawer.set_types_for_locations` (space_drawer,
                                                         GTK_SOURCE_SPACE_LOCATION_TRAILING,
                                                         GTK_SOURCE_SPACE_TYPE_ALL &
                                                         ~GTK_SOURCE_SPACE_TYPE_NEWLINE);

        :meth:`GtkSource.SpaceDrawer.set_enable_matrix` (space_drawer, TRUE);
    .. code-block:: python

        space_drawer.set_types_for_locations(
            locations=GtkSource.SpaceLocationFlags.ALL,
            types=GtkSource.SpaceTypeFlags.NBSP,
        )

        all_types_except_newline = GtkSource.SpaceTypeFlags(
            int(GtkSource.SpaceTypeFlags.ALL) & ~int(GtkSource.SpaceTypeFlags.NEWLINE)
        )
        space_drawer.set_types_for_locations(
            locations=GtkSource.SpaceLocationFlags.TRAILING,
            types=all_types_except_newline,
        )

        space_drawer.set_enable_matrix(True)

    # Use-case: draw unwanted white spaces

    A possible use-case is to draw only unwanted white spaces. Examples:

    - Draw all trailing spaces.
    - If the indentation and alignment must be done with spaces, draw tabs.

    And non-breaking spaces can always be drawn, everywhere, to distinguish them
    from normal spaces.
    """
    gimeta: ClassVar[_GIMeta]
    enable_matrix: bool
    matrix: GLib.Variant
    def __init__(self, *, enable_matrix: bool = ..., matrix: GLib.Variant = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def bind_matrix_setting(self, settings: Gio.Settings, key: str, flags: Gio.SettingsBindFlags | int) -> None:
        """
        Binds the :attr:`SpaceDrawer.matrix` property to a :class:`Gio.Settings` key.

        The :class:`Gio.Settings` key must be of the same type as the
        :attr:`SpaceDrawer.matrix` property, that is, ``"au"``.

        The :meth:`Gio.Settings.bind` function cannot be used, because the default GIO
        mapping functions don't support :class:`GLib.Variant` properties (maybe it will be
        supported by a future GIO version, in which case this function can be
        deprecated).
        """
    def get_enable_matrix(self) -> bool: ...
    def get_matrix(self) -> GLib.Variant:
        """
        Gets the value of the :attr:`SpaceDrawer.matrix` property, as a :class:`GLib.Variant`.

        An empty array can be returned in case the matrix is a zero matrix.

        The :meth:`SpaceDrawer.get_types_for_locations` function may be more
        convenient to use.
        """
    def get_types_for_locations(self, locations: SpaceLocationFlags | int) -> SpaceTypeFlags:
        """
        If only one location is specified, this function returns what kind of
        white spaces are drawn at that location.

        The value is retrieved from the :attr:`SpaceDrawer.matrix` property.

        If several locations are specified, this function returns the logical AND for
        those locations. Which means that if a certain kind of white space is present
        in the return value, then that kind of white space is drawn at all the
        specified ``locations``.
        """
    def set_enable_matrix(self, enable_matrix: bool) -> None:
        """
        Sets whether the :attr:`SpaceDrawer.matrix` property is enabled.
        """
    def set_matrix(self, matrix: GLib.Variant | None = ...) -> None:
        """
        Sets a new value to the :attr:`SpaceDrawer.matrix` property, as a :class:`GLib.Variant`.

        If ``matrix`` is ``None``, then an empty array is set.

        If ``matrix`` is floating, it is consumed.

        The :meth:`SpaceDrawer.set_types_for_locations` function may be more
        convenient to use.
        """
    def set_types_for_locations(self, locations: SpaceLocationFlags | int, types: SpaceTypeFlags | int) -> None:
        """
        Modifies the :attr:`SpaceDrawer.matrix` property at the specified
        ``locations``.
        """

class SpaceDrawerClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class Style(GObject.Object):
    """
    Represents a style.

    The :class:`GtkSource.Style` structure is used to describe text attributes
    which are set when given style is used.
    """
    gimeta: ClassVar[_GIMeta]
    background: str
    background_set: bool
    bold: bool
    bold_set: bool
    foreground: str
    foreground_set: bool
    italic: bool
    italic_set: bool
    line_background: str
    line_background_set: bool
    pango_underline: Pango.Underline | int
    scale: str
    scale_set: bool
    strikethrough: bool
    strikethrough_set: bool
    underline_color: str
    underline_color_set: bool
    underline_set: bool
    weight: Pango.Weight | int
    weight_set: bool
    def __init__(self, *, background: str = ..., background_set: bool = ..., bold: bool = ..., bold_set: bool = ..., foreground: str = ..., foreground_set: bool = ..., italic: bool = ..., italic_set: bool = ..., line_background: str = ..., line_background_set: bool = ..., pango_underline: Pango.Underline | int = ..., scale: str = ..., scale_set: bool = ..., strikethrough: bool = ..., strikethrough_set: bool = ..., underline_color: str = ..., underline_color_set: bool = ..., underline_set: bool = ..., weight: Pango.Weight | int = ..., weight_set: bool = ..., **kwargs: Any) -> None: ...
    def apply(self, tag: Gtk.TextTag) -> None:
        """
        This function modifies the :class:`Gtk.TextTag` properties that are related to the
        :class:`GtkSource.Style` properties. Other :class:`Gtk.TextTag` properties are left untouched.

        If ``style`` is non-``None``, applies ``style`` to ``tag``.

        If ``style`` is ``None``, the related *-set properties of :class:`Gtk.TextTag` are set to
        ``False``.
        """
    def copy(self) -> Style:
        """
        Creates a copy of ``style``, that is a new :class:`GtkSource.Style` instance which
        has the same attributes set.
        """

class StyleClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class StyleScheme(GObject.Object):
    """
    Controls the appearance of :class:`View`.

    :class:`GtkSource.StyleScheme` contains all the text styles to be used in
    :class:`View` and :class:`Buffer`. For instance, it contains text styles
    for syntax highlighting, it may contain foreground and background color for
    non-highlighted text, color for the line numbers, current line highlighting,
    bracket matching, etc.

    Style schemes are stored in XML files. The format of a scheme file is
    documented in the [style scheme reference](./style-reference.html).

    The two style schemes with IDs "classic" and "tango" follow more closely the
    GTK theme (for example for the background color).
    """
    gimeta: ClassVar[_GIMeta]
    description: str
    filename: str
    id: str
    name: str
    def __init__(self, *, id: str = ..., **kwargs: Any) -> None: ...
    def get_authors(self) -> list[str] | None: ...
    def get_description(self) -> str | None: ...
    def get_filename(self) -> str | None: ...
    def get_id(self) -> str: ...
    def get_metadata(self, name: str) -> str | None:
        """
        Gets a metadata property from the style scheme.
        """
    def get_name(self) -> str: ...
    def get_style(self, style_id: str) -> Style | None: ...

class StyleSchemeChooser:
    """
    Interface implemented by widgets for choosing style schemes.

    :class:`GtkSource.StyleSchemeChooser` is an interface that is implemented by widgets
    for choosing style schemes.

    In :class:`GtkSource.View`, the main widgets that implement this interface are
    :class:`StyleSchemeChooserWidget` and :class:`StyleSchemeChooserButton`.
    """
    gimeta: ClassVar[_GIMeta]
    style_scheme: StyleScheme
    def __init__(self, *, style_scheme: StyleScheme = ..., **kwargs: Any) -> None: ...
    def get_style_scheme(self) -> StyleScheme:
        """
        Gets the currently-selected scheme.
        """
    def set_style_scheme(self, scheme: StyleScheme) -> None:
        """
        Sets the scheme.
        """
    def do_get_style_scheme(self) -> StyleScheme:
        """
        Gets the currently-selected scheme.
        """
    def do_set_style_scheme(self, scheme: StyleScheme) -> None:
        """
        Sets the scheme.
        """

class StyleSchemeChooserButton(Gtk.Button, Gtk.Accessible, Gtk.Actionable, Gtk.Buildable, Gtk.ConstraintTarget, StyleSchemeChooser):
    """
    A button to launch a style scheme selection dialog.

    The :class:`GtkSource.StyleSchemeChooserButton` is a button which displays
    the currently selected style scheme and allows to open a style scheme
    selection dialog to change the style scheme.
    It is suitable widget for selecting a style scheme in a preference dialog.

    In :class:`GtkSource.StyleSchemeChooserButton`, a :class:`StyleSchemeChooserWidget`
    is used to provide a dialog for selecting style schemes.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...

class StyleSchemeChooserButtonClass:
    gimeta: ClassVar[_GIMeta]
    parent: Gtk.ButtonClass
    _reserved: list[int]

class StyleSchemeChooserInterface:
    gimeta: ClassVar[_GIMeta]
    base_interface: GObject.TypeInterface
    get_style_scheme: Any
    set_style_scheme: Any
    _reserved: list[int]

class StyleSchemeChooserWidget(Gtk.Widget, Gtk.Accessible, Gtk.Buildable, Gtk.ConstraintTarget, StyleSchemeChooser):
    """
    A widget for choosing style schemes.

    The :class:`GtkSource.StyleSchemeChooserWidget` widget lets the user select a
    style scheme. By default, the chooser presents a predefined list
    of style schemes.

    To change the initially selected style scheme,
    use :meth:`StyleSchemeChooser.set_style_scheme`.
    To get the selected style scheme
    use :meth:`StyleSchemeChooser.get_style_scheme`.
    """
    gimeta: ClassVar[_GIMeta]
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...

class StyleSchemeChooserWidgetClass:
    gimeta: ClassVar[_GIMeta]
    parent: Gtk.WidgetClass
    _reserved: list[int]

class StyleSchemeClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class StyleSchemeManager(GObject.Object):
    """
    Provides access to :class:`StyleScheme`s.
    """
    gimeta: ClassVar[_GIMeta]
    scheme_ids: list[str]
    search_path: list[str]
    def __init__(self, *, search_path: list[str] = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def append_search_path(self, path: str) -> None:
        """
        Appends ``path`` to the list of directories where the ``manager`` looks for
        style scheme files.

        See :meth:`StyleSchemeManager.set_search_path` for details.
        """
    def force_rescan(self) -> None:
        """
        Mark any currently cached information about the available style schems
        as invalid.

        All the available style schemes will be reloaded next time the ``manager`` is accessed.
        """
    def get_scheme(self, scheme_id: str) -> StyleScheme | None:
        """
        Looks up style scheme by id.
        """
    def get_scheme_ids(self) -> list[str] | None:
        """
        Returns the ids of the available style schemes.
        """
    def get_search_path(self) -> list[str]:
        """
        Returns the current search path for the ``manager``.

        See :meth:`StyleSchemeManager.set_search_path` for details.
        """
    def prepend_search_path(self, path: str) -> None:
        """
        Prepends ``path`` to the list of directories where the ``manager`` looks
        for style scheme files.

        See :meth:`StyleSchemeManager.set_search_path` for details.
        """
    def set_search_path(self, path: list[str] | None = ...) -> None:
        """
        Sets the list of directories where the ``manager`` looks for
        style scheme files.

        If ``path`` is ``None``, the search path is reset to default.

        Since :class:`GtkSource.View` 5.16 this function will allow you to provide
        paths in the form of "resource:///" URIs to embedded ``GResource``s.
        They must contain the path of a directory within the ``GResource``.
        """
    @staticmethod
    def get_default() -> StyleSchemeManager:
        """
        Returns the default :class:`GtkSource.StyleSchemeManager` instance.
        """

class StyleSchemeManagerClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

class StyleSchemePreview(Gtk.Widget, Gtk.Accessible, Gtk.Actionable, Gtk.Buildable, Gtk.ConstraintTarget):
    """
    A preview widget for :class:`StyleScheme`.

    This widget provides a convenient :class:`Gtk.Widget` to preview a :class:`StyleScheme`.

    The :attr:`StyleSchemePreview.selected` property can be used to manage
    the selection state of a single preview widget.
    """
    gimeta: ClassVar[_GIMeta]
    scheme: StyleScheme
    selected: bool
    def __init__(self, *, scheme: StyleScheme = ..., selected: bool = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_scheme(self) -> StyleScheme:
        """
        Gets the :class:`GtkSource.StyleScheme` previewed by the widget.
        """
    def get_selected(self) -> bool: ...
    def set_selected(self, selected: bool) -> None: ...
    activate: Signal[Self, [], None]
    def do_activate(self) -> None: ...

class StyleSchemePreviewClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.WidgetClass

class Tag(Gtk.TextTag):
    """
    A tag that can be applied to text in a :class:`Buffer`.

    :class:`GtkSource.Tag` is a subclass of :class:`Gtk.TextTag` that adds properties useful for
    the :class:`GtkSource.View` library.

    If, for a certain tag, :class:`Gtk.TextTag` is sufficient, it's better that you create
    a :class:`Gtk.TextTag`, not a :class:`Tag`.
    """
    gimeta: ClassVar[_GIMeta]
    draw_spaces: bool
    draw_spaces_set: bool
    def __init__(self, *, draw_spaces: bool = ..., draw_spaces_set: bool = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...

class TagClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.TextTagClass
    _reserved: list[int]

class View(Gtk.TextView, Gtk.Accessible, Gtk.AccessibleText, Gtk.Buildable, Gtk.ConstraintTarget, Gtk.Scrollable):
    """
    Subclass of :class:`Gtk.TextView`.

    :class:`GtkSource.View` is the main class of the :class:`GtkSource.View` library.
    Use a :class:`Buffer` to display text with a :class:`GtkSource.View`.

    This class provides:

     - Show the line numbers;
     - Show a right margin;
     - Highlight the current line;
     - Indentation settings;
     - Configuration for the Home and End keyboard keys;
     - Configure and show line marks;
     - And a few other things.

    An easy way to test all these features is to use the test-widget mini-program
    provided in the :class:`GtkSource.View` repository, in the tests/ directory.

    # :class:`GtkSource.View` as GtkBuildable

    The :class:`GtkSource.View` implementation of the :class:`Gtk.Buildable` interface exposes the
    :attr:`View.completion` object with the internal-child "completion".

    An example of a UI definition fragment with :class:`GtkSource.View`:
    .. code-block:: xml

        <object class=":class:`GtkSource.View`" id="source_view">
          <property name="tab-width">4</property>
          <property name="auto-indent">True</property>
          <child internal-child="completion">
            <object class=":class:`GtkSource.Completion`">
              <property name="select-on-show">False</property>
            </object>
          </child>
        </object>

    # Changing the Font

    Gtk CSS provides the best way to change the font for a :class:`GtkSource.View` in a
    manner that allows for components like :class:`Map` to scale the desired
    font.

    .. code-block:: c

        GtkCssProvider *provider = gtk_css_provider_new ();
        gtk_css_provider_load_from_string (provider,
                                          "textview { font-family: Monospace; font-size: 8pt; }");
        gtk_style_context_add_provider (gtk_widget_get_style_context (view),
                                        GTK_STYLE_PROVIDER (provider),
                                        GTK_STYLE_PROVIDER_PRIORITY_APPLICATION);
        g_object_unref (provider);
    .. code-block:: python

        provider = Gtk.CssProvider()
        provider.load_from_string("textview { font-family: Monospace; font-size: 8pt; }")
        style_context = view.get_style_context()
        style_context.add_provider(provider, Gtk.STYLE_PROVIDER_PRIORITY_APPLICATION)

    If you need to adjust the font or size of font within a portion of the
    document only, you should use a :class:`Gtk.TextTag` with the :attr:`Gtk.TextTag.family` or
    :attr:`Gtk.TextTag.scale` set so that the font size may be scaled relative to
    the default font set in CSS.
    """
    gimeta: ClassVar[_GIMeta]
    annotations: Annotations
    auto_indent: bool
    background_pattern: BackgroundPatternType | int
    completion: Completion
    enable_snippets: bool
    highlight_current_line: bool
    indent_on_tab: bool
    indent_width: int
    indenter: Indenter
    insert_spaces_instead_of_tabs: bool
    right_margin_position: int
    show_line_marks: bool
    show_line_numbers: bool
    show_right_margin: bool
    smart_backspace: bool
    smart_home_end: SmartHomeEndType | int
    space_drawer: SpaceDrawer
    tab_width: int
    def __init__(self, *, auto_indent: bool = ..., background_pattern: BackgroundPatternType | int = ..., enable_snippets: bool = ..., highlight_current_line: bool = ..., indent_on_tab: bool = ..., indent_width: int = ..., indenter: Indenter = ..., insert_spaces_instead_of_tabs: bool = ..., right_margin_position: int = ..., show_line_marks: bool = ..., show_line_numbers: bool = ..., show_right_margin: bool = ..., smart_backspace: bool = ..., smart_home_end: SmartHomeEndType | int = ..., tab_width: int = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    @classmethod
    def new_with_buffer(cls, buffer: Buffer) -> Self:
        """
        Creates a new :class:`GtkSource.View` widget displaying the buffer ``buffer``.

        One buffer can be shared among many widgets.
        """
    def get_annotations(self) -> Annotations:
        """
        Gets the :class:`Annotations` associated with ``view``.

        The returned object is guaranteed to be the same for the lifetime of ``view``.
        Each :class:`View` object has a different :class:`Annotations`.
        """
    def get_auto_indent(self) -> bool:
        """
        Returns whether auto-indentation of text is enabled.
        """
    def get_background_pattern(self) -> BackgroundPatternType:
        """
        Returns the :class:`GtkSource.BackgroundPatternType` specifying if and how
        the background pattern should be displayed for this ``view``.
        """
    def get_completion(self) -> Completion:
        """
        Gets the :class:`Completion` associated with ``view``.

        The returned object is guaranteed to be the same for the lifetime of ``view``.
        Each :class:`GtkSource.View` object has a different :class:`Completion`.
        """
    def get_enable_snippets(self) -> bool:
        """
        Gets the :attr:`View.enable-snippets` property.

        If ``True``, matching snippets found in the :class:`SnippetManager`
        may be expanded when the user presses Tab after a word in the :class:`View`.
        """
    def get_gutter(self, window_type: Gtk.TextWindowType | int) -> Gutter:
        """
        Returns the :class:`Gutter` object associated with ``window_type`` for ``view``.

        Only ``GTK_TEXT_WINDOW_LEFT`` and ``GTK_TEXT_WINDOW_RIGHT`` are supported,
        respectively corresponding to the left and right gutter. The line numbers
        and mark category icons are rendered in the left gutter.
        """
    def get_highlight_current_line(self) -> bool:
        """
        Returns whether the current line is highlighted.
        """
    def get_hover(self) -> Hover:
        """
        Gets the :class:`Hover` associated with ``view``.

        The returned object is guaranteed to be the same for the lifetime of ``view``.
        Each :class:`View` object has a different :class:`Hover`.
        """
    def get_indent_on_tab(self) -> bool:
        """
        Returns whether when the tab key is pressed the current selection
        should get indented instead of replaced with the ``\\t`` character.
        """
    def get_indent_width(self) -> int:
        """
        Returns the number of spaces to use for each step of indent.

        See :meth:`View.set_indent_width` for details.
        """
    def get_indenter(self) -> Indenter | None:
        """
        Gets the :attr:`View.indenter` property.
        """
    def get_insert_spaces_instead_of_tabs(self) -> bool:
        """
        Returns whether when inserting a tabulator character it should
        be replaced by a group of space characters.
        """
    def get_mark_attributes(self, category: str, priority: int) -> MarkAttributes:
        """
        Gets attributes and priority for the ``category``.
        """
    def get_right_margin_position(self) -> int:
        """
        Gets the position of the right margin in the given ``view``.
        """
    def get_show_line_marks(self) -> bool:
        """
        Returns whether line marks are displayed beside the text.
        """
    def get_show_line_numbers(self) -> bool:
        """
        Returns whether line numbers are displayed beside the text.
        """
    def get_show_right_margin(self) -> bool:
        """
        Returns whether a right margin is displayed.
        """
    def get_smart_backspace(self) -> bool:
        """
        Returns ``True`` if pressing the Backspace key will try to delete spaces
        up to the previous tab stop.
        """
    def get_smart_home_end(self) -> SmartHomeEndType:
        """
        Returns a :class:`SmartHomeEndType` end value specifying
        how the cursor will move when HOME and END keys are pressed.
        """
    def get_space_drawer(self) -> SpaceDrawer:
        """
        Gets the :class:`SpaceDrawer` associated with ``view``.

        The returned object is guaranteed to be the same for the lifetime of ``view``.
        Each :class:`View` object has a different :class:`SpaceDrawer`.
        """
    def get_tab_width(self) -> int:
        """
        Returns the width of tabulation in characters.
        """
    def get_visual_column(self, iter: Gtk.TextIter) -> int:
        """
        Determines the visual column at ``iter`` taking into consideration the
        :attr:`View.tab-width` of ``view``.
        """
    def indent_lines(self, start: Gtk.TextIter, end: Gtk.TextIter) -> None:
        """
        Inserts one indentation level at the beginning of the specified lines. The
        empty lines are not indented.
        """
    def set_auto_indent(self, enable: bool) -> None:
        """
        If ``True`` auto-indentation of text is enabled.

        When Enter is pressed to create a new line, the auto-indentation inserts the
        same indentation as the previous line. This is **not** a
        "smart indentation" where an indentation level is added or removed depending
        on the context.
        """
    def set_background_pattern(self, background_pattern: BackgroundPatternType | int) -> None:
        """
        Set if and how the background pattern should be displayed.
        """
    def set_enable_snippets(self, enable_snippets: bool) -> None:
        """
        Sets the :attr:`View.enable-snippets` property.

        If ``enable_snippets`` is ``True``, matching snippets found in the
        :class:`SnippetManager` may be expanded when the user presses
        Tab after a word in the :class:`View`.
        """
    def set_highlight_current_line(self, highlight: bool) -> None:
        """
        If ``highlight`` is ``True`` the current line will be highlighted.
        """
    def set_indent_on_tab(self, enable: bool) -> None:
        """
        If ``True``, when the tab key is pressed when several lines are selected, the
        selected lines are indented of one level instead of being replaced with a ``\\t``
        character. Shift+Tab unindents the selection.

        If the first or last line is not selected completely, it is also indented or
        unindented.

        When the selection doesn't span several lines, the tab key always replaces
        the selection with a normal ``\\t`` character.
        """
    def set_indent_width(self, width: int) -> None:
        """
        Sets the number of spaces to use for each step of indent when the tab key is
        pressed.

        If ``width`` is -1, the value of the :attr:`View.tab-width` property
        will be used.

        The :attr:`View.indent-width` interacts with the
        :attr:`View.insert-spaces-instead-of-tabs` property and
        :attr:`View.tab-width`. An example will be clearer:

        If the :attr:`View.indent-width` is 4 and :attr:`View.tab-width` is 8 and
        :attr:`View.insert-spaces-instead-of-tabs` is ``False``, then pressing the tab
        key at the beginning of a line will insert 4 spaces. So far so good. Pressing
        the tab key a second time will remove the 4 spaces and insert a ``\\t`` character
        instead (since :attr:`View.tab-width` is 8). On the other hand, if
        :attr:`View.insert-spaces-instead-of-tabs` is ``True``, the second tab key
        pressed will insert 4 more spaces for a total of 8 spaces in the
        :class:`Gtk.TextBuffer`.

        The test-widget program (available in the :class:`GtkSource.View` repository) may be
        useful to better understand the indentation settings (enable the space
        drawing!).
        """
    def set_indenter(self, indenter: Indenter | None = ...) -> None:
        """
        Sets the indenter for ``view`` to ``indenter``.

        Note that the indenter will not be used unless :class:`GtkSource.View`:auto-indent
        has been set to ``True``.
        """
    def set_insert_spaces_instead_of_tabs(self, enable: bool) -> None:
        """
        If ``True`` a tab key pressed is replaced by a group of space characters.

        Of course it is still possible to insert a real ``\\t`` programmatically with the
        :class:`Gtk.TextBuffer` API.
        """
    def set_mark_attributes(self, category: str, attributes: MarkAttributes, priority: int) -> None:
        """
        Sets attributes and priority for the ``category``.
        """
    def set_right_margin_position(self, pos: int) -> None:
        """
        Sets the position of the right margin in the given ``view``.
        """
    def set_show_line_marks(self, show: bool) -> None:
        """
        If ``True`` line marks will be displayed beside the text.
        """
    def set_show_line_numbers(self, show: bool) -> None:
        """
        If ``True`` line numbers will be displayed beside the text.
        """
    def set_show_right_margin(self, show: bool) -> None:
        """
        If ``True`` a right margin is displayed.
        """
    def set_smart_backspace(self, smart_backspace: bool) -> None:
        """
        When set to ``True``, pressing the Backspace key will try to delete spaces
        up to the previous tab stop.
        """
    def set_smart_home_end(self, smart_home_end: SmartHomeEndType | int) -> None:
        """
        Set the desired movement of the cursor when HOME and END keys
        are pressed.
        """
    def set_tab_width(self, width: int) -> None:
        """
        Sets the width of tabulation in characters.

        The :class:`GtkTextBuffer` still contains ``\\t`` characters,
        but they can take a different visual width in a :class:`View` widget.
        """
    def unindent_lines(self, start: Gtk.TextIter, end: Gtk.TextIter) -> None:
        """
        Removes one indentation level at the beginning of the
        specified lines.
        """
    def do_line_mark_activated(self, iter: Gtk.TextIter, button: int, state: Gdk.ModifierType | int, n_presses: int) -> None: ...
    def do_move_lines(self, down: bool) -> None: ...
    def do_move_words(self, step: int) -> None: ...
    def do_push_snippet(self, snippet: Snippet, location: Gtk.TextIter | None = ...) -> None:
        """
        Inserts a new snippet at ``location``

        If another snippet was already active, it will be paused and the new
        snippet will become active. Once the focus positions of ``snippet`` have
        been exhausted, editing will return to the previous snippet.
        """
    def do_show_completion(self) -> None: ...
    change_case: Signal[Self, [ChangeCaseType], None]
    change_number: Signal[Self, [int], None]
    join_lines: Signal[Self, [], None]
    line_mark_activated: Signal[Self, [Gtk.TextIter, int, Gdk.ModifierType, int], None]
    move_lines: Signal[Self, [bool], None]
    move_to_matching_bracket: Signal[Self, [bool], None]
    move_words: Signal[Self, [int], None]
    push_snippet: SignalMethod[Self, [Snippet, Gtk.TextIter], None]
    show_completion: Signal[Self, [], None]
    def do_change_case(self, case_type: ChangeCaseType) -> None: ...
    def do_change_number(self, count: int) -> None: ...
    def do_join_lines(self) -> None: ...
    def do_move_to_matching_bracket(self, extend_selection: bool) -> None: ...
    def do_smart_home_end(self, iter: Gtk.TextIter, count: int) -> None: ...

class ViewClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.TextViewClass
    line_mark_activated: Any
    show_completion: Any
    move_lines: Any
    move_words: Any
    push_snippet: Any
    _reserved: list[int]

class VimIMContext(Gtk.IMContext):
    """
    Vim emulation.

    The :class:`GtkSource.VimIMContext` is a :class:`Gtk.IMContext` implementation that can
    be used to provide Vim-like editing controls within a :class:`View`.

    The ``GtkSourceViMIMContext`` will process incoming :class:`Gdk.KeyEvent` as the
    user types. It should be used in conjunction with a :class:`Gtk.EventControllerKey`.

    Various features supported by :class:`GtkSource.VimIMContext` include:

     - Normal, Insert, Replace, Visual, and Visual Line modes
     - Support for an integrated command bar and current command preview
     - Search and replace
     - Motions and Text Objects
     - History replay
     - Jumplists within the current file
     - Registers including the system and primary clipboards
     - Creation and motion to marks
     - Some commonly used Vim commands

    It is recommended that applications display the contents of
    :attr:`VimIMContext.command-bar-text` and
    :attr:`VimIMContext.command-text` to the user as they represent the
    command-bar and current command preview found in Vim.

    :class:`GtkSource.VimIMContext` attempts to work with additional :class:`Gtk.IMContext`
    implementations such as IBus by querying the :class:`Gtk.TextView` before processing
    the command in states which support it (notably Insert and Replace modes).

    .. code-block:: c

        GtkEventController *key;
        GtkIMContext *im_context;
        GtkWidget *view;

        view = :meth:`GtkSource.View.new`;
        im_context = :meth:`GtkSource.VimIMContext.new`;
        key = gtk_event_controller_key_new ();

        gtk_event_controller_key_set_im_context (GTK_EVENT_CONTROLLER_KEY (key), im_context);
        gtk_event_controller_set_propagation_phase (key, GTK_PHASE_CAPTURE);
        gtk_widget_add_controller (view, key);
        gtk_im_context_set_client_widget (im_context, view);

        g_object_bind_property (im_context, "command-bar-text", command_bar_label, "label", 0);
        g_object_bind_property (im_context, "command-text", command_label, "label", 0);
    .. code-block:: python

        key = Gtk.EventControllerKey.new()
        im_context = GtkSource.VimIMContext.new()
        buffer = GtkSource.Buffer()
        view = GtkSource.View.new_with_buffer(buffer)

        key.set_im_context(im_context)
        key.set_propagation_phase(Gtk.PropagationPhase.CAPTURE)
        view.add_controller(key)
        im_context.set_client_widget(view)

        im_context.bind_property(
            source_property="command-text",
            target=command_label,
            target_property="label",
            flags=GObject.BindingFlags.DEFAULT,
        )

        im_context.bind_property(
            source_property="command-bar-text",
            target=command_bar_label,
            target_property="label",
            flags=GObject.BindingFlags.DEFAULT,
        )
    """
    gimeta: ClassVar[_GIMeta]
    command_bar_text: str
    command_text: str
    def __init__(self, **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def get_command_bar_text(self) -> str:
        """
        Gets the current command-bar text as it is entered by the user.
        """
    def get_command_text(self) -> str:
        """
        Gets the current command text as it is entered by the user.
        """
    edit: Signal[Self, [View, str | None], None]
    execute_command: SignalMethod[Self, [str], bool]
    format_text: Signal[Self, [Gtk.TextIter, Gtk.TextIter], None]
    write: Signal[Self, [View, str | None], None]
    def do_edit(self, view: View, path: str | None) -> None: ...
    def do_execute_command(self, command: str) -> bool: ...
    def do_format_text(self, begin: Gtk.TextIter, end: Gtk.TextIter) -> None: ...
    def do_write(self, view: View, path: str | None) -> None: ...

class VimIMContextClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: Gtk.IMContextClass

MAJOR_VERSION: int
"""
Like :func:`GtkSource.get_major_version`, but from the headers used at
application compile time, rather than from the library linked
against at application run time.
"""
MICRO_VERSION: int
"""
Like :func:`GtkSource.get_micro_version`, but from the headers used at
application compile time, rather than from the library linked
against at application run time.
"""
MINOR_VERSION: int
"""
Like :func:`GtkSource.get_minor_version`, but from the headers used at
application compile time, rather than from the library linked
against at application run time.
"""
def check_version(major: int, minor: int, micro: int) -> bool:
    """
    Like GTK_SOURCE_CHECK_VERSION, but the check for :func:`GtkSource.check_version` is
    at runtime instead of compile time. This is useful for compiling
    against older versions of :class:`GtkSource.View`, but using features from newer
    versions.
    """
def encoding_get_all() -> list[Encoding]:
    """
    Gets all encodings.
    """
def encoding_get_current() -> Encoding:
    """
    Gets the :class:`GtkSource.Encoding` for the current locale.

    See also :func:`GLib.get_charset`.
    """
def encoding_get_default_candidates() -> list[Encoding]:
    """
    Gets the list of default candidate encodings to try when loading a file.

    See :meth:`FileLoader.set_candidate_encodings`.

    This function returns a different list depending on the current locale (i.e.
    language, country and default encoding). The UTF-8 encoding and the current
    locale encoding are guaranteed to be present in the returned list.
    """
def encoding_get_from_charset(charset: str) -> Encoding | None:
    """
    Gets a :class:`GtkSource.Encoding` from a character set such as "UTF-8" or
    "ISO-8859-1".
    """
def encoding_get_utf8() -> Encoding: ...
def file_loader_error_quark() -> GLib.Quark: ...
def file_saver_error_quark() -> GLib.Quark: ...
def finalize() -> None:
    """
    Free the resources allocated by :class:`GtkSource.View`. For example it unrefs the
    singleton objects.

    It is not mandatory to call this function, it's just to be friendlier to
    memory debugging tools. This function is meant to be called at the end of
    main(). It can be called several times.
    """
def get_major_version() -> int:
    """
    Returns the major version number of the :class:`GtkSource.View` library.
    (e.g. in :class:`GtkSource.View` version 3.20.0 this is 3.)

    This function is in the library, so it represents the :class:`GtkSource.View` library
    your code is running against. Contrast with the :data:`GtkSource.MAJOR_VERSION`
    macro, which represents the major version of the :class:`GtkSource.View` headers you
    have included when compiling your code.
    """
def get_micro_version() -> int:
    """
    Returns the micro version number of the :class:`GtkSource.View` library.
    (e.g. in :class:`GtkSource.View` version 3.20.0 this is 0.)

    This function is in the library, so it represents the :class:`GtkSource.View` library
    your code is running against. Contrast with the :data:`GtkSource.MICRO_VERSION`
    macro, which represents the micro version of the :class:`GtkSource.View` headers you
    have included when compiling your code.
    """
def get_minor_version() -> int:
    """
    Returns the minor version number of the :class:`GtkSource.View` library.
    (e.g. in :class:`GtkSource.View` version 3.20.0 this is 20.)

    This function is in the library, so it represents the :class:`GtkSource.View` library
    your code is running against. Contrast with the :data:`GtkSource.MINOR_VERSION`
    macro, which represents the minor version of the :class:`GtkSource.View` headers you
    have included when compiling your code.
    """
def init() -> None:
    """
    Initializes the :class:`GtkSource.View` library (e.g. for the internationalization).

    This function can be called several times, but is meant to be called at the
    beginning of main(), before any other :class:`GtkSource.View` function call.

    The counterpart to this function is :func:`finalize` which can be convenient
    when using memory debugging tools.
    """
def scheduler_add(callback: SchedulerCallback) -> int:
    """
    Adds a new callback that will be executed as time permits on the main thread.

    This is useful when you need to do a lot of background work but want to do
    it incrementally.

    ``callback`` will be provided a deadline that it should complete it's work by
    (or near) and can be checked using :func:`GLib.get_monotonic_time` for comparison.

    Use :func:`scheduler_remove` to remove the handler.
    """
def scheduler_remove(handler_id: int) -> None:
    """
    Removes a scheduler callback previously registered with
    :func:`scheduler_add` or :func:`scheduler_add_full`.
    """
def utils_escape_search_text(text: str) -> str:
    """
    Use this function to escape the following characters: ``\\n``, ``\\r``, ``\\t`` and ``\\``.

    For a regular expression search, use g_regex_escape_string() instead.

    One possible use case is to take the :class:`GtkTextBuffer`'s selection and put it in a
    search entry. The selection can contain tabulations, newlines, etc. So it's
    better to escape those special characters to better fit in the search entry.

    See also: :func:`utils_unescape_search_text`.

    <warning>
    Warning: the escape and unescape functions are not reciprocal! For example,
    escape (unescape (\\)) = \\\\. So avoid cycles such as: search entry -> unescape
    -> search settings -> escape -> search entry. The original search entry text
    may be modified.
    </warning>
    """
def utils_unescape_search_text(text: str) -> str:
    """
    Use this function before :meth:`SearchSettings.set_search_text`, to
    unescape the following sequences of characters: ``\\n``, ``\\r``, ``\\t`` and ``\\\\``.
    The purpose is to easily write those characters in a search entry.

    Note that unescaping the search text is not needed for regular expression
    searches.

    See also: :func:`utils_escape_search_text`.
    """
