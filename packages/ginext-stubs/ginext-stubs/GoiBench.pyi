# Auto-generated by ginext-stubgen from GoiBench-1.0.gir.
# Do not edit by hand.
# mypy: disable-error-code="override,type-arg,misc,valid-type"
from __future__ import annotations

from collections.abc import Iterator
from contextlib import AbstractContextManager
from typing import Any, Awaitable, Callable, ClassVar, Concatenate, Generic, Final, Literal, NamedTuple, ParamSpec as _ParamSpec, Self, TypeVar, override, overload
from ginext import GObject
from ginext.overlay.registrar import OverlayRegistrar as _OverlayRegistrar
from ginext.private import GIMeta as _GIMeta

overlay: _OverlayRegistrar
__version__: tuple[int, ...]
def __getattr__(name: str) -> Any: ...

_SigO = TypeVar('_SigO')
_SigP = _ParamSpec('_SigP')
_SigR = TypeVar('_SigR')

class SignalConnection:
    handler_id: int
    signal_name: str
    source: Any
    callback: Callable[..., Any]
    after: bool
    once: bool
    owner: Any
    is_connected: bool
    def disconnect(self) -> None: ...
    def blocked(self) -> AbstractContextManager[SignalConnection]: ...

class Signal(Generic[_SigO, _SigP, _SigR]):
    def __call__(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...
    def connect(self, handler: Callable[Concatenate[_SigO, _SigP], _SigR], *, after: bool = ..., once: bool = ..., owner: Any = ...) -> SignalConnection: ...
    def connect_after(self, handler: Callable[Concatenate[_SigO, _SigP], _SigR]) -> SignalConnection: ...
    def emit(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...
    def disconnect(self, connection: SignalConnection) -> None: ...
class SignalMethod(Signal[_SigO, _SigP, _SigR]):
    def __call__(self, *args: _SigP.args, **kwargs: _SigP.kwargs) -> _SigR: ...

class DetailedSignal(Signal[_SigO, _SigP, _SigR]):
    def __call__(self, detail: str | GObject.Property) -> 'Signal[_SigO, _SigP, _SigR]': ...

CallbackInt = Callable[[int], int]
CallbackMixed = Callable[[int, int, float, bool, str], int]
CallbackNoArgs = Callable[[], None]
CallbackOutInt = Callable[[int], None]
CallbackWithUserData = Callable[[int], int]
class Object(GObject.Object):
    gimeta: ClassVar[_GIMeta]
    value: int
    def __init__(self, *, value: int = ..., **kwargs: Any) -> None: ...
    @classmethod
    def new(cls, *args: Any, **kwargs: Any) -> Self: ...
    def children(self) -> list[Object]:
        """
        (self) → GList<:class:`GoiBench.Object`>. Mirrors ``Gtk.Notebook.get_children``.
        Not yet covered by the narrow JIT — drives the GList specialization
        phase.
        """
    def get_index(self) -> int:
        """
        (self) → int. Mirrors ``Gtk.Notebook.get_current_page`` /
        ``GdkPixbuf.Pixbuf.get_width``.
        """
    def get_label(self) -> str | None:
        """
        (self) → utf8. Borrowed return.
        """
    def index_of(self, child: Object) -> int:
        """
        (self, GObject) → int. Mirrors ``Gtk.Notebook.page_num``.
        """
    def lookup(self, name: str) -> Object:
        """
        (self, utf8) → GObject. Mirrors ``Gtk.Window.lookup_action``. Always
        returns ``self`` (transfer none) so we measure dispatch, not lookup cost.
        """
    def nth(self, i: int) -> Object:
        """
        (self, int) → GObject. Mirrors ``Gtk.Notebook.get_nth_page``.
        """
    def set_flag(self, v: bool) -> None:
        """
        (self, bool) → void. Mirrors ``Gio.SimpleAction.set_enabled``.
        """
    def set_label(self, s: str | None = ...) -> None:
        """
        (self, utf8?) → void. Mirrors ``Gtk.Label.set_label`` /
        ``Gtk.Widget.set_tooltip_text`` (nullable string).
        """
    tick: SignalMethod[Self, [], None]
    def do_tick(self) -> None: ...

class ObjectClass:
    gimeta: ClassVar[_GIMeta]
    parent_class: GObject.ObjectClass

def callback_int_loop(callback: CallbackInt, n: int) -> int:
    """
    Calls ``callback`` with an int argument ``n`` times and accumulates int returns.
    """
def callback_mixed_loop(callback: CallbackMixed, n: int) -> int:
    """
    Calls ``callback`` with mixed scalar and pointer lanes ``n`` times.
    """
def callback_no_args_loop(callback: CallbackNoArgs, n: int) -> int:
    """
    Calls ``callback`` ``n`` times from C and returns the number of invocations.
    """
def callback_out_int_loop(callback: CallbackOutInt, n: int) -> int:
    """
    Calls ``callback`` with an int argument and one int OUT parameter ``n`` times.
    """
def callback_user_data_loop(callback: CallbackWithUserData, n: int) -> int:
    """
    Calls ``callback`` with explicit user-data closure ``n`` times.
    """
def g_object_get_int_loop(obj: GObject.Object, name: str, n: int) -> int:
    """
    Repeatedly reads int property ``name`` with ``g_object_get()`` and accumulates
    the results. Used to benchmark and validate Python-defined property access
    through the native GObject property machinery.
    """
def in_1_int(a: int) -> int:
    """
    1 IN int → int. Single-arg-marshal baseline.
    """
def in_2_int(a: int, b: int) -> int: ...
def in_3_int(a: int, b: int, c: int) -> int: ...
def in_4_int(a: int, b: int, c: int, d: int) -> int: ...
def in_5_int(a: int, b: int, c: int, d: int, e: int) -> int:
    """
    5 IN int args (last one spills off SysV's 6-int-register window once
    self/this fills rdi). Body: ``return a;``
    """
def in_5_mixed(a: int, b: int, c: float, d: int, e: int) -> int:
    """
    Mixed scalar shapes (i32/i64/double/i32/u32). Tests the FFI register
    lane mux (integer vs xmm).
    """
def in_6_int(a: int, b: int, c: int, d: int, e: int, f: int) -> int:
    """
    6 IN int args (fills the SysV register window).
    """
def noop_int() -> int:
    """
    0 args, int return. Baseline + return-marshal cost.
    """
def noop_void() -> None:
    """
    0 args, void return. Baseline call overhead.
    """
