# Auto-generated by ginext-stubgen from HarfBuzz-0.0.gir.
# Do not edit by hand.
# mypy: disable-error-code="override,type-arg,misc,valid-type"
from __future__ import annotations

from collections.abc import Iterator
from typing_extensions import deprecated
from typing import Any, Awaitable, Callable, ClassVar, Final, Generic, Literal, NamedTuple, Self, TypeVar, override, overload
from enum import IntEnum, IntFlag
from ginext import GLib
from ginext import freetype2
from ginext.overlay.registrar import OverlayRegistrar as _OverlayRegistrar
from ginext.private import GIMeta as _GIMeta

overlay: _OverlayRegistrar
__version__: tuple[int, ...]
def __getattr__(name: str) -> Any: ...

bool_t = int
codepoint_t = int
color_t = int
font_get_font_h_extents_func_t = font_get_font_extents_func_t
font_get_font_v_extents_func_t = font_get_font_extents_func_t
font_get_glyph_h_advance_func_t = font_get_glyph_advance_func_t
font_get_glyph_h_advances_func_t = font_get_glyph_advances_func_t
font_get_glyph_h_kerning_func_t = font_get_glyph_kerning_func_t
font_get_glyph_h_origin_func_t = font_get_glyph_origin_func_t
font_get_glyph_h_origins_func_t = font_get_glyph_origins_func_t
font_get_glyph_v_advance_func_t = font_get_glyph_advance_func_t
font_get_glyph_v_advances_func_t = font_get_glyph_advances_func_t
font_get_glyph_v_kerning_func_t = font_get_glyph_kerning_func_t
font_get_glyph_v_origin_func_t = font_get_glyph_origin_func_t
font_get_glyph_v_origins_func_t = font_get_glyph_origins_func_t
mask_t = int
ot_name_id_t = int
position_t = int
tag_t = int
class aat_layout_feature_selector_t(IntEnum):
    INVALID = 65535
    ALL_TYPE_FEATURES_ON = 0
    ALL_TYPE_FEATURES_OFF = 1
    REQUIRED_LIGATURES_ON = 0
    REQUIRED_LIGATURES_OFF = 1
    COMMON_LIGATURES_ON = 2
    COMMON_LIGATURES_OFF = 3
    RARE_LIGATURES_ON = 4
    RARE_LIGATURES_OFF = 5
    LOGOS_ON = 6
    LOGOS_OFF = 7
    REBUS_PICTURES_ON = 8
    REBUS_PICTURES_OFF = 9
    DIPHTHONG_LIGATURES_ON = 10
    DIPHTHONG_LIGATURES_OFF = 11
    SQUARED_LIGATURES_ON = 12
    SQUARED_LIGATURES_OFF = 13
    ABBREV_SQUARED_LIGATURES_ON = 14
    ABBREV_SQUARED_LIGATURES_OFF = 15
    SYMBOL_LIGATURES_ON = 16
    SYMBOL_LIGATURES_OFF = 17
    CONTEXTUAL_LIGATURES_ON = 18
    CONTEXTUAL_LIGATURES_OFF = 19
    HISTORICAL_LIGATURES_ON = 20
    HISTORICAL_LIGATURES_OFF = 21
    UNCONNECTED = 0
    PARTIALLY_CONNECTED = 1
    CURSIVE = 2
    UPPER_AND_LOWER_CASE = 0
    ALL_CAPS = 1
    ALL_LOWER_CASE = 2
    SMALL_CAPS = 3
    INITIAL_CAPS = 4
    INITIAL_CAPS_AND_SMALL_CAPS = 5
    SUBSTITUTE_VERTICAL_FORMS_ON = 0
    SUBSTITUTE_VERTICAL_FORMS_OFF = 1
    LINGUISTIC_REARRANGEMENT_ON = 0
    LINGUISTIC_REARRANGEMENT_OFF = 1
    MONOSPACED_NUMBERS = 0
    PROPORTIONAL_NUMBERS = 1
    THIRD_WIDTH_NUMBERS = 2
    QUARTER_WIDTH_NUMBERS = 3
    WORD_INITIAL_SWASHES_ON = 0
    WORD_INITIAL_SWASHES_OFF = 1
    WORD_FINAL_SWASHES_ON = 2
    WORD_FINAL_SWASHES_OFF = 3
    LINE_INITIAL_SWASHES_ON = 4
    LINE_INITIAL_SWASHES_OFF = 5
    LINE_FINAL_SWASHES_ON = 6
    LINE_FINAL_SWASHES_OFF = 7
    NON_FINAL_SWASHES_ON = 8
    NON_FINAL_SWASHES_OFF = 9
    SHOW_DIACRITICS = 0
    HIDE_DIACRITICS = 1
    DECOMPOSE_DIACRITICS = 2
    NORMAL_POSITION = 0
    SUPERIORS = 1
    INFERIORS = 2
    ORDINALS = 3
    SCIENTIFIC_INFERIORS = 4
    NO_FRACTIONS = 0
    VERTICAL_FRACTIONS = 1
    DIAGONAL_FRACTIONS = 2
    PREVENT_OVERLAP_ON = 0
    PREVENT_OVERLAP_OFF = 1
    HYPHENS_TO_EM_DASH_ON = 0
    HYPHENS_TO_EM_DASH_OFF = 1
    HYPHEN_TO_EN_DASH_ON = 2
    HYPHEN_TO_EN_DASH_OFF = 3
    SLASHED_ZERO_ON = 4
    SLASHED_ZERO_OFF = 5
    FORM_INTERROBANG_ON = 6
    FORM_INTERROBANG_OFF = 7
    SMART_QUOTES_ON = 8
    SMART_QUOTES_OFF = 9
    PERIODS_TO_ELLIPSIS_ON = 10
    PERIODS_TO_ELLIPSIS_OFF = 11
    HYPHEN_TO_MINUS_ON = 0
    HYPHEN_TO_MINUS_OFF = 1
    ASTERISK_TO_MULTIPLY_ON = 2
    ASTERISK_TO_MULTIPLY_OFF = 3
    SLASH_TO_DIVIDE_ON = 4
    SLASH_TO_DIVIDE_OFF = 5
    INEQUALITY_LIGATURES_ON = 6
    INEQUALITY_LIGATURES_OFF = 7
    EXPONENTS_ON = 8
    EXPONENTS_OFF = 9
    MATHEMATICAL_GREEK_ON = 10
    MATHEMATICAL_GREEK_OFF = 11
    NO_ORNAMENTS = 0
    DINGBATS = 1
    PI_CHARACTERS = 2
    FLEURONS = 3
    DECORATIVE_BORDERS = 4
    INTERNATIONAL_SYMBOLS = 5
    MATH_SYMBOLS = 6
    NO_ALTERNATES = 0
    DESIGN_LEVEL1 = 0
    DESIGN_LEVEL2 = 1
    DESIGN_LEVEL3 = 2
    DESIGN_LEVEL4 = 3
    DESIGN_LEVEL5 = 4
    NO_STYLE_OPTIONS = 0
    DISPLAY_TEXT = 1
    ENGRAVED_TEXT = 2
    ILLUMINATED_CAPS = 3
    TITLING_CAPS = 4
    TALL_CAPS = 5
    TRADITIONAL_CHARACTERS = 0
    SIMPLIFIED_CHARACTERS = 1
    JIS1978_CHARACTERS = 2
    JIS1983_CHARACTERS = 3
    JIS1990_CHARACTERS = 4
    TRADITIONAL_ALT_ONE = 5
    TRADITIONAL_ALT_TWO = 6
    TRADITIONAL_ALT_THREE = 7
    TRADITIONAL_ALT_FOUR = 8
    TRADITIONAL_ALT_FIVE = 9
    EXPERT_CHARACTERS = 10
    JIS2004_CHARACTERS = 11
    HOJO_CHARACTERS = 12
    NLCCHARACTERS = 13
    TRADITIONAL_NAMES_CHARACTERS = 14
    LOWER_CASE_NUMBERS = 0
    UPPER_CASE_NUMBERS = 1
    PROPORTIONAL_TEXT = 0
    MONOSPACED_TEXT = 1
    HALF_WIDTH_TEXT = 2
    THIRD_WIDTH_TEXT = 3
    QUARTER_WIDTH_TEXT = 4
    ALT_PROPORTIONAL_TEXT = 5
    ALT_HALF_WIDTH_TEXT = 6
    NO_TRANSLITERATION = 0
    HANJA_TO_HANGUL = 1
    HIRAGANA_TO_KATAKANA = 2
    KATAKANA_TO_HIRAGANA = 3
    KANA_TO_ROMANIZATION = 4
    ROMANIZATION_TO_HIRAGANA = 5
    ROMANIZATION_TO_KATAKANA = 6
    HANJA_TO_HANGUL_ALT_ONE = 7
    HANJA_TO_HANGUL_ALT_TWO = 8
    HANJA_TO_HANGUL_ALT_THREE = 9
    NO_ANNOTATION = 0
    BOX_ANNOTATION = 1
    ROUNDED_BOX_ANNOTATION = 2
    CIRCLE_ANNOTATION = 3
    INVERTED_CIRCLE_ANNOTATION = 4
    PARENTHESIS_ANNOTATION = 5
    PERIOD_ANNOTATION = 6
    ROMAN_NUMERAL_ANNOTATION = 7
    DIAMOND_ANNOTATION = 8
    INVERTED_BOX_ANNOTATION = 9
    INVERTED_ROUNDED_BOX_ANNOTATION = 10
    FULL_WIDTH_KANA = 0
    PROPORTIONAL_KANA = 1
    FULL_WIDTH_IDEOGRAPHS = 0
    PROPORTIONAL_IDEOGRAPHS = 1
    HALF_WIDTH_IDEOGRAPHS = 2
    CANONICAL_COMPOSITION_ON = 0
    CANONICAL_COMPOSITION_OFF = 1
    COMPATIBILITY_COMPOSITION_ON = 2
    COMPATIBILITY_COMPOSITION_OFF = 3
    TRANSCODING_COMPOSITION_ON = 4
    TRANSCODING_COMPOSITION_OFF = 5
    NO_RUBY_KANA = 0
    RUBY_KANA = 1
    RUBY_KANA_ON = 2
    RUBY_KANA_OFF = 3
    NO_CJK_SYMBOL_ALTERNATIVES = 0
    CJK_SYMBOL_ALT_ONE = 1
    CJK_SYMBOL_ALT_TWO = 2
    CJK_SYMBOL_ALT_THREE = 3
    CJK_SYMBOL_ALT_FOUR = 4
    CJK_SYMBOL_ALT_FIVE = 5
    NO_IDEOGRAPHIC_ALTERNATIVES = 0
    IDEOGRAPHIC_ALT_ONE = 1
    IDEOGRAPHIC_ALT_TWO = 2
    IDEOGRAPHIC_ALT_THREE = 3
    IDEOGRAPHIC_ALT_FOUR = 4
    IDEOGRAPHIC_ALT_FIVE = 5
    CJK_VERTICAL_ROMAN_CENTERED = 0
    CJK_VERTICAL_ROMAN_HBASELINE = 1
    NO_CJK_ITALIC_ROMAN = 0
    CJK_ITALIC_ROMAN = 1
    CJK_ITALIC_ROMAN_ON = 2
    CJK_ITALIC_ROMAN_OFF = 3
    CASE_SENSITIVE_LAYOUT_ON = 0
    CASE_SENSITIVE_LAYOUT_OFF = 1
    CASE_SENSITIVE_SPACING_ON = 2
    CASE_SENSITIVE_SPACING_OFF = 3
    ALTERNATE_HORIZ_KANA_ON = 0
    ALTERNATE_HORIZ_KANA_OFF = 1
    ALTERNATE_VERT_KANA_ON = 2
    ALTERNATE_VERT_KANA_OFF = 3
    NO_STYLISTIC_ALTERNATES = 0
    STYLISTIC_ALT_ONE_ON = 2
    STYLISTIC_ALT_ONE_OFF = 3
    STYLISTIC_ALT_TWO_ON = 4
    STYLISTIC_ALT_TWO_OFF = 5
    STYLISTIC_ALT_THREE_ON = 6
    STYLISTIC_ALT_THREE_OFF = 7
    STYLISTIC_ALT_FOUR_ON = 8
    STYLISTIC_ALT_FOUR_OFF = 9
    STYLISTIC_ALT_FIVE_ON = 10
    STYLISTIC_ALT_FIVE_OFF = 11
    STYLISTIC_ALT_SIX_ON = 12
    STYLISTIC_ALT_SIX_OFF = 13
    STYLISTIC_ALT_SEVEN_ON = 14
    STYLISTIC_ALT_SEVEN_OFF = 15
    STYLISTIC_ALT_EIGHT_ON = 16
    STYLISTIC_ALT_EIGHT_OFF = 17
    STYLISTIC_ALT_NINE_ON = 18
    STYLISTIC_ALT_NINE_OFF = 19
    STYLISTIC_ALT_TEN_ON = 20
    STYLISTIC_ALT_TEN_OFF = 21
    STYLISTIC_ALT_ELEVEN_ON = 22
    STYLISTIC_ALT_ELEVEN_OFF = 23
    STYLISTIC_ALT_TWELVE_ON = 24
    STYLISTIC_ALT_TWELVE_OFF = 25
    STYLISTIC_ALT_THIRTEEN_ON = 26
    STYLISTIC_ALT_THIRTEEN_OFF = 27
    STYLISTIC_ALT_FOURTEEN_ON = 28
    STYLISTIC_ALT_FOURTEEN_OFF = 29
    STYLISTIC_ALT_FIFTEEN_ON = 30
    STYLISTIC_ALT_FIFTEEN_OFF = 31
    STYLISTIC_ALT_SIXTEEN_ON = 32
    STYLISTIC_ALT_SIXTEEN_OFF = 33
    STYLISTIC_ALT_SEVENTEEN_ON = 34
    STYLISTIC_ALT_SEVENTEEN_OFF = 35
    STYLISTIC_ALT_EIGHTEEN_ON = 36
    STYLISTIC_ALT_EIGHTEEN_OFF = 37
    STYLISTIC_ALT_NINETEEN_ON = 38
    STYLISTIC_ALT_NINETEEN_OFF = 39
    STYLISTIC_ALT_TWENTY_ON = 40
    STYLISTIC_ALT_TWENTY_OFF = 41
    CONTEXTUAL_ALTERNATES_ON = 0
    CONTEXTUAL_ALTERNATES_OFF = 1
    SWASH_ALTERNATES_ON = 2
    SWASH_ALTERNATES_OFF = 3
    CONTEXTUAL_SWASH_ALTERNATES_ON = 4
    CONTEXTUAL_SWASH_ALTERNATES_OFF = 5
    DEFAULT_LOWER_CASE = 0
    LOWER_CASE_SMALL_CAPS = 1
    LOWER_CASE_PETITE_CAPS = 2
    DEFAULT_UPPER_CASE = 0
    UPPER_CASE_SMALL_CAPS = 1
    UPPER_CASE_PETITE_CAPS = 2
    HALF_WIDTH_CJK_ROMAN = 0
    PROPORTIONAL_CJK_ROMAN = 1
    DEFAULT_CJK_ROMAN = 2
    FULL_WIDTH_CJK_ROMAN = 3

class aat_layout_feature_type_t(IntEnum):
    INVALID = 65535
    ALL_TYPOGRAPHIC = 0
    LIGATURES = 1
    CURSIVE_CONNECTION = 2
    LETTER_CASE = 3
    VERTICAL_SUBSTITUTION = 4
    LINGUISTIC_REARRANGEMENT = 5
    NUMBER_SPACING = 6
    SMART_SWASH_TYPE = 8
    DIACRITICS_TYPE = 9
    VERTICAL_POSITION = 10
    FRACTIONS = 11
    OVERLAPPING_CHARACTERS_TYPE = 13
    TYPOGRAPHIC_EXTRAS = 14
    MATHEMATICAL_EXTRAS = 15
    ORNAMENT_SETS_TYPE = 16
    CHARACTER_ALTERNATIVES = 17
    DESIGN_COMPLEXITY_TYPE = 18
    STYLE_OPTIONS = 19
    CHARACTER_SHAPE = 20
    NUMBER_CASE = 21
    TEXT_SPACING = 22
    TRANSLITERATION = 23
    ANNOTATION_TYPE = 24
    KANA_SPACING_TYPE = 25
    IDEOGRAPHIC_SPACING_TYPE = 26
    UNICODE_DECOMPOSITION_TYPE = 27
    RUBY_KANA = 28
    CJK_SYMBOL_ALTERNATIVES_TYPE = 29
    IDEOGRAPHIC_ALTERNATIVES_TYPE = 30
    CJK_VERTICAL_ROMAN_PLACEMENT_TYPE = 31
    ITALIC_CJK_ROMAN = 32
    CASE_SENSITIVE_LAYOUT = 33
    ALTERNATE_KANA = 34
    STYLISTIC_ALTERNATIVES = 35
    CONTEXTUAL_ALTERNATIVES = 36
    LOWER_CASE = 37
    UPPER_CASE = 38
    LANGUAGE_TAG_TYPE = 39
    CJK_ROMAN_SPACING_TYPE = 103

class buffer_cluster_level_t(IntEnum):
    MONOTONE_GRAPHEMES = 0
    MONOTONE_CHARACTERS = 1
    CHARACTERS = 2
    GRAPHEMES = 3
    DEFAULT = 0

class buffer_content_type_t(IntEnum):
    INVALID = 0
    UNICODE = 1
    GLYPHS = 2

class buffer_diff_flags_t(IntFlag):
    EQUAL = 0
    CONTENT_TYPE_MISMATCH = 1
    LENGTH_MISMATCH = 2
    NOTDEF_PRESENT = 4
    DOTTED_CIRCLE_PRESENT = 8
    CODEPOINT_MISMATCH = 16
    CLUSTER_MISMATCH = 32
    GLYPH_FLAGS_MISMATCH = 64
    POSITION_MISMATCH = 128

class buffer_flags_t(IntFlag):
    DEFAULT = 0
    BOT = 1
    EOT = 2
    PRESERVE_DEFAULT_IGNORABLES = 4
    REMOVE_DEFAULT_IGNORABLES = 8
    DO_NOT_INSERT_DOTTED_CIRCLE = 16
    VERIFY = 32
    PRODUCE_UNSAFE_TO_CONCAT = 64
    PRODUCE_SAFE_TO_INSERT_TATWEEL = 128
    DEFINED = 255

class buffer_serialize_flags_t(IntFlag):
    DEFAULT = 0
    NO_CLUSTERS = 1
    NO_POSITIONS = 2
    NO_GLYPH_NAMES = 4
    GLYPH_EXTENTS = 8
    GLYPH_FLAGS = 16
    NO_ADVANCES = 32
    DEFINED = 63

class buffer_serialize_format_t(IntEnum):
    TEXT = 1413830740
    JSON = 1246973774
    INVALID = 0

class direction_t(IntEnum):
    INVALID = 0
    LTR = 4
    RTL = 5
    TTB = 6
    BTT = 7

class glyph_flags_t(IntFlag):
    UNSAFE_TO_BREAK = 1
    UNSAFE_TO_CONCAT = 2
    SAFE_TO_INSERT_TATWEEL = 4
    DEFINED = 7

class memory_mode_t(IntEnum):
    DUPLICATE = 0
    READONLY = 1
    WRITABLE = 2
    READONLY_MAY_MAKE_WRITABLE = 3

class ot_color_palette_flags_t(IntFlag):
    DEFAULT = 0
    USABLE_WITH_LIGHT_BACKGROUND = 1
    USABLE_WITH_DARK_BACKGROUND = 2

class ot_layout_baseline_tag_t(IntEnum):
    ROMAN = 1919905134
    HANGING = 1751215719
    IDEO_FACE_BOTTOM_OR_LEFT = 1768121954
    IDEO_FACE_TOP_OR_RIGHT = 1768121972
    IDEO_FACE_CENTRAL = 1231251043
    IDEO_EMBOX_BOTTOM_OR_LEFT = 1768187247
    IDEO_EMBOX_TOP_OR_RIGHT = 1768191088
    IDEO_EMBOX_CENTRAL = 1231315813
    MATH = 1835103336

class ot_layout_glyph_class_t(IntEnum):
    UNCLASSIFIED = 0
    BASE_GLYPH = 1
    LIGATURE = 2
    MARK = 3
    COMPONENT = 4

class ot_math_constant_t(IntEnum):
    SCRIPT_PERCENT_SCALE_DOWN = 0
    SCRIPT_SCRIPT_PERCENT_SCALE_DOWN = 1
    DELIMITED_SUB_FORMULA_MIN_HEIGHT = 2
    DISPLAY_OPERATOR_MIN_HEIGHT = 3
    MATH_LEADING = 4
    AXIS_HEIGHT = 5
    ACCENT_BASE_HEIGHT = 6
    FLATTENED_ACCENT_BASE_HEIGHT = 7
    SUBSCRIPT_SHIFT_DOWN = 8
    SUBSCRIPT_TOP_MAX = 9
    SUBSCRIPT_BASELINE_DROP_MIN = 10
    SUPERSCRIPT_SHIFT_UP = 11
    SUPERSCRIPT_SHIFT_UP_CRAMPED = 12
    SUPERSCRIPT_BOTTOM_MIN = 13
    SUPERSCRIPT_BASELINE_DROP_MAX = 14
    SUB_SUPERSCRIPT_GAP_MIN = 15
    SUPERSCRIPT_BOTTOM_MAX_WITH_SUBSCRIPT = 16
    SPACE_AFTER_SCRIPT = 17
    UPPER_LIMIT_GAP_MIN = 18
    UPPER_LIMIT_BASELINE_RISE_MIN = 19
    LOWER_LIMIT_GAP_MIN = 20
    LOWER_LIMIT_BASELINE_DROP_MIN = 21
    STACK_TOP_SHIFT_UP = 22
    STACK_TOP_DISPLAY_STYLE_SHIFT_UP = 23
    STACK_BOTTOM_SHIFT_DOWN = 24
    STACK_BOTTOM_DISPLAY_STYLE_SHIFT_DOWN = 25
    STACK_GAP_MIN = 26
    STACK_DISPLAY_STYLE_GAP_MIN = 27
    STRETCH_STACK_TOP_SHIFT_UP = 28
    STRETCH_STACK_BOTTOM_SHIFT_DOWN = 29
    STRETCH_STACK_GAP_ABOVE_MIN = 30
    STRETCH_STACK_GAP_BELOW_MIN = 31
    FRACTION_NUMERATOR_SHIFT_UP = 32
    FRACTION_NUMERATOR_DISPLAY_STYLE_SHIFT_UP = 33
    FRACTION_DENOMINATOR_SHIFT_DOWN = 34
    FRACTION_DENOMINATOR_DISPLAY_STYLE_SHIFT_DOWN = 35
    FRACTION_NUMERATOR_GAP_MIN = 36
    FRACTION_NUM_DISPLAY_STYLE_GAP_MIN = 37
    FRACTION_RULE_THICKNESS = 38
    FRACTION_DENOMINATOR_GAP_MIN = 39
    FRACTION_DENOM_DISPLAY_STYLE_GAP_MIN = 40
    SKEWED_FRACTION_HORIZONTAL_GAP = 41
    SKEWED_FRACTION_VERTICAL_GAP = 42
    OVERBAR_VERTICAL_GAP = 43
    OVERBAR_RULE_THICKNESS = 44
    OVERBAR_EXTRA_ASCENDER = 45
    UNDERBAR_VERTICAL_GAP = 46
    UNDERBAR_RULE_THICKNESS = 47
    UNDERBAR_EXTRA_DESCENDER = 48
    RADICAL_VERTICAL_GAP = 49
    RADICAL_DISPLAY_STYLE_VERTICAL_GAP = 50
    RADICAL_RULE_THICKNESS = 51
    RADICAL_EXTRA_ASCENDER = 52
    RADICAL_KERN_BEFORE_DEGREE = 53
    RADICAL_KERN_AFTER_DEGREE = 54
    RADICAL_DEGREE_BOTTOM_RAISE_PERCENT = 55

class ot_math_glyph_part_flags_t(IntFlag):
    EXTENDER = 1

class ot_math_kern_t(IntEnum):
    TOP_RIGHT = 0
    TOP_LEFT = 1
    BOTTOM_RIGHT = 2
    BOTTOM_LEFT = 3

class ot_meta_tag_t(IntEnum):
    DESIGN_LANGUAGES = 1684827751
    SUPPORTED_LANGUAGES = 1936485991

class ot_metrics_tag_t(IntEnum):
    HORIZONTAL_ASCENDER = 1751216995
    HORIZONTAL_DESCENDER = 1751413603
    HORIZONTAL_LINE_GAP = 1751934832
    HORIZONTAL_CLIPPING_ASCENT = 1751346273
    HORIZONTAL_CLIPPING_DESCENT = 1751346276
    VERTICAL_ASCENDER = 1986098019
    VERTICAL_DESCENDER = 1986294627
    VERTICAL_LINE_GAP = 1986815856
    HORIZONTAL_CARET_RISE = 1751347827
    HORIZONTAL_CARET_RUN = 1751347822
    HORIZONTAL_CARET_OFFSET = 1751347046
    VERTICAL_CARET_RISE = 1986228851
    VERTICAL_CARET_RUN = 1986228846
    VERTICAL_CARET_OFFSET = 1986228070
    X_HEIGHT = 2020108148
    CAP_HEIGHT = 1668311156
    SUBSCRIPT_EM_X_SIZE = 1935833203
    SUBSCRIPT_EM_Y_SIZE = 1935833459
    SUBSCRIPT_EM_X_OFFSET = 1935833199
    SUBSCRIPT_EM_Y_OFFSET = 1935833455
    SUPERSCRIPT_EM_X_SIZE = 1936750707
    SUPERSCRIPT_EM_Y_SIZE = 1936750963
    SUPERSCRIPT_EM_X_OFFSET = 1936750703
    SUPERSCRIPT_EM_Y_OFFSET = 1936750959
    STRIKEOUT_SIZE = 1937011315
    STRIKEOUT_OFFSET = 1937011311
    UNDERLINE_SIZE = 1970168947
    UNDERLINE_OFFSET = 1970168943

class ot_name_id_predefined_t(IntEnum):
    COPYRIGHT = 0
    FONT_FAMILY = 1
    FONT_SUBFAMILY = 2
    UNIQUE_ID = 3
    FULL_NAME = 4
    VERSION_STRING = 5
    POSTSCRIPT_NAME = 6
    TRADEMARK = 7
    MANUFACTURER = 8
    DESIGNER = 9
    DESCRIPTION = 10
    VENDOR_URL = 11
    DESIGNER_URL = 12
    LICENSE = 13
    LICENSE_URL = 14
    TYPOGRAPHIC_FAMILY = 16
    TYPOGRAPHIC_SUBFAMILY = 17
    MAC_FULL_NAME = 18
    SAMPLE_TEXT = 19
    CID_FINDFONT_NAME = 20
    WWS_FAMILY = 21
    WWS_SUBFAMILY = 22
    LIGHT_BACKGROUND = 23
    DARK_BACKGROUND = 24
    VARIATIONS_PS_PREFIX = 25
    INVALID = 65535

class ot_var_axis_flags_t(IntFlag):
    HIDDEN = 1

class paint_composite_mode_t(IntEnum):
    CLEAR = 0
    SRC = 1
    DEST = 2
    SRC_OVER = 3
    DEST_OVER = 4
    SRC_IN = 5
    DEST_IN = 6
    SRC_OUT = 7
    DEST_OUT = 8
    SRC_ATOP = 9
    DEST_ATOP = 10
    XOR = 11
    PLUS = 12
    SCREEN = 13
    OVERLAY = 14
    DARKEN = 15
    LIGHTEN = 16
    COLOR_DODGE = 17
    COLOR_BURN = 18
    HARD_LIGHT = 19
    SOFT_LIGHT = 20
    DIFFERENCE = 21
    EXCLUSION = 22
    MULTIPLY = 23
    HSL_HUE = 24
    HSL_SATURATION = 25
    HSL_COLOR = 26
    HSL_LUMINOSITY = 27

class paint_extend_t(IntEnum):
    PAD = 0
    REPEAT = 1
    REFLECT = 2

class script_t(IntEnum):
    COMMON = 1517910393
    INHERITED = 1516858984
    UNKNOWN = 1517976186
    ARABIC = 1098015074
    ARMENIAN = 1098018158
    BENGALI = 1113943655
    CYRILLIC = 1132032620
    DEVANAGARI = 1147500129
    GEORGIAN = 1197830002
    GREEK = 1198679403
    GUJARATI = 1198877298
    GURMUKHI = 1198879349
    HANGUL = 1214344807
    HAN = 1214344809
    HEBREW = 1214603890
    HIRAGANA = 1214870113
    KANNADA = 1265525857
    KATAKANA = 1264676449
    LAO = 1281453935
    LATIN = 1281455214
    MALAYALAM = 1298954605
    ORIYA = 1332902241
    TAMIL = 1415671148
    TELUGU = 1415933045
    THAI = 1416126825
    TIBETAN = 1416192628
    BOPOMOFO = 1114599535
    BRAILLE = 1114792297
    CANADIAN_SYLLABICS = 1130458739
    CHEROKEE = 1130915186
    ETHIOPIC = 1165256809
    KHMER = 1265134962
    MONGOLIAN = 1299148391
    MYANMAR = 1299803506
    OGHAM = 1332175213
    RUNIC = 1383427698
    SINHALA = 1399418472
    SYRIAC = 1400468067
    THAANA = 1416126817
    YI = 1500080489
    DESERET = 1148416628
    GOTHIC = 1198486632
    OLD_ITALIC = 1232363884
    BUHID = 1114990692
    HANUNOO = 1214344815
    TAGALOG = 1416064103
    TAGBANWA = 1415669602
    CYPRIOT = 1131442804
    LIMBU = 1281977698
    LINEAR_B = 1281977954
    OSMANYA = 1332964705
    SHAVIAN = 1399349623
    TAI_LE = 1415670885
    UGARITIC = 1432838514
    BUGINESE = 1114990441
    COPTIC = 1131376756
    GLAGOLITIC = 1198285159
    KHAROSHTHI = 1265131890
    NEW_TAI_LUE = 1415670901
    OLD_PERSIAN = 1483761007
    SYLOTI_NAGRI = 1400466543
    TIFINAGH = 1415999079
    BALINESE = 1113681001
    CUNEIFORM = 1483961720
    NKO = 1315663727
    PHAGS_PA = 1349017959
    PHOENICIAN = 1349021304
    CARIAN = 1130459753
    CHAM = 1130914157
    KAYAH_LI = 1264675945
    LEPCHA = 1281716323
    LYCIAN = 1283023721
    LYDIAN = 1283023977
    OL_CHIKI = 1332503403
    REJANG = 1382706791
    SAURASHTRA = 1398895986
    SUNDANESE = 1400204900
    VAI = 1449224553
    AVESTAN = 1098281844
    BAMUM = 1113681269
    EGYPTIAN_HIEROGLYPHS = 1164409200
    IMPERIAL_ARAMAIC = 1098018153
    INSCRIPTIONAL_PAHLAVI = 1349020777
    INSCRIPTIONAL_PARTHIAN = 1349678185
    JAVANESE = 1247901281
    KAITHI = 1265920105
    LISU = 1281979253
    MEETEI_MAYEK = 1299473769
    OLD_SOUTH_ARABIAN = 1398895202
    OLD_TURKIC = 1332898664
    SAMARITAN = 1398893938
    TAI_THAM = 1281453665
    TAI_VIET = 1415673460
    BATAK = 1113683051
    BRAHMI = 1114792296
    MANDAIC = 1298230884
    CHAKMA = 1130457965
    MEROITIC_CURSIVE = 1298494051
    MEROITIC_HIEROGLYPHS = 1298494063
    MIAO = 1349284452
    SHARADA = 1399353956
    SORA_SOMPENG = 1399812705
    TAKRI = 1415670642
    BASSA_VAH = 1113682803
    CAUCASIAN_ALBANIAN = 1097295970
    DUPLOYAN = 1148547180
    ELBASAN = 1164730977
    GRANTHA = 1198678382
    KHOJKI = 1265135466
    KHUDAWADI = 1399418468
    LINEAR_A = 1281977953
    MAHAJANI = 1298229354
    MANICHAEAN = 1298230889
    MENDE_KIKAKUI = 1298493028
    MODI = 1299145833
    MRO = 1299345263
    NABATAEAN = 1315070324
    OLD_NORTH_ARABIAN = 1315009122
    OLD_PERMIC = 1348825709
    PAHAWH_HMONG = 1215131239
    PALMYRENE = 1348562029
    PAU_CIN_HAU = 1348564323
    PSALTER_PAHLAVI = 1349020784
    SIDDHAM = 1399415908
    TIRHUTA = 1416196712
    WARANG_CITI = 1466004065
    AHOM = 1097363309
    ANATOLIAN_HIEROGLYPHS = 1215067511
    HATRAN = 1214346354
    MULTANI = 1299541108
    OLD_HUNGARIAN = 1215655527
    SIGNWRITING = 1399287415
    ADLAM = 1097100397
    BHAIKSUKI = 1114139507
    MARCHEN = 1298231907
    OSAGE = 1332963173
    TANGUT = 1415671399
    NEWA = 1315272545
    MASARAM_GONDI = 1198485101
    NUSHU = 1316186229
    SOYOMBO = 1399814511
    ZANABAZAR_SQUARE = 1516334690
    DOGRA = 1148151666
    GUNJALA_GONDI = 1198485095
    HANIFI_ROHINGYA = 1383032935
    MAKASAR = 1298230113
    MEDEFAIDRIN = 1298490470
    OLD_SOGDIAN = 1399809903
    SOGDIAN = 1399809892
    ELYMAIC = 1164736877
    NANDINAGARI = 1315008100
    NYIAKENG_PUACHUE_HMONG = 1215131248
    WANCHO = 1466132591
    CHORASMIAN = 1130918515
    DIVES_AKURU = 1147756907
    KHITAN_SMALL_SCRIPT = 1265202291
    YEZIDI = 1499822697
    CYPRO_MINOAN = 1131441518
    OLD_UYGHUR = 1333094258
    TANGSA = 1416524641
    TOTO = 1416590447
    VITHKUQI = 1449751656
    MATH = 1517122664
    KAWI = 1264678761
    NAG_MUNDARI = 1315006317
    GARAY = 1197568609
    GURUNG_KHEMA = 1198877544
    KIRAT_RAI = 1265787241
    OL_ONAL = 1332633967
    SUNUWAR = 1400204917
    TODHRI = 1416586354
    TULU_TIGALARI = 1416983655
    BERIA_ERFE = 1113944678
    SIDETIC = 1399415924
    TAI_YO = 1415674223
    TOLONG_SIKI = 1416588403
    INVALID = 0

class style_tag_t(IntEnum):
    ITALIC = 1769234796
    OPTICAL_SIZE = 1869640570
    SLANT_ANGLE = 1936486004
    SLANT_RATIO = 1399615092
    WIDTH = 2003072104
    WEIGHT = 2003265652

class unicode_combining_class_t(IntEnum):
    NOT_REORDERED = 0
    OVERLAY = 1
    NUKTA = 7
    KANA_VOICING = 8
    VIRAMA = 9
    CCC10 = 10
    CCC11 = 11
    CCC12 = 12
    CCC13 = 13
    CCC14 = 14
    CCC15 = 15
    CCC16 = 16
    CCC17 = 17
    CCC18 = 18
    CCC19 = 19
    CCC20 = 20
    CCC21 = 21
    CCC22 = 22
    CCC23 = 23
    CCC24 = 24
    CCC25 = 25
    CCC26 = 26
    CCC27 = 27
    CCC28 = 28
    CCC29 = 29
    CCC30 = 30
    CCC31 = 31
    CCC32 = 32
    CCC33 = 33
    CCC34 = 34
    CCC35 = 35
    CCC36 = 36
    CCC84 = 84
    CCC91 = 91
    CCC103 = 103
    CCC107 = 107
    CCC118 = 118
    CCC122 = 122
    CCC129 = 129
    CCC130 = 130
    CCC132 = 132
    ATTACHED_BELOW_LEFT = 200
    ATTACHED_BELOW = 202
    ATTACHED_ABOVE = 214
    ATTACHED_ABOVE_RIGHT = 216
    BELOW_LEFT = 218
    BELOW = 220
    BELOW_RIGHT = 222
    LEFT = 224
    RIGHT = 226
    ABOVE_LEFT = 228
    ABOVE = 230
    ABOVE_RIGHT = 232
    DOUBLE_BELOW = 233
    DOUBLE_ABOVE = 234
    IOTA_SUBSCRIPT = 240
    INVALID = 255

class unicode_general_category_t(IntEnum):
    CONTROL = 0
    FORMAT = 1
    UNASSIGNED = 2
    PRIVATE_USE = 3
    SURROGATE = 4
    LOWERCASE_LETTER = 5
    MODIFIER_LETTER = 6
    OTHER_LETTER = 7
    TITLECASE_LETTER = 8
    UPPERCASE_LETTER = 9
    SPACING_MARK = 10
    ENCLOSING_MARK = 11
    NON_SPACING_MARK = 12
    DECIMAL_NUMBER = 13
    LETTER_NUMBER = 14
    OTHER_NUMBER = 15
    CONNECT_PUNCTUATION = 16
    DASH_PUNCTUATION = 17
    CLOSE_PUNCTUATION = 18
    FINAL_PUNCTUATION = 19
    INITIAL_PUNCTUATION = 20
    OTHER_PUNCTUATION = 21
    OPEN_PUNCTUATION = 22
    CURRENCY_SYMBOL = 23
    MODIFIER_SYMBOL = 24
    MATH_SYMBOL = 25
    OTHER_SYMBOL = 26
    LINE_SEPARATOR = 27
    PARAGRAPH_SEPARATOR = 28
    SPACE_SEPARATOR = 29

buffer_message_func_t = Callable[[buffer_t, font_t, str], bool_t]
color_line_get_color_stops_func_t = Callable[[color_line_t, int | None, int], int]
color_line_get_extend_func_t = Callable[[color_line_t], paint_extend_t | int]
destroy_func_t = Callable[[], None]
draw_close_path_func_t = Callable[[draw_funcs_t, int | None, draw_state_t], None]
draw_cubic_to_func_t = Callable[[draw_funcs_t, int | None, draw_state_t, float, float, float, float, float, float], None]
draw_line_to_func_t = Callable[[draw_funcs_t, int | None, draw_state_t, float, float], None]
draw_move_to_func_t = Callable[[draw_funcs_t, int | None, draw_state_t, float, float], None]
draw_quadratic_to_func_t = Callable[[draw_funcs_t, int | None, draw_state_t, float, float, float, float], None]
font_draw_glyph_func_t = Callable[[font_t, int | None, codepoint_t, draw_funcs_t], None]
font_draw_glyph_or_fail_func_t = Callable[[font_t, int | None, codepoint_t, draw_funcs_t], bool_t]
font_get_font_extents_func_t = Callable[[font_t], bool_t]
font_get_glyph_advance_func_t = Callable[[font_t, int | None, codepoint_t], position_t]
font_get_glyph_advances_func_t = Callable[[font_t, int | None, int, codepoint_t, int, int], None]
font_get_glyph_contour_point_func_t = Callable[[font_t, int | None, codepoint_t, int], bool_t]
font_get_glyph_extents_func_t = Callable[[font_t, int | None, codepoint_t], bool_t]
font_get_glyph_from_name_func_t = Callable[[font_t, int | None, list[str]], bool_t]
font_get_glyph_func_t = Callable[[font_t, int | None, codepoint_t, codepoint_t], bool_t]
font_get_glyph_kerning_func_t = Callable[[font_t, int | None, codepoint_t, codepoint_t], position_t]
font_get_glyph_name_func_t = Callable[[font_t, int | None, codepoint_t], bool_t]
font_get_glyph_origin_func_t = Callable[[font_t, int | None, codepoint_t], bool_t]
font_get_glyph_origins_func_t = Callable[[font_t, int | None, int, codepoint_t, int, int, int], bool_t]
font_get_glyph_shape_func_t = Callable[[font_t, int | None, codepoint_t, draw_funcs_t], None]
font_get_nominal_glyph_func_t = Callable[[font_t, int | None, codepoint_t], bool_t]
font_get_nominal_glyphs_func_t = Callable[[font_t, int | None, int, codepoint_t, int, int], int]
font_get_variation_glyph_func_t = Callable[[font_t, int | None, codepoint_t, codepoint_t], bool_t]
font_paint_glyph_func_t = Callable[[font_t, int | None, codepoint_t, paint_funcs_t, int | None, int, color_t], bool_t]
font_paint_glyph_or_fail_func_t = Callable[[font_t, int | None, codepoint_t, paint_funcs_t, int | None, int, color_t], bool_t]
get_table_tags_func_t = Callable[[face_t, int], int]
paint_color_func_t = Callable[[paint_funcs_t, int | None, bool_t, color_t], None]
paint_color_glyph_func_t = Callable[[paint_funcs_t, int | None, codepoint_t, font_t], bool_t]
paint_custom_palette_color_func_t = Callable[[paint_funcs_t, int | None, int], bool_t]
paint_image_func_t = Callable[[paint_funcs_t, int | None, blob_t, int, int, tag_t, float, glyph_extents_t | None], bool_t]
paint_linear_gradient_func_t = Callable[[paint_funcs_t, int | None, color_line_t, float, float, float, float, float, float], None]
paint_pop_clip_func_t = Callable[[paint_funcs_t], None]
paint_pop_group_func_t = Callable[[paint_funcs_t, int | None, paint_composite_mode_t | int], None]
paint_pop_transform_func_t = Callable[[paint_funcs_t], None]
paint_push_clip_glyph_func_t = Callable[[paint_funcs_t, int | None, codepoint_t, font_t], None]
paint_push_clip_rectangle_func_t = Callable[[paint_funcs_t, int | None, float, float, float, float], None]
paint_push_group_func_t = Callable[[paint_funcs_t], None]
paint_push_transform_func_t = Callable[[paint_funcs_t, int | None, float, float, float, float, float, float], None]
paint_radial_gradient_func_t = Callable[[paint_funcs_t, int | None, color_line_t, float, float, float, float, float, float], None]
paint_sweep_gradient_func_t = Callable[[paint_funcs_t, int | None, color_line_t, float, float, float, float], None]
reference_table_func_t = Callable[[face_t, tag_t], blob_t]
unicode_combining_class_func_t = Callable[[unicode_funcs_t, codepoint_t], unicode_combining_class_t | int]
unicode_compose_func_t = Callable[[unicode_funcs_t, codepoint_t, codepoint_t], bool_t]
unicode_decompose_compatibility_func_t = Callable[[unicode_funcs_t, codepoint_t, codepoint_t], int]
unicode_decompose_func_t = Callable[[unicode_funcs_t, codepoint_t], bool_t]
unicode_eastasian_width_func_t = Callable[[unicode_funcs_t, codepoint_t], int]
unicode_general_category_func_t = Callable[[unicode_funcs_t, codepoint_t], unicode_general_category_t | int]
unicode_mirroring_func_t = Callable[[unicode_funcs_t, codepoint_t], codepoint_t]
unicode_script_func_t = Callable[[unicode_funcs_t, codepoint_t], script_t | int]
class aat_layout_feature_selector_info_t:
    """
    Structure representing a setting for an :class:`HarfBuzz.aat_layout_feature_type_t`.
    """
    gimeta: ClassVar[_GIMeta]
    name_id: ot_name_id_t
    enable: aat_layout_feature_selector_t | int
    disable: aat_layout_feature_selector_t | int
    reserved: int

class blob_t:
    """
    Data type for blobs. A blob wraps a chunk of binary
    data and facilitates its lifecycle management between
    a client program and HarfBuzz.
    """
    gimeta: ClassVar[_GIMeta]

class buffer_t:
    """
    The main structure holding the input text and its properties before shaping,
    and output glyphs and their information after shaping.
    """
    gimeta: ClassVar[_GIMeta]

class color_line_t:
    """
    A struct containing color information for a gradient.
    """
    gimeta: ClassVar[_GIMeta]
    data: int
    get_color_stops: color_line_get_color_stops_func_t
    get_color_stops_user_data: int
    get_extend: color_line_get_extend_func_t
    get_extend_user_data: int
    reserved0: int
    reserved1: int
    reserved2: int
    reserved3: int
    reserved5: int
    reserved6: int
    reserved7: int
    reserved8: int

class color_stop_t:
    """
    Information about a color stop on a color line.

    Color lines typically have offsets ranging between 0 and 1,
    but that is not required.

    Note: despite ``color`` being unpremultiplied here, interpolation in
    gradients shall happen in premultiplied space. See the OpenType spec
    `COLR <https://learn.microsoft.com/en-us/typography/opentype/spec/colr>`_
    section for details.
    """
    gimeta: ClassVar[_GIMeta]
    offset: float
    is_foreground: bool_t
    color: color_t

class draw_funcs_t:
    """
    Glyph draw callbacks.

    #hb_draw_move_to_func_t, #hb_draw_line_to_func_t and
    #hb_draw_cubic_to_func_t calls are necessary to be defined but we translate
    #hb_draw_quadratic_to_func_t calls to #hb_draw_cubic_to_func_t if the
    callback isn't defined.
    """
    gimeta: ClassVar[_GIMeta]

class draw_state_t:
    """
    Current drawing state.
    """
    gimeta: ClassVar[_GIMeta]
    path_open: bool_t
    path_start_x: float
    path_start_y: float
    current_x: float
    current_y: float
    reserved1: var_num_t
    reserved2: var_num_t
    reserved3: var_num_t
    reserved4: var_num_t
    reserved5: var_num_t
    reserved6: var_num_t
    reserved7: var_num_t

class face_t:
    """
    Data type for holding font faces.
    """
    gimeta: ClassVar[_GIMeta]

class feature_t:
    """
    The :class:`HarfBuzz.feature_t` is the structure that holds information about requested
    feature application. The feature will be applied with the given value to all
    glyphs which are in clusters between ``start`` (inclusive) and ``end`` (exclusive).
    Setting start to :data:`HarfBuzz.FEATURE_GLOBAL_START` and end to :class:`HB_FEATURE_GLOBAL_END`
    specifies that the feature always applies to the entire buffer.
    """
    gimeta: ClassVar[_GIMeta]
    tag: tag_t
    value: int
    start: int
    end: int
    def _string(self) -> list[str]:
        """
        Converts a :class:`HarfBuzz.feature_t` into a ``None``-terminated string in the format
        understood by :func:`HarfBuzz.feature_from_string`. The client in responsible for
        allocating big enough size for ``buf``, 128 bytes is more than enough.

        Note that the feature value will be omitted if it is '1', but the
        string won't include any whitespace.
        """

class font_extents_t:
    """
    Font-wide extent values, measured in scaled units.

    Note that typically ``ascender`` is positive and ``descender``
    negative, in coordinate systems that grow up.
    """
    gimeta: ClassVar[_GIMeta]
    ascender: position_t
    descender: position_t
    line_gap: position_t
    reserved9: position_t
    reserved8: position_t
    reserved7: position_t
    reserved6: position_t
    reserved5: position_t
    reserved4: position_t
    reserved3: position_t
    reserved2: position_t
    reserved1: position_t

class font_funcs_t:
    """
    Data type containing a set of virtual methods used for
    working on :class:`HarfBuzz.font_t` font objects.

    HarfBuzz provides a lightweight default function for each of
    the methods in :class:`HarfBuzz.font_funcs_t`. Client programs can implement
    their own replacements for the individual font functions, as
    needed, and replace the default by calling the setter for a
    method.
    """
    gimeta: ClassVar[_GIMeta]

class font_t:
    """
    Data type for holding fonts.
    """
    gimeta: ClassVar[_GIMeta]

class glyph_extents_t:
    """
    Glyph extent values, measured in font units.

    Note that ``height`` is negative, in coordinate systems that grow up.
    """
    gimeta: ClassVar[_GIMeta]
    x_bearing: position_t
    y_bearing: position_t
    width: position_t
    height: position_t

class glyph_info_t:
    """
    The :class:`HarfBuzz.glyph_info_t` is the structure that holds information about the
    glyphs and their relation to input text.
    """
    gimeta: ClassVar[_GIMeta]
    codepoint: codepoint_t
    mask: mask_t
    cluster: int
    var1: var_int_t
    var2: var_int_t

class glyph_position_t:
    """
    The :class:`HarfBuzz.glyph_position_t` is the structure that holds the positions of the
    glyph in both horizontal and vertical directions. All positions in
    :class:`HarfBuzz.glyph_position_t` are relative to the current point.
    """
    gimeta: ClassVar[_GIMeta]
    x_advance: position_t
    y_advance: position_t
    x_offset: position_t
    y_offset: position_t
    var: var_int_t

class language_t:
    """
    Data type for languages. Each :class:`HarfBuzz.language_t` corresponds to a BCP 47
    language tag.
    """
    gimeta: ClassVar[_GIMeta]
    def _string(self) -> str:
        """
        Converts an :class:`HarfBuzz.language_t` to a string.
        """

class map_t:
    """
    Data type for holding integer-to-integer hash maps.
    """
    gimeta: ClassVar[_GIMeta]

class ot_color_layer_t:
    """
    Pairs of glyph and color index.

    A color index of 0xFFFF does not refer to a palette
    color, but indicates that the foreground color should
    be used.
    """
    gimeta: ClassVar[_GIMeta]
    glyph: codepoint_t
    color_index: int

class ot_math_glyph_part_t:
    """
    Data type to hold information for a "part" component of a math-variant glyph.
    Large variants for stretchable math glyphs (such as parentheses) can be constructed
    on the fly from parts.
    """
    gimeta: ClassVar[_GIMeta]
    glyph: codepoint_t
    start_connector_length: position_t
    end_connector_length: position_t
    full_advance: position_t
    flags: ot_math_glyph_part_flags_t | int

class ot_math_glyph_variant_t:
    """
    Data type to hold math-variant information for a glyph.
    """
    gimeta: ClassVar[_GIMeta]
    glyph: codepoint_t
    advance: position_t

class ot_math_kern_entry_t:
    """
    Data type to hold math kerning (cut-in) information for a glyph.
    """
    gimeta: ClassVar[_GIMeta]
    max_correction_height: position_t
    kern_value: position_t

class ot_name_entry_t:
    """
    Structure representing a name ID in a particular language.
    """
    gimeta: ClassVar[_GIMeta]
    name_id: ot_name_id_t
    var: var_int_t
    language: language_t

class ot_var_axis_info_t:
    """
    Data type for holding variation-axis values.

    The minimum, default, and maximum values are in un-normalized, user scales.

    <note>Note: at present, the only flag defined for ``flags`` is
    :data:`HarfBuzz.ot_var_axis_flags_t.HIDDEN`.</note>
    """
    gimeta: ClassVar[_GIMeta]
    axis_index: int
    tag: tag_t
    name_id: ot_name_id_t
    flags: ot_var_axis_flags_t | int
    min_value: float
    default_value: float
    max_value: float
    reserved: int

@deprecated("Deprecated since 2.2.0.")
class ot_var_axis_t:
    """
    Use :class:`HarfBuzz.ot_var_axis_info_t` instead.
    """
    gimeta: ClassVar[_GIMeta]
    tag: tag_t
    name_id: ot_name_id_t
    min_value: float
    default_value: float
    max_value: float

class paint_funcs_t:
    """
    Glyph paint callbacks.

    The callbacks assume that the caller maintains a stack
    of current transforms, clips and intermediate surfaces,
    as evidenced by the pairs of push/pop callbacks. The
    push/pop calls will be properly nested, so it is fine
    to store the different kinds of object on a single stack.

    Not all callbacks are required for all kinds of glyphs.
    For rendering COLRv0 or non-color outline glyphs, the
    gradient callbacks are not needed, and the composite
    callback only needs to handle simple alpha compositing
    (:data:`HarfBuzz.paint_composite_mode_t.SRC_OVER`).

    The paint-image callback is only needed for glyphs
    with image blobs in the CBDT, sbix or SVG tables.

    The custom-palette-color callback is only necessary if
    you want to override colors from the font palette with
    custom colors.
    """
    gimeta: ClassVar[_GIMeta]

class segment_properties_t:
    """
    The structure that holds various text properties of an :class:`HarfBuzz.buffer_t`. Can be
    set and retrieved using :func:`HarfBuzz.buffer_set_segment_properties` and
    :func:`HarfBuzz.buffer_get_segment_properties`, respectively.
    """
    gimeta: ClassVar[_GIMeta]
    direction: direction_t | int
    script: script_t | int
    language: language_t
    reserved1: int
    reserved2: int

class set_t:
    """
    Data type for holding a set of integers. :class:`HarfBuzz.set_t`'s are
    used to gather and contain glyph IDs, Unicode code
    points, and various other collections of discrete
    values.
    """
    gimeta: ClassVar[_GIMeta]

class shape_plan_t:
    """
    Data type for holding a shaping plan.

    Shape plans contain information about how HarfBuzz will shape a
    particular text segment, based on the segment's properties and the
    capabilities in the font face in use.

    Shape plans can be queried about how shaping will perform, given a set
    of specific input parameters (script, language, direction, features,
    etc.).
    """
    gimeta: ClassVar[_GIMeta]

class unicode_funcs_t:
    """
    Data type containing a set of virtual methods used for
    accessing various Unicode character properties.

    HarfBuzz provides a default function for each of the
    methods in :class:`HarfBuzz.unicode_funcs_t`. Client programs can implement
    their own replacements for the individual Unicode functions, as
    needed, and replace the default by calling the setter for a
    method.
    """
    gimeta: ClassVar[_GIMeta]

class user_data_key_t:
    """
    Data structure for holding user-data keys.
    """
    gimeta: ClassVar[_GIMeta]
    unused: int

class var_int_t:
    gimeta: ClassVar[_GIMeta]
    u32: int
    i32: int
    u16: list[int]
    i16: list[int]
    u8: list[int]
    i8: list[int]

class var_num_t:
    gimeta: ClassVar[_GIMeta]
    f: float
    u32: int
    i32: int
    u16: list[int]
    i16: list[int]
    u8: list[int]
    i8: list[int]

class variation_t:
    """
    Data type for holding variation data. Registered OpenType
    variation-axis tags are listed in
    `OpenType Axis Tag Registry <https://docs.microsoft.com/en-us/typography/opentype/spec/dvaraxisreg>`_.
    """
    gimeta: ClassVar[_GIMeta]
    tag: tag_t
    value: float
    def _string(self) -> list[str]:
        """
        Converts an :class:`HarfBuzz.variation_t` into a ``None``-terminated string in the format
        understood by :func:`HarfBuzz.variation_from_string`. The client in responsible for
        allocating big enough size for ``buf``, 128 bytes is more than enough.

        Note that the string won't include any whitespace.
        """

AAT_LAYOUT_NO_SELECTOR_INDEX: int
"""
Used when getting or setting AAT feature selectors. Indicates that
there is no selector index corresponding to the selector of interest.
"""
BUFFER_REPLACEMENT_CODEPOINT_DEFAULT: int
"""
The default code point for replacing invalid characters in a given encoding.
Set to U+FFFD REPLACEMENT CHARACTER.
"""
CODEPOINT_INVALID: codepoint_t
"""
Unused #hb_codepoint_t value.
"""
FEATURE_GLOBAL_START: int
"""
Special setting for :class:`HarfBuzz.feature_t`.start to apply the feature from the start
of the buffer.
"""
FONT_NO_VAR_NAMED_INSTANCE: int
"""
Constant signifying that a font does not have any
named-instance index set.  This is the default of
a font.
"""
LANGUAGE_INVALID: language_t
"""
An unset :class:`HarfBuzz.language_t`.
"""
OT_LAYOUT_DEFAULT_LANGUAGE_INDEX: int
"""
Special value for language index indicating default or unsupported language.
"""
OT_LAYOUT_NO_FEATURE_INDEX: int
"""
Special value for feature index indicating unsupported feature.
"""
OT_LAYOUT_NO_SCRIPT_INDEX: int
"""
Special value for script index indicating unsupported script.
"""
OT_LAYOUT_NO_VARIATIONS_INDEX: int
"""
Special value for variations index indicating unsupported variation.
"""
OT_MAX_TAGS_PER_LANGUAGE: int
"""
Maximum number of OpenType tags that can correspond to a give :class:`HarfBuzz.language_t`.
"""
OT_MAX_TAGS_PER_SCRIPT: int
"""
Maximum number of OpenType tags that can correspond to a give :class:`HarfBuzz.script_t`.
"""
OT_VAR_NO_AXIS_INDEX: int
"""
Do not use.
"""
UNICODE_COMBINING_CLASS_CCC133: int
"""
[Tibetan]
"""
UNICODE_MAX: int
"""
Maximum valid Unicode code point.
"""
UNICODE_MAX_DECOMPOSITION_LEN: int
"""
See Unicode 6.1 for details on the maximum decomposition length.
"""
VERSION_MAJOR: int
VERSION_MICRO: int
VERSION_MINOR: int
VERSION_STRING: str
def aat_layout_feature_type_get_name_id(face: face_t, feature_type: aat_layout_feature_type_t | int) -> ot_name_id_t:
    """
    Fetches the name identifier of the specified feature type in the face's ``name`` table.
    """
def aat_layout_feature_type_get_selector_infos(face: face_t, feature_type: aat_layout_feature_type_t | int, start_offset: int) -> tuple[int, list[aat_layout_feature_selector_info_t], int]:
    """
    Fetches a list of the selectors available for the specified feature in the given face.

    If upon return, ``default_index`` is set to :data:`HarfBuzz.AAT_LAYOUT_NO_SELECTOR_INDEX`, then
    the feature type is non-exclusive.  Otherwise, ``default_index`` is the index of
    the selector that is selected by default.
    """
def aat_layout_get_feature_types(face: face_t, start_offset: int) -> tuple[int, list[aat_layout_feature_type_t | int]]:
    """
    Fetches a list of the AAT feature types included in the specified face.
    """
def aat_layout_has_positioning(face: face_t) -> bool_t:
    """
    Tests whether the specified face includes any positioning information
    in the ``kerx`` table.

    <note>Note: does not examine the ``GPOS`` table.</note>
    """
def aat_layout_has_substitution(face: face_t) -> bool_t:
    """
    Tests whether the specified face includes any substitutions in the
    ``morx`` or ``mort`` tables.

    <note>Note: does not examine the ``GSUB`` table.</note>
    """
def aat_layout_has_tracking(face: face_t) -> bool_t:
    """
    Tests whether the specified face includes any tracking information
    in the ``trak`` table.
    """
def blob_copy_writable_or_fail(blob: blob_t) -> blob_t:
    """
    Makes a writable copy of ``blob``.
    """
def blob_create_from_file(file_name: str) -> blob_t:
    """
    Creates a new blob containing the data from the
    specified binary font file.

    The filename is passed directly to the system on all platforms,
    except on Windows, where the filename is interpreted as UTF-8.
    Only if the filename is not valid UTF-8, it will be interpreted
    according to the system codepage.
    """
def blob_create_from_file_or_fail(file_name: str) -> blob_t:
    """
    Creates a new blob containing the data from the specified file.

    The filename is passed directly to the system on all platforms,
    except on Windows, where the filename is interpreted as UTF-8.
    Only if the filename is not valid UTF-8, it will be interpreted
    according to the system codepage.
    """
def blob_create_sub_blob(parent: blob_t, offset: int, length: int) -> blob_t:
    """
    Returns a blob that represents a range of bytes in ``parent``.  The new
    blob is always created with :data:`HarfBuzz.memory_mode_t.READONLY`, meaning that it
    will never modify data in the parent blob.  The parent data is not
    expected to be modified, and will result in undefined behavior if it
    is.

    Makes ``parent`` immutable.
    """
def blob_get_data(blob: blob_t) -> list[str] | None:
    """
    Fetches the data from a blob.
    """
def blob_get_data_writable(blob: blob_t) -> list[str]:
    """
    Tries to make blob data writable (possibly copying it) and
    return pointer to data.

    Fails if blob has been made immutable, or if memory allocation
    fails.
    """
def blob_get_empty() -> blob_t:
    """
    Returns the singleton empty blob.

    See TODO:link object types for more information.
    """
def blob_get_length(blob: blob_t) -> int:
    """
    Fetches the length of a blob's data.
    """
def blob_is_immutable(blob: blob_t) -> bool_t:
    """
    Tests whether a blob is immutable.
    """
def blob_make_immutable(blob: blob_t) -> None:
    """
    Makes a blob immutable.
    """
def buffer_add(buffer: buffer_t, codepoint: codepoint_t, cluster: int) -> None:
    """
    Appends a character with the Unicode value of ``codepoint`` to ``buffer``, and
    gives it the initial cluster value of ``cluster``. Clusters can be any thing
    the client wants, they are usually used to refer to the index of the
    character in the input text stream and are output in
    :class:`HarfBuzz.glyph_info_t`.cluster field.

    This function does not check the validity of ``codepoint``, it is up to the
    caller to ensure it is a valid Unicode code point.
    """
def buffer_add_codepoints(buffer: buffer_t, text: list[codepoint_t], item_offset: int, item_length: int) -> None:
    """
    Appends characters from ``text`` array to ``buffer``. The ``item_offset`` is the
    position of the first character from ``text`` that will be appended, and
    ``item_length`` is the number of character. When shaping part of a larger text
    (e.g. a run of text from a paragraph), instead of passing just the substring
    corresponding to the run, it is preferable to pass the whole
    paragraph and specify the run start and length as ``item_offset`` and
    ``item_length``, respectively, to give HarfBuzz the full context to be able,
    for example, to do cross-run Arabic shaping or properly handle combining
    marks at stat of run.

    This function does not check the validity of ``text``, it is up to the caller
    to ensure it contains a valid Unicode scalar values.  In contrast,
    :func:`HarfBuzz.buffer_add_utf32` can be used that takes similar input but performs
    sanity-check on the input.
    """
def buffer_add_latin1(buffer: buffer_t, text: list[int], item_offset: int, item_length: int) -> None:
    """
    Similar to :func:`HarfBuzz.buffer_add_codepoints`, but allows only access to first 256
    Unicode code points that can fit in 8-bit strings.

    <note>Has nothing to do with non-Unicode Latin-1 encoding.</note>
    """
def buffer_add_utf16(buffer: buffer_t, text: list[int], item_offset: int, item_length: int) -> None:
    """
    See :func:`HarfBuzz.buffer_add_codepoints`.

    Replaces invalid UTF-16 characters with the ``buffer`` replacement code point,
    see :func:`HarfBuzz.buffer_set_replacement_codepoint`.
    """
def buffer_add_utf32(buffer: buffer_t, text: list[int], item_offset: int, item_length: int) -> None:
    """
    See :func:`HarfBuzz.buffer_add_codepoints`.

    Replaces invalid UTF-32 characters with the ``buffer`` replacement code point,
    see :func:`HarfBuzz.buffer_set_replacement_codepoint`.
    """
def buffer_add_utf8(buffer: buffer_t, text: list[int], item_offset: int, item_length: int) -> None:
    """
    See :func:`HarfBuzz.buffer_add_codepoints`.

    Replaces invalid UTF-8 characters with the ``buffer`` replacement code point,
    see :func:`HarfBuzz.buffer_set_replacement_codepoint`.
    """
def buffer_allocation_successful(buffer: buffer_t) -> bool_t:
    """
    Check if allocating memory for the buffer succeeded.
    """
def buffer_append(buffer: buffer_t, source: buffer_t, start: int, end: int) -> None:
    """
    Append (part of) contents of another buffer to this buffer.
    """
def buffer_clear_contents(buffer: buffer_t) -> None:
    """
    Similar to :func:`HarfBuzz.buffer_reset`, but does not clear the Unicode functions and
    the replacement code point.
    """
def buffer_create() -> buffer_t:
    """
    Creates a new :class:`HarfBuzz.buffer_t` with all properties to defaults.
    """
def buffer_create_similar(src: buffer_t) -> buffer_t:
    """
    Creates a new :class:`HarfBuzz.buffer_t`, similar to :func:`HarfBuzz.buffer_create`. The only
    difference is that the buffer is configured similarly to ``src``.
    """
def buffer_deserialize_glyphs(buffer: buffer_t, buf: list[str], font: font_t | None, format: buffer_serialize_format_t | int) -> tuple[bool_t, str]:
    """
    Deserializes glyphs ``buffer`` from textual representation in the format
    produced by :func:`HarfBuzz.buffer_serialize_glyphs`.
    """
def buffer_deserialize_unicode(buffer: buffer_t, buf: list[str], format: buffer_serialize_format_t | int) -> tuple[bool_t, str]:
    """
    Deserializes Unicode ``buffer`` from textual representation in the format
    produced by :func:`HarfBuzz.buffer_serialize_unicode`.
    """
def buffer_diff(buffer: buffer_t, reference: buffer_t, dottedcircle_glyph: codepoint_t, position_fuzz: int) -> buffer_diff_flags_t:
    """
    If dottedcircle_glyph is (hb_codepoint_t) -1 then :data:`HarfBuzz.buffer_diff_flags_t.DOTTED_CIRCLE_PRESENT`
    and :data:`HarfBuzz.buffer_diff_flags_t.NOTDEF_PRESENT` are never returned.  This should be used by most
    callers if just comparing two buffers is needed.
    """
def buffer_get_cluster_level(buffer: buffer_t) -> buffer_cluster_level_t:
    """
    Fetches the cluster level of a buffer. The :class:`HarfBuzz.buffer_cluster_level_t`
    dictates one aspect of how HarfBuzz will treat non-base characters
    during shaping.
    """
def buffer_get_content_type(buffer: buffer_t) -> buffer_content_type_t:
    """
    Fetches the type of ``buffer`` contents. Buffers are either empty, contain
    characters (before shaping), or contain glyphs (the result of shaping).
    """
def buffer_get_direction(buffer: buffer_t) -> direction_t:
    """
    See :func:`HarfBuzz.buffer_set_direction`
    """
def buffer_get_empty() -> buffer_t:
    """
    Fetches an empty :class:`HarfBuzz.buffer_t`.
    """
def buffer_get_flags(buffer: buffer_t) -> buffer_flags_t:
    """
    Fetches the :class:`HarfBuzz.buffer_flags_t` of ``buffer``.
    """
def buffer_get_glyph_infos(buffer: buffer_t) -> list[glyph_info_t]:
    """
    Returns ``buffer`` glyph information array.  Returned pointer
    is valid as long as ``buffer`` contents are not modified.
    """
def buffer_get_glyph_positions(buffer: buffer_t) -> list[glyph_position_t]:
    """
    Returns ``buffer`` glyph position array.  Returned pointer
    is valid as long as ``buffer`` contents are not modified.

    If buffer did not have positions before, the positions will be
    initialized to zeros, unless this function is called from
    within a buffer message callback (see :func:`HarfBuzz.buffer_set_message_func`),
    in which case ``None`` is returned.
    """
def buffer_get_invisible_glyph(buffer: buffer_t) -> codepoint_t:
    """
    See :func:`HarfBuzz.buffer_set_invisible_glyph`.
    """
def buffer_get_language(buffer: buffer_t) -> language_t:
    """
    See :func:`HarfBuzz.buffer_set_language`.
    """
def buffer_get_length(buffer: buffer_t) -> int:
    """
    Returns the number of items in the buffer.
    """
def buffer_get_not_found_glyph(buffer: buffer_t) -> codepoint_t:
    """
    See :func:`HarfBuzz.buffer_set_not_found_glyph`.
    """
def buffer_get_not_found_variation_selector_glyph(buffer: buffer_t) -> codepoint_t:
    """
    See :func:`HarfBuzz.buffer_set_not_found_variation_selector_glyph`.
    """
def buffer_get_random_state(buffer: buffer_t) -> int:
    """
    See :func:`HarfBuzz.buffer_set_random_state`.
    """
def buffer_get_replacement_codepoint(buffer: buffer_t) -> codepoint_t:
    """
    Fetches the #hb_codepoint_t that replaces invalid entries for a given encoding
    when adding text to ``buffer``.
    """
def buffer_get_script(buffer: buffer_t) -> script_t:
    """
    Fetches the script of ``buffer``.
    """
def buffer_get_segment_properties(buffer: buffer_t) -> segment_properties_t:
    """
    Sets ``props`` to the :class:`HarfBuzz.segment_properties_t` of ``buffer``.
    """
def buffer_get_unicode_funcs(buffer: buffer_t) -> unicode_funcs_t:
    """
    Fetches the Unicode-functions structure of a buffer.
    """
def buffer_guess_segment_properties(buffer: buffer_t) -> None:
    """
    Sets unset buffer segment properties based on buffer Unicode
    contents.  If buffer is not empty, it must have content type
    :data:`HarfBuzz.buffer_content_type_t.UNICODE`.

    If buffer script is not set (ie. is :data:`HarfBuzz.script_t.INVALID`), it
    will be set to the Unicode script of the first character in
    the buffer that has a script other than :data:`HarfBuzz.script_t.COMMON`,
    :data:`HarfBuzz.script_t.INHERITED`, and :data:`HarfBuzz.script_t.UNKNOWN`.

    Next, if buffer direction is not set (ie. is :data:`HarfBuzz.direction_t.INVALID`),
    it will be set to the natural horizontal direction of the
    buffer script as returned by :func:`HarfBuzz.script_get_horizontal_direction`.
    If :func:`HarfBuzz.script_get_horizontal_direction` returns :data:`HarfBuzz.direction_t.INVALID`,
    then :data:`HarfBuzz.direction_t.LTR` is used.

    Finally, if buffer language is not set (ie. is :data:`HarfBuzz.LANGUAGE_INVALID`),
    it will be set to the process's default language as returned by
    :func:`HarfBuzz.language_get_default`.  This may change in the future by
    taking buffer script into consideration when choosing a language.
    Note that :func:`HarfBuzz.language_get_default` is NOT threadsafe the first time
    it is called.  See documentation for that function for details.
    """
def buffer_has_positions(buffer: buffer_t) -> bool_t:
    """
    Returns whether ``buffer`` has glyph position data.
    A buffer gains position data when :func:`HarfBuzz.buffer_get_glyph_positions` is called on it,
    and cleared of position data when :func:`HarfBuzz.buffer_clear_contents` is called.
    """
def buffer_normalize_glyphs(buffer: buffer_t) -> None:
    """
    Reorders a glyph buffer to have canonical in-cluster glyph order / position.
    The resulting clusters should behave identical to pre-reordering clusters.

    <note>This has nothing to do with Unicode normalization.</note>
    """
def buffer_pre_allocate(buffer: buffer_t, size: int) -> bool_t:
    """
    Pre allocates memory for ``buffer`` to fit at least ``size`` number of items.
    """
def buffer_reset(buffer: buffer_t) -> None:
    """
    Resets the buffer to its initial status, as if it was just newly created
    with :func:`HarfBuzz.buffer_create`.
    """
def buffer_reverse(buffer: buffer_t) -> None:
    """
    Reverses buffer contents.
    """
def buffer_reverse_clusters(buffer: buffer_t) -> None:
    """
    Reverses buffer clusters.  That is, the buffer contents are
    reversed, then each cluster (consecutive items having the
    same cluster number) are reversed again.
    """
def buffer_reverse_range(buffer: buffer_t, start: int, end: int) -> None:
    """
    Reverses buffer contents between ``start`` and ``end``.
    """
def buffer_serialize(buffer: buffer_t, start: int, end: int, font: font_t | None, format: buffer_serialize_format_t | int, flags: buffer_serialize_flags_t | int) -> tuple[int, list[int], int]:
    """
    Serializes ``buffer`` into a textual representation of its content, whether
    Unicode codepoints or glyph identifiers and positioning information. This is
    useful for showing the contents of the buffer, for example during debugging.
    See the documentation of :func:`HarfBuzz.buffer_serialize_unicode` and
    :func:`HarfBuzz.buffer_serialize_glyphs` for a description of the output format.
    """
def buffer_serialize_format_from_string(str: list[int]) -> buffer_serialize_format_t:
    """
    Parses a string into an :class:`HarfBuzz.buffer_serialize_format_t`. Does not check if
    ``str`` is a valid buffer serialization format, use
    :func:`HarfBuzz.buffer_serialize_list_formats` to get the list of supported formats.
    """
def buffer_serialize_format_to_string(format: buffer_serialize_format_t | int) -> str:
    """
    Converts ``format`` to the string corresponding it, or ``None`` if it is not a valid
    :class:`HarfBuzz.buffer_serialize_format_t`.
    """
def buffer_serialize_glyphs(buffer: buffer_t, start: int, end: int, font: font_t | None, format: buffer_serialize_format_t | int, flags: buffer_serialize_flags_t | int) -> tuple[int, list[int], int]:
    """
    Serializes ``buffer`` into a textual representation of its glyph content,
    useful for showing the contents of the buffer, for example during debugging.
    There are currently two supported serialization formats:

    ## text
    A human-readable, plain text format.
    The serialized glyphs will look something like:

    .. code-block:: text

        [uni0651=0@518,0+0|uni0628=0+1897]

    - The serialized glyphs are delimited with ``[`` and ``]``.
    - Glyphs are separated with ``|``
    - Each glyph starts with glyph name, or glyph index if
      :data:`HarfBuzz.buffer_serialize_flags_t.NO_GLYPH_NAMES` flag is set. Then,
      - If :data:`HarfBuzz.buffer_serialize_flags_t.NO_CLUSTERS` is not set, ``=`` then :class:`HarfBuzz.glyph_info_t`.cluster.
      - If :data:`HarfBuzz.buffer_serialize_flags_t.NO_POSITIONS` is not set, the :class:`HarfBuzz.glyph_position_t` in the format:
        - If :class:`HarfBuzz.glyph_position_t`.x_offset and :class:`HarfBuzz.glyph_position_t`.y_offset are not both 0, ``x_offset``,y_offset``. Then,
        - ``+x_advance``, then ``,y_advance`` if :class:`HarfBuzz.glyph_position_t`.y_advance is not 0. Then,
      - If :data:`HarfBuzz.buffer_serialize_flags_t.GLYPH_EXTENTS` is set, the :class:`HarfBuzz.glyph_extents_t` in the format ``<x_bearing,y_bearing,width,height>``

    ## json
    A machine-readable, structured format.
    The serialized glyphs will look something like:

    .. code-block:: text

        [{"g":"uni0651","cl":0,"dx":518,"dy":0,"ax":0,"ay":0},
        {"g":"uni0628","cl":0,"dx":0,"dy":0,"ax":1897,"ay":0}]

    Each glyph is a JSON object, with the following properties:
    - ``g``: the glyph name or glyph index if
      :data:`HarfBuzz.buffer_serialize_flags_t.NO_GLYPH_NAMES` flag is set.
    - ``cl``: :class:`HarfBuzz.glyph_info_t`.cluster if
      :data:`HarfBuzz.buffer_serialize_flags_t.NO_CLUSTERS` is not set.
    - ``dx``,``dy``,``ax``,``ay``: :class:`HarfBuzz.glyph_position_t`.x_offset, :class:`HarfBuzz.glyph_position_t`.y_offset,
       :class:`HarfBuzz.glyph_position_t`.x_advance and :class:`HarfBuzz.glyph_position_t`.y_advance
       respectively, if :data:`HarfBuzz.buffer_serialize_flags_t.NO_POSITIONS` is not set.
    - ``xb``,``yb``,``w``,``h``: :class:`HarfBuzz.glyph_extents_t`.x_bearing, :class:`HarfBuzz.glyph_extents_t`.y_bearing,
       :class:`HarfBuzz.glyph_extents_t`.width and :class:`HarfBuzz.glyph_extents_t`.height respectively if
       :data:`HarfBuzz.buffer_serialize_flags_t.GLYPH_EXTENTS` is set.
    """
def buffer_serialize_list_formats() -> list[str]:
    """
    Returns a list of supported buffer serialization formats.
    """
def buffer_serialize_unicode(buffer: buffer_t, start: int, end: int, format: buffer_serialize_format_t | int, flags: buffer_serialize_flags_t | int) -> tuple[int, list[int], int]:
    """
    Serializes ``buffer`` into a textual representation of its content,
    when the buffer contains Unicode codepoints (i.e., before shaping). This is
    useful for showing the contents of the buffer, for example during debugging.
    There are currently two supported serialization formats:

    ## text
    A human-readable, plain text format.
    The serialized codepoints will look something like:

    .. code-block:: text

         <U+0651=0|U+0628=1>

    - Glyphs are separated with ``|``
    - Unicode codepoints are expressed as zero-padded four (or more)
      digit hexadecimal numbers preceded by ``U+``
    - If :data:`HarfBuzz.buffer_serialize_flags_t.NO_CLUSTERS` is not set, the cluster
      will be indicated with a ``=`` then :class:`HarfBuzz.glyph_info_t`.cluster.

    ## json
    A machine-readable, structured format.
    The serialized codepoints will be a list of objects with the following
    properties:
    - ``u``: the Unicode codepoint as a decimal integer
    - ``cl``: :class:`HarfBuzz.glyph_info_t`.cluster if
      :data:`HarfBuzz.buffer_serialize_flags_t.NO_CLUSTERS` is not set.

    For example:

    .. code-block:: text

        [{u:1617,cl:0},{u:1576,cl:1}]
    """
def buffer_set_cluster_level(buffer: buffer_t, cluster_level: buffer_cluster_level_t | int) -> None:
    """
    Sets the cluster level of a buffer. The :class:`HarfBuzz.buffer_cluster_level_t`
    dictates one aspect of how HarfBuzz will treat non-base characters
    during shaping.
    """
def buffer_set_content_type(buffer: buffer_t, content_type: buffer_content_type_t | int) -> None:
    """
    Sets the type of ``buffer`` contents. Buffers are either empty, contain
    characters (before shaping), or contain glyphs (the result of shaping).

    You rarely need to call this function, since a number of other
    functions transition the content type for you. Namely:

    - A newly created buffer starts with content type
      :data:`HarfBuzz.buffer_content_type_t.INVALID`. Calling :func:`HarfBuzz.buffer_reset`,
      :func:`HarfBuzz.buffer_clear_contents`, as well as calling :func:`HarfBuzz.buffer_set_length`
      with an argument of zero all set the buffer content type to invalid
      as well.

    - Calling :func:`HarfBuzz.buffer_add_utf8`, :func:`HarfBuzz.buffer_add_utf16`,
      :func:`HarfBuzz.buffer_add_utf32`, :func:`HarfBuzz.buffer_add_codepoints` and
      :func:`HarfBuzz.buffer_add_latin1` expect that buffer is either empty and
      have a content type of invalid, or that buffer content type is
      :data:`HarfBuzz.buffer_content_type_t.UNICODE`, and they also set the content
      type to Unicode if they added anything to an empty buffer.

    - Finally :func:`HarfBuzz.shape` and :func:`HarfBuzz.shape_full` expect that the buffer
      is either empty and have content type of invalid, or that buffer
      content type is :data:`HarfBuzz.buffer_content_type_t.UNICODE`, and upon
      success they set the buffer content type to
      :data:`HarfBuzz.buffer_content_type_t.GLYPHS`.

    The above transitions are designed such that one can use a buffer
    in a loop of "reset : add-text : shape" without needing to ever
    modify the content type manually.
    """
def buffer_set_direction(buffer: buffer_t, direction: direction_t | int) -> None:
    """
    Set the text flow direction of the buffer. No shaping can happen without
    setting ``buffer`` direction, and it controls the visual direction for the
    output glyphs; for RTL direction the glyphs will be reversed. Many layout
    features depend on the proper setting of the direction, for example,
    reversing RTL text before shaping, then shaping with LTR direction is not
    the same as keeping the text in logical order and shaping with RTL
    direction.
    """
def buffer_set_flags(buffer: buffer_t, flags: buffer_flags_t | int) -> None:
    """
    Sets ``buffer`` flags to ``flags``. See :class:`HarfBuzz.buffer_flags_t`.
    """
def buffer_set_invisible_glyph(buffer: buffer_t, invisible: codepoint_t) -> None:
    """
    Sets the #hb_codepoint_t that replaces invisible characters in
    the shaping result.  If set to zero (default), the glyph for the
    U+0020 SPACE character is used.  Otherwise, this value is used
    verbatim.
    """
def buffer_set_language(buffer: buffer_t, language: language_t) -> None:
    """
    Sets the language of ``buffer`` to ``language``.

    Languages are crucial for selecting which OpenType feature to apply to the
    buffer which can result in applying language-specific behaviour. Languages
    are orthogonal to the scripts, and though they are related, they are
    different concepts and should not be confused with each other.

    Use :func:`HarfBuzz.language_from_string` to convert from BCP 47 language tags to
    :class:`HarfBuzz.language_t`.
    """
def buffer_set_length(buffer: buffer_t, length: int) -> bool_t:
    """
    Similar to :func:`HarfBuzz.buffer_pre_allocate`, but clears any new items added at the
    end.
    """
def buffer_set_message_func(buffer: buffer_t, func: buffer_message_func_t) -> None:
    """
    Sets the implementation function for #hb_buffer_message_func_t.
    """
def buffer_set_not_found_glyph(buffer: buffer_t, not_found: codepoint_t) -> None:
    """
    Sets the #hb_codepoint_t that replaces characters not found in
    the font during shaping.

    The not-found glyph defaults to zero, sometimes known as the
    ".notdef" glyph.  This API allows for differentiating the two.
    """
def buffer_set_not_found_variation_selector_glyph(buffer: buffer_t, not_found_variation_selector: codepoint_t) -> None:
    """
    Sets the #hb_codepoint_t that replaces variation-selector characters not resolved
    in the font during shaping.

    The not-found-variation-selector glyph defaults to :data:`HarfBuzz.CODEPOINT_INVALID`,
    in which case an unresolved variation-selector will be removed from the glyph
    string during shaping. This API allows for changing that and retaining a glyph,
    such that the situation can be detected by the client and handled accordingly
    (e.g. by using a different font).
    """
def buffer_set_random_state(buffer: buffer_t, state: int) -> None:
    """
    Sets the random state of the buffer. The state changes
    every time a glyph uses randomness (eg. the ``rand``
    OpenType feature). This function together with
    :func:`HarfBuzz.buffer_get_random_state` allow for transferring
    the current random state to a subsequent buffer, to
    get better randomness distribution.

    Defaults to 1 and when buffer contents are cleared.
    A value of 0 disables randomness during shaping.
    """
def buffer_set_replacement_codepoint(buffer: buffer_t, replacement: codepoint_t) -> None:
    """
    Sets the #hb_codepoint_t that replaces invalid entries for a given encoding
    when adding text to ``buffer``.

    Default is :data:`HarfBuzz.BUFFER_REPLACEMENT_CODEPOINT_DEFAULT`.
    """
def buffer_set_script(buffer: buffer_t, script: script_t | int) -> None:
    """
    Sets the script of ``buffer`` to ``script``.

    Script is crucial for choosing the proper shaping behaviour for scripts that
    require it (e.g. Arabic) and the which OpenType features defined in the font
    to be applied.

    You can pass one of the predefined :class:`HarfBuzz.script_t` values, or use
    :func:`HarfBuzz.script_from_string` or :func:`HarfBuzz.script_from_iso15924_tag` to get the
    corresponding script from an ISO 15924 script tag.
    """
def buffer_set_segment_properties(buffer: buffer_t, props: segment_properties_t) -> None:
    """
    Sets the segment properties of the buffer, a shortcut for calling
    :func:`HarfBuzz.buffer_set_direction`, :func:`HarfBuzz.buffer_set_script` and
    :func:`HarfBuzz.buffer_set_language` individually.
    """
def buffer_set_unicode_funcs(buffer: buffer_t, unicode_funcs: unicode_funcs_t) -> None:
    """
    Sets the Unicode-functions structure of a buffer to
    ``unicode_funcs``.
    """
def calloc(nmemb: int, size: int) -> int | None:
    """
    Allocates ``nmemb`` elements of ``size`` bytes each, initialized to zero,
    using the allocator set at compile-time. Typically just calloc().
    """
def color_get_alpha(color: color_t) -> int:
    """
    Fetches the alpha channel of the given ``color``.
    """
def color_get_blue(color: color_t) -> int:
    """
    Fetches the blue channel of the given ``color``.
    """
def color_get_green(color: color_t) -> int:
    """
    Fetches the green channel of the given ``color``.
    """
def color_get_red(color: color_t) -> int:
    """
    Fetches the red channel of the given ``color``.
    """
def color_line_get_color_stops(color_line: color_line_t, start: int) -> tuple[int, list[color_stop_t]]:
    """
    Fetches a list of color stops from the given color line object.

    Note that due to variations being applied, the returned color stops
    may be out of order. It is the callers responsibility to ensure that
    color stops are sorted by their offset before they are used.
    """
def color_line_get_extend(color_line: color_line_t) -> paint_extend_t:
    """
    Fetches the extend mode of the color line object.
    """
def direction_from_string(str: list[int]) -> direction_t:
    """
    Converts a string to an :class:`HarfBuzz.direction_t`.

    Matching is loose and applies only to the first letter. For
    examples, "LTR" and "left-to-right" will both return :data:`HarfBuzz.direction_t.LTR`.

    Unmatched strings will return :data:`HarfBuzz.direction_t.INVALID`.
    """
def direction_to_string(direction: direction_t | int) -> str:
    """
    Converts an :class:`HarfBuzz.direction_t` to a string.
    """
def draw_close_path(dfuncs: draw_funcs_t, draw_data: int | None, st: draw_state_t) -> None:
    """
    Perform a "close-path" draw operation.
    """
def draw_cubic_to(dfuncs: draw_funcs_t, draw_data: int | None, st: draw_state_t, control1_x: float, control1_y: float, control2_x: float, control2_y: float, to_x: float, to_y: float) -> None:
    """
    Perform a "cubic-to" draw operation.
    """
def draw_funcs_create() -> draw_funcs_t:
    """
    Creates a new draw callbacks object.
    """
def draw_funcs_get_empty() -> draw_funcs_t:
    """
    Fetches the singleton empty draw-functions structure.
    """
def draw_funcs_is_immutable(dfuncs: draw_funcs_t) -> bool_t:
    """
    Checks whether ``dfuncs`` is immutable.
    """
def draw_funcs_make_immutable(dfuncs: draw_funcs_t) -> None:
    """
    Makes ``dfuncs`` object immutable.
    """
def draw_funcs_set_close_path_func(dfuncs: draw_funcs_t, func: draw_close_path_func_t) -> None:
    """
    Sets close-path callback to the draw functions object.
    """
def draw_funcs_set_cubic_to_func(dfuncs: draw_funcs_t, func: draw_cubic_to_func_t) -> None:
    """
    Sets cubic-to callback to the draw functions object.
    """
def draw_funcs_set_line_to_func(dfuncs: draw_funcs_t, func: draw_line_to_func_t) -> None:
    """
    Sets line-to callback to the draw functions object.
    """
def draw_funcs_set_move_to_func(dfuncs: draw_funcs_t, func: draw_move_to_func_t) -> None:
    """
    Sets move-to callback to the draw functions object.
    """
def draw_funcs_set_quadratic_to_func(dfuncs: draw_funcs_t, func: draw_quadratic_to_func_t) -> None:
    """
    Sets quadratic-to callback to the draw functions object.
    """
def draw_line_to(dfuncs: draw_funcs_t, draw_data: int | None, st: draw_state_t, to_x: float, to_y: float) -> None:
    """
    Perform a "line-to" draw operation.
    """
def draw_move_to(dfuncs: draw_funcs_t, draw_data: int | None, st: draw_state_t, to_x: float, to_y: float) -> None:
    """
    Perform a "move-to" draw operation.
    """
def draw_quadratic_to(dfuncs: draw_funcs_t, draw_data: int | None, st: draw_state_t, control_x: float, control_y: float, to_x: float, to_y: float) -> None:
    """
    Perform a "quadratic-to" draw operation.
    """
def face_builder_add_table(face: face_t, tag: tag_t, blob: blob_t) -> bool_t:
    """
    Add table for ``tag`` with data provided by ``blob`` to the face.  ``face`` must
    be created using :func:`HarfBuzz.face_builder_create`.
    """
def face_builder_create() -> face_t:
    """
    Creates a :class:`HarfBuzz.face_t` that can be used with :func:`HarfBuzz.face_builder_add_table`.
    After tables are added to the face, it can be compiled to a binary
    font file by calling :func:`HarfBuzz.face_reference_blob`.
    """
def face_builder_sort_tables(face: face_t, tags: list[tag_t]) -> None:
    """
    Set the ordering of tables for serialization. Any tables not
    specified in the tags list will be ordered after the tables in
    tags, ordered by the default sort ordering.
    """
def face_collect_nominal_glyph_mapping(face: face_t) -> tuple[map_t, set_t]:
    """
    Collects the mapping from Unicode characters to nominal glyphs of the ``face``,
    and optionally all of the Unicode characters covered by ``face``.
    """
def face_collect_unicodes(face: face_t) -> set_t:
    """
    Collects all of the Unicode characters covered by ``face`` and adds
    them to the :class:`HarfBuzz.set_t` set ``out``.
    """
def face_collect_variation_selectors(face: face_t) -> set_t:
    """
    Collects all Unicode "Variation Selector" characters covered by ``face`` and adds
    them to the :class:`HarfBuzz.set_t` set ``out``.
    """
def face_collect_variation_unicodes(face: face_t, variation_selector: codepoint_t) -> set_t:
    """
    Collects all Unicode characters for ``variation_selector`` covered by ``face`` and adds
    them to the :class:`HarfBuzz.set_t` set ``out``.
    """
def face_count(blob: blob_t) -> int:
    """
    Fetches the number of faces in a blob.
    """
def face_create(blob: blob_t, index: int) -> face_t:
    """
    Constructs a new face object from the specified blob and
    a face index into that blob.

    The face index is used for blobs of file formats such as TTC and
    DFont that can contain more than one face.  Face indices within
    such collections are zero-based.

    <note>Note: If the blob font format is not a collection, ``index``
    is ignored.  Otherwise, only the lower 16-bits of ``index`` are used.
    The unmodified ``index`` can be accessed via :func:`HarfBuzz.face_get_index`.</note>

    <note>Note: The high 16-bits of ``index``, if non-zero, are used by
    :func:`HarfBuzz.font_create` to load named-instances in variable fonts.  See
    :func:`HarfBuzz.font_create` for details.</note>
    """
def face_create_for_tables(reference_table_func: reference_table_func_t) -> face_t:
    """
    Variant of :func:`HarfBuzz.face_create`, built for those cases where it is more
    convenient to provide data for individual tables instead of the whole font
    data. With the caveat that :func:`HarfBuzz.face_get_table_tags` would not work
    with faces created this way. You can address that by calling the
    :func:`HarfBuzz.face_set_get_table_tags_func` function and setting the appropriate callback.

    Creates a new face object from the specified ``user_data`` and ``reference_table_func``,
    with the ``destroy`` callback.
    """
def face_create_from_file_or_fail(file_name: str, index: int) -> face_t:
    """
    A thin wrapper around :func:`HarfBuzz.blob_create_from_file_or_fail`
    followed by :func:`HarfBuzz.face_create_or_fail`.
    """
def face_create_from_file_or_fail_using(file_name: str, index: int, loader_name: str | None = ...) -> face_t:
    """
    A thin wrapper around the face loader functions registered with HarfBuzz.
    If ``loader_name`` is ``None`` or the empty string, the first available loader
    is used.

    For example, the FreeType ("ft") loader might be able to load
    WOFF and WOFF2 files if FreeType is built with those features,
    whereas the OpenType ("ot") loader will not.
    """
def face_create_or_fail(blob: blob_t, index: int) -> face_t:
    """
    Like :func:`HarfBuzz.face_create`, but returns ``None`` if the blob data
    contains no usable font face at the specified index.
    """
def face_create_or_fail_using(blob: blob_t, index: int, loader_name: str | None = ...) -> face_t:
    """
    A thin wrapper around the face loader functions registered with HarfBuzz.
    If ``loader_name`` is ``None`` or the empty string, the first available loader
    is used.

    For example, the FreeType ("ft") loader might be able to load
    WOFF and WOFF2 files if FreeType is built with those features,
    whereas the OpenType ("ot") loader will not.
    """
def face_get_empty() -> face_t:
    """
    Fetches the singleton empty face object.
    """
def face_get_glyph_count(face: face_t) -> int:
    """
    Fetches the glyph-count value of the specified face object.
    """
def face_get_index(face: face_t) -> int:
    """
    Fetches the face-index corresponding to the given face.

    <note>Note: face indices within a collection are zero-based.</note>
    """
def face_get_table_tags(face: face_t, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches a list of all table tags for a face, if possible. The list returned will
    begin at the offset provided
    """
def face_get_upem(face: face_t) -> int:
    """
    Fetches the units-per-em (UPEM) value of the specified face object.

    Typical UPEM values for fonts are 1000, or 2048, but any value
    in between 16 and 16,384 is allowed for OpenType fonts.
    """
def face_is_immutable(face: face_t) -> bool_t:
    """
    Tests whether the given face object is immutable.
    """
def face_list_loaders() -> list[str]:
    """
    Retrieves the list of face loaders supported by HarfBuzz.
    """
def face_make_immutable(face: face_t) -> None:
    """
    Makes the given face object immutable.
    """
def face_reference_blob(face: face_t) -> blob_t:
    """
    Fetches a pointer to the binary blob that contains the specified face.
    If referencing the face data is not possible, this function creates a blob
    out of individual table blobs if :func:`HarfBuzz.face_get_table_tags` works with this
    face, otherwise it returns an empty blob.
    """
def face_reference_table(face: face_t, tag: tag_t) -> blob_t:
    """
    Fetches a reference to the specified table within
    the specified face. Returns an empty blob if referencing table data is not
    possible.
    """
def face_set_get_table_tags_func(face: face_t, func: get_table_tags_func_t) -> None:
    """
    Sets the table-tag-fetching function for the specified face object.
    """
def face_set_glyph_count(face: face_t, glyph_count: int) -> None:
    """
    Sets the glyph count for a face object to the specified value.

    This API is used in rare circumstances.
    """
def face_set_index(face: face_t, index: int) -> None:
    """
    Assigns the specified face-index to ``face``. Fails if the
    face is immutable.

    <note>Note: changing the index has no effect on the face itself
    This only changes the value returned by :func:`HarfBuzz.face_get_index`.</note>
    """
def face_set_upem(face: face_t, upem: int) -> None:
    """
    Sets the units-per-em (upem) for a face object to the specified value.

    This API is used in rare circumstances.
    """
def feature_from_string(str: list[int]) -> tuple[bool_t, feature_t]:
    """
    Parses a string into a :class:`HarfBuzz.feature_t`.

    The format for specifying feature strings follows. All valid CSS
    font-feature-settings values other than 'normal' and the global values are
    also accepted, though not documented below. CSS string escapes are not
    supported.

    The range indices refer to the positions between Unicode characters. The
    position before the first character is always 0.

    The format is Python-esque.  Here is how it all works:

    <informaltable pgwide='1' align='left' frame='none'>
    <tgroup cols='5'>
    <thead>
    <row><entry>Syntax</entry>    <entry>Value</entry> <entry>Start</entry> <entry>End</entry></row>
    </thead>
    <tbody>
    <row><entry>Setting value:</entry></row>
    <row><entry>kern</entry>      <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
    <row><entry>+kern</entry>     <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
    <row><entry>-kern</entry>     <entry>0</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature off</entry></row>
    <row><entry>kern=0</entry>    <entry>0</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature off</entry></row>
    <row><entry>kern=1</entry>    <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
    <row><entry>aalt=2</entry>    <entry>2</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Choose 2nd alternate</entry></row>
    <row><entry>Setting index:</entry></row>
    <row><entry>kern[]</entry>    <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
    <row><entry>kern[:]</entry>   <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
    <row><entry>kern[5:]</entry>  <entry>1</entry>     <entry>5</entry>      <entry>∞</entry>   <entry>Turn feature on, partial</entry></row>
    <row><entry>kern[:5]</entry>  <entry>1</entry>     <entry>0</entry>      <entry>5</entry>   <entry>Turn feature on, partial</entry></row>
    <row><entry>kern[3:5]</entry> <entry>1</entry>     <entry>3</entry>      <entry>5</entry>   <entry>Turn feature on, range</entry></row>
    <row><entry>kern[3]</entry>   <entry>1</entry>     <entry>3</entry>      <entry>3+1</entry> <entry>Turn feature on, single char</entry></row>
    <row><entry>Mixing it all:</entry></row>
    <row><entry>aalt[3:5]=2</entry> <entry>2</entry>   <entry>3</entry>      <entry>5</entry>   <entry>Turn 2nd alternate on for range</entry></row>
    </tbody>
    </tgroup>
    </informaltable>
    """
def feature_to_string(feature: feature_t) -> list[str]:
    """
    Converts a :class:`HarfBuzz.feature_t` into a ``None``-terminated string in the format
    understood by :func:`HarfBuzz.feature_from_string`. The client in responsible for
    allocating big enough size for ``buf``, 128 bytes is more than enough.

    Note that the feature value will be omitted if it is '1', but the
    string won't include any whitespace.
    """
def font_add_glyph_origin_for_direction(font: font_t, glyph: codepoint_t, direction: direction_t | int, x: position_t, y: position_t) -> tuple[position_t, position_t]:
    """
    Adds the origin coordinates to an (X,Y) point coordinate, in
    the specified glyph ID in the specified font.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_changed(font: font_t) -> None:
    """
    Notifies the ``font`` that underlying font data has changed.
    This has the effect of increasing the serial as returned
    by :func:`HarfBuzz.font_get_serial`, which invalidates internal caches.
    """
def font_create(face: face_t) -> font_t:
    """
    Constructs a new font object from the specified face.

    <note>Note: If ``face``'s index value (as passed to :func:`HarfBuzz.face_create`
    has non-zero top 16-bits, those bits minus one are passed to
    :func:`HarfBuzz.font_set_var_named_instance`, effectively loading a named-instance
    of a variable font, instead of the default-instance.  This allows
    specifying which named-instance to load by default when creating the
    face.</note>
    """
def font_create_sub_font(parent: font_t) -> font_t:
    """
    Constructs a sub-font font object from the specified ``parent`` font,
    replicating the parent's properties.
    """
def font_draw_glyph(font: font_t, glyph: codepoint_t, dfuncs: draw_funcs_t, draw_data: int | None = ...) -> None:
    """
    Draws the outline that corresponds to a glyph in the specified ``font``.

    This is an older name for :func:`HarfBuzz.font_draw_glyph_or_fail`, with no
    return value.

    The outline is returned by way of calls to the callbacks of the ``dfuncs``
    objects, with ``draw_data`` passed to them.
    """
def font_draw_glyph_or_fail(font: font_t, glyph: codepoint_t, dfuncs: draw_funcs_t, draw_data: int | None = ...) -> bool_t:
    """
    Draws the outline that corresponds to a glyph in the specified ``font``.

    This is a newer name for :func:`HarfBuzz.font_draw_glyph`, that returns ``false``
    if the font has no outlines for the glyph.

    The outline is returned by way of calls to the callbacks of the ``dfuncs``
    objects, with ``draw_data`` passed to them.
    """
def font_funcs_create() -> font_funcs_t:
    """
    Creates a new :class:`HarfBuzz.font_funcs_t` structure of font functions.
    """
def font_funcs_get_empty() -> font_funcs_t:
    """
    Fetches an empty font-functions structure.
    """
def font_funcs_is_immutable(ffuncs: font_funcs_t) -> bool_t:
    """
    Tests whether a font-functions structure is immutable.
    """
def font_funcs_make_immutable(ffuncs: font_funcs_t) -> None:
    """
    Makes a font-functions structure immutable.
    """
@deprecated("Deprecated since 11.2.0.")
def font_funcs_set_draw_glyph_func(ffuncs: font_funcs_t, func: font_draw_glyph_func_t) -> None:
    """
    Sets the implementation function for #hb_font_draw_glyph_func_t.
    """
def font_funcs_set_draw_glyph_or_fail_func(ffuncs: font_funcs_t, func: font_draw_glyph_or_fail_func_t) -> None:
    """
    Sets the implementation function for #hb_font_draw_glyph_or_fail_func_t.
    """
def font_funcs_set_font_h_extents_func(ffuncs: font_funcs_t, func: font_get_font_h_extents_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_font_h_extents_func_t.
    """
def font_funcs_set_font_v_extents_func(ffuncs: font_funcs_t, func: font_get_font_v_extents_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_font_v_extents_func_t.
    """
def font_funcs_set_glyph_contour_point_func(ffuncs: font_funcs_t, func: font_get_glyph_contour_point_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_contour_point_func_t.
    """
def font_funcs_set_glyph_extents_func(ffuncs: font_funcs_t, func: font_get_glyph_extents_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_extents_func_t.
    """
def font_funcs_set_glyph_from_name_func(ffuncs: font_funcs_t, func: font_get_glyph_from_name_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_from_name_func_t.
    """
@deprecated("Deprecated since 1.2.3.")
def font_funcs_set_glyph_func(ffuncs: font_funcs_t, func: font_get_glyph_func_t) -> None:
    """
    Deprecated.  Use :func:`HarfBuzz.font_funcs_set_nominal_glyph_func` and
    :func:`HarfBuzz.font_funcs_set_variation_glyph_func` instead.
    """
def font_funcs_set_glyph_h_advance_func(ffuncs: font_funcs_t, func: font_get_glyph_h_advance_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_h_advance_func_t.
    """
def font_funcs_set_glyph_h_advances_func(ffuncs: font_funcs_t, func: font_get_glyph_h_advances_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_h_advances_func_t.
    """
def font_funcs_set_glyph_h_kerning_func(ffuncs: font_funcs_t, func: font_get_glyph_h_kerning_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_h_kerning_func_t.
    """
def font_funcs_set_glyph_h_origin_func(ffuncs: font_funcs_t, func: font_get_glyph_h_origin_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_h_origin_func_t.
    """
def font_funcs_set_glyph_h_origins_func(ffuncs: font_funcs_t, func: font_get_glyph_h_origins_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_h_origins_func_t.
    """
def font_funcs_set_glyph_name_func(ffuncs: font_funcs_t, func: font_get_glyph_name_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_name_func_t.
    """
@deprecated("Deprecated since 7.0.0.")
def font_funcs_set_glyph_shape_func(ffuncs: font_funcs_t, func: font_get_glyph_shape_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_shape_func_t,
    which is the same as #hb_font_draw_glyph_func_t.
    """
def font_funcs_set_glyph_v_advance_func(ffuncs: font_funcs_t, func: font_get_glyph_v_advance_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_v_advance_func_t.
    """
def font_funcs_set_glyph_v_advances_func(ffuncs: font_funcs_t, func: font_get_glyph_v_advances_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_v_advances_func_t.
    """
@deprecated("Deprecated since 2.0.0.")
def font_funcs_set_glyph_v_kerning_func(ffuncs: font_funcs_t, func: font_get_glyph_v_kerning_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_v_kerning_func_t.
    """
def font_funcs_set_glyph_v_origin_func(ffuncs: font_funcs_t, func: font_get_glyph_v_origin_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_v_origin_func_t.
    """
def font_funcs_set_glyph_v_origins_func(ffuncs: font_funcs_t, func: font_get_glyph_v_origins_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_glyph_v_origins_func_t.
    """
def font_funcs_set_nominal_glyph_func(ffuncs: font_funcs_t, func: font_get_nominal_glyph_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_nominal_glyph_func_t.
    """
def font_funcs_set_nominal_glyphs_func(ffuncs: font_funcs_t, func: font_get_nominal_glyphs_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_nominal_glyphs_func_t.
    """
@deprecated("Deprecated since 11.2.0.")
def font_funcs_set_paint_glyph_func(ffuncs: font_funcs_t, func: font_paint_glyph_func_t) -> None:
    """
    Sets the implementation function for #hb_font_paint_glyph_func_t.
    """
def font_funcs_set_paint_glyph_or_fail_func(ffuncs: font_funcs_t, func: font_paint_glyph_or_fail_func_t) -> None:
    """
    Sets the implementation function for #hb_font_paint_glyph_or_fail_func_t.
    """
def font_funcs_set_variation_glyph_func(ffuncs: font_funcs_t, func: font_get_variation_glyph_func_t) -> None:
    """
    Sets the implementation function for #hb_font_get_variation_glyph_func_t.
    """
def font_get_empty() -> font_t:
    """
    Fetches the empty font object.
    """
def font_get_extents_for_direction(font: font_t, direction: direction_t | int) -> font_extents_t:
    """
    Fetches the extents for a font in a text segment of the
    specified direction.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_face(font: font_t) -> face_t:
    """
    Fetches the face associated with the specified font object.
    """
def font_get_glyph(font: font_t, unicode: codepoint_t, variation_selector: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the glyph ID for a Unicode code point in the specified
    font, with an optional variation selector.

    If ``variation_selector`` is 0, calls :func:`HarfBuzz.font_get_nominal_glyph`;
    otherwise calls :func:`HarfBuzz.font_get_variation_glyph`.
    """
def font_get_glyph_advance_for_direction(font: font_t, glyph: codepoint_t, direction: direction_t | int) -> tuple[position_t, position_t]:
    """
    Fetches the advance for a glyph ID from the specified font,
    in a text segment of the specified direction.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_glyph_advances_for_direction(font: font_t, direction: direction_t | int, count: int, first_glyph: codepoint_t, glyph_stride: int) -> tuple[position_t, int]:
    """
    Fetches the advances for a sequence of glyph IDs in the specified
    font, in a text segment of the specified direction.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_glyph_contour_point(font: font_t, glyph: codepoint_t, point_index: int) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (x,y) coordinates of a specified contour-point index
    in the specified glyph, within the specified font.
    """
def font_get_glyph_contour_point_for_origin(font: font_t, glyph: codepoint_t, point_index: int, direction: direction_t | int) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of a specified contour-point index
    in the specified glyph ID in the specified font, with respect
    to the origin in a text segment in the specified direction.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_glyph_extents(font: font_t, glyph: codepoint_t) -> tuple[bool_t, glyph_extents_t]:
    """
    Fetches the :class:`HarfBuzz.glyph_extents_t` data for a glyph ID
    in the specified font.
    """
def font_get_glyph_extents_for_origin(font: font_t, glyph: codepoint_t, direction: direction_t | int) -> tuple[bool_t, glyph_extents_t]:
    """
    Fetches the :class:`HarfBuzz.glyph_extents_t` data for a glyph ID
    in the specified font, with respect to the origin in
    a text segment in the specified direction.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_glyph_from_name(font: font_t, name: list[str]) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the glyph ID that corresponds to a name string in the specified ``font``.

    <note>Note: ``len`` == -1 means the name string is null-terminated.</note>
    """
def font_get_glyph_h_advance(font: font_t, glyph: codepoint_t) -> position_t:
    """
    Fetches the advance for a glyph ID in the specified font,
    for horizontal text segments.
    """
def font_get_glyph_h_advances(font: font_t, count: int, first_glyph: codepoint_t, glyph_stride: int, advance_stride: int) -> position_t:
    """
    Fetches the advances for a sequence of glyph IDs in the specified
    font, for horizontal text segments.
    """
def font_get_glyph_h_kerning(font: font_t, left_glyph: codepoint_t, right_glyph: codepoint_t) -> position_t:
    """
    Fetches the kerning-adjustment value for a glyph-pair in
    the specified font, for horizontal text segments.

    <note>It handles legacy kerning only (as returned by the corresponding
    :class:`HarfBuzz.font_funcs_t` function).</note>
    """
def font_get_glyph_h_origin(font: font_t, glyph: codepoint_t) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of the origin for a glyph ID
    in the specified font, for horizontal text segments.
    """
def font_get_glyph_h_origins(font: font_t, count: int, first_glyph: codepoint_t, glyph_stride: int, x_stride: int, y_stride: int) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of the origin for requested glyph IDs
    in the specified font, for horizontal text segments.
    """
def font_get_glyph_kerning_for_direction(font: font_t, first_glyph: codepoint_t, second_glyph: codepoint_t, direction: direction_t | int) -> tuple[position_t, position_t]:
    """
    Fetches the kerning-adjustment value for a glyph-pair in the specified font.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def font_get_glyph_name(font: font_t, glyph: codepoint_t) -> tuple[bool_t, list[str]]:
    """
    Fetches the glyph-name string for a glyph ID in the specified ``font``.

    According to the OpenType specification, glyph names are limited to 63
    characters and can only contain (a subset of) ASCII.
    """
def font_get_glyph_origin_for_direction(font: font_t, glyph: codepoint_t, direction: direction_t | int) -> tuple[position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of the origin for a glyph in
    the specified font.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
@deprecated("Deprecated since 7.0.0.")
def font_get_glyph_shape(font: font_t, glyph: codepoint_t, dfuncs: draw_funcs_t, draw_data: int | None = ...) -> None:
    """
    Fetches the glyph shape that corresponds to a glyph in the specified ``font``.
    The shape is returned by way of calls to the callbacks of the ``dfuncs``
    objects, with ``draw_data`` passed to them.
    """
def font_get_glyph_v_advance(font: font_t, glyph: codepoint_t) -> position_t:
    """
    Fetches the advance for a glyph ID in the specified font,
    for vertical text segments.
    """
def font_get_glyph_v_advances(font: font_t, count: int, first_glyph: codepoint_t, glyph_stride: int) -> tuple[position_t, int]:
    """
    Fetches the advances for a sequence of glyph IDs in the specified
    font, for vertical text segments.
    """
@deprecated("Deprecated since 2.0.0.")
def font_get_glyph_v_kerning(font: font_t, top_glyph: codepoint_t, bottom_glyph: codepoint_t) -> position_t:
    """
    Fetches the kerning-adjustment value for a glyph-pair in
    the specified font, for vertical text segments.

    <note>It handles legacy kerning only (as returned by the corresponding
    :class:`HarfBuzz.font_funcs_t` function).</note>
    """
def font_get_glyph_v_origin(font: font_t, glyph: codepoint_t) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of the origin for a glyph ID
    in the specified font, for vertical text segments.
    """
def font_get_glyph_v_origins(font: font_t, count: int, first_glyph: codepoint_t, glyph_stride: int, x_stride: int, y_stride: int) -> tuple[bool_t, position_t, position_t]:
    """
    Fetches the (X,Y) coordinates of the origin for requested glyph IDs
    in the specified font, for vertical text segments.
    """
def font_get_h_extents(font: font_t) -> tuple[bool_t, font_extents_t]:
    """
    Fetches the extents for a specified font, for horizontal
    text segments.
    """
def font_get_nominal_glyph(font: font_t, unicode: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the nominal glyph ID for a Unicode code point in the
    specified font.

    This version of the function should not be used to fetch glyph IDs
    for code points modified by variation selectors. For variation-selector
    support, user :func:`HarfBuzz.font_get_variation_glyph` or use :func:`HarfBuzz.font_get_glyph`.
    """
def font_get_nominal_glyphs(font: font_t, count: int, first_unicode: codepoint_t, unicode_stride: int, glyph_stride: int) -> tuple[int, codepoint_t]:
    """
    Fetches the nominal glyph IDs for a sequence of Unicode code points. Glyph
    IDs must be returned in a #hb_codepoint_t output parameter. Stops at the
    first unsupported glyph ID.
    """
def font_get_parent(font: font_t) -> font_t:
    """
    Fetches the parent font of ``font``.
    """
def font_get_ppem(font: font_t) -> tuple[int, int]:
    """
    Fetches the horizontal and vertical points-per-em (ppem) of a font.
    """
def font_get_ptem(font: font_t) -> float:
    """
    Fetches the "point size" of a font. Used in CoreText to
    implement optical sizing.
    """
def font_get_scale(font: font_t) -> tuple[int, int]:
    """
    Fetches the horizontal and vertical scale of a font.
    """
def font_get_serial(font: font_t) -> int:
    """
    Returns the internal serial number of the font. The serial
    number is increased every time a setting on the font is
    changed, using a setter function.
    """
def font_get_synthetic_bold(font: font_t) -> tuple[float, float, bool_t]:
    """
    Fetches the "synthetic boldness" parameters of a font.
    """
def font_get_synthetic_slant(font: font_t) -> float:
    """
    Fetches the "synthetic slant" of a font.
    """
def font_get_v_extents(font: font_t) -> tuple[bool_t, font_extents_t]:
    """
    Fetches the extents for a specified font, for vertical
    text segments.
    """
def font_get_var_coords_design(font: font_t) -> tuple[float, int]:
    """
    Fetches the list of variation coordinates (in design-space units) currently
    set on a font.

    <note>Note that if no variation coordinates are set, this function may
    return ``None``.</note>

    <note>If variations have been set on the font using normalized coordinates
    (i.e. via :func:`HarfBuzz.font_set_var_coords_normalized`), the design coordinates will
    have NaN (Not a Number) values.</note>

    Return value is valid as long as variation coordinates of the font
    are not modified.
    """
def font_get_var_coords_normalized(font: font_t) -> tuple[int, int]:
    """
    Fetches the list of normalized variation coordinates currently
    set on a font.

    <note>Note that if no variation coordinates are set, this function may
    return ``None``.</note>

    Return value is valid as long as variation coordinates of the font
    are not modified.
    """
def font_get_var_named_instance(font: font_t) -> int:
    """
    Returns the currently-set named-instance index of the font.
    """
def font_get_variation_glyph(font: font_t, unicode: codepoint_t, variation_selector: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the glyph ID for a Unicode code point when followed by
    by the specified variation-selector code point, in the specified
    font.
    """
def font_glyph_from_string(font: font_t, s: list[int]) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the glyph ID from ``font`` that matches the specified string.
    Strings of the format ``gidDDD`` or ``uniUUUU`` are parsed automatically.

    <note>Note: ``len`` == -1 means the string is null-terminated.</note>
    """
def font_glyph_to_string(font: font_t, glyph: codepoint_t) -> list[str]:
    """
    Fetches the name of the specified glyph ID in ``font`` and returns
    it in string ``s``.

    If the glyph ID has no name in ``font``, a string of the form ``gidDDD`` is
    generated, with ``DDD`` being the glyph ID.

    According to the OpenType specification, glyph names are limited to 63
    characters and can only contain (a subset of) ASCII.
    """
def font_is_immutable(font: font_t) -> bool_t:
    """
    Tests whether a font object is immutable.
    """
def font_is_synthetic(font: font_t) -> bool_t:
    """
    Tests whether a font is synthetic. A synthetic font is one
    that has either synthetic slant or synthetic bold set on it.
    """
def font_list_funcs() -> list[str]:
    """
    Retrieves the list of font functions supported by HarfBuzz.
    """
def font_make_immutable(font: font_t) -> None:
    """
    Makes ``font`` immutable.
    """
def font_paint_glyph(font: font_t, glyph: codepoint_t, pfuncs: paint_funcs_t, paint_data: int | None, palette_index: int, foreground: color_t) -> None:
    """
    Paints the glyph. This function is similar to
    :func:`HarfBuzz.font_paint_glyph_or_fail`, but if painting a color glyph
    failed, it will fall back to painting an outline monochrome
    glyph.

    The painting instructions are returned by way of calls to
    the callbacks of the ``funcs`` object, with ``paint_data`` passed
    to them.

    If the font has color palettes (see :func:`HarfBuzz.ot_color_has_palettes`),
    then ``palette_index`` selects the palette to use. If the font only
    has one palette, this will be 0.
    """
def font_paint_glyph_or_fail(font: font_t, glyph: codepoint_t, pfuncs: paint_funcs_t, paint_data: int | None, palette_index: int, foreground: color_t) -> bool_t:
    """
    Paints a color glyph.

    This function is similar to, but lower-level than,
    :func:`HarfBuzz.font_paint_glyph`. It is suitable for clients that
    need more control.  If there are no color glyphs available,
    it will return ``false``. The client can then fall back to
    :func:`HarfBuzz.font_draw_glyph_or_fail` for the monochrome outline glyph.

    The painting instructions are returned by way of calls to
    the callbacks of the ``funcs`` object, with ``paint_data`` passed
    to them.

    If the font has color palettes (see :func:`HarfBuzz.ot_color_has_palettes`),
    then ``palette_index`` selects the palette to use. If the font only
    has one palette, this will be 0.
    """
def font_set_face(font: font_t, face: face_t) -> None:
    """
    Sets ``face`` as the font-face value of ``font``.
    """
def font_set_funcs_using(font: font_t, name: str) -> bool_t:
    """
    Sets the font-functions structure to use for a font, based on the
    specified name.

    If ``name`` is ``None`` or the empty string, the default (first) functioning font-functions
    are used.  This default can be changed by setting the ``HB_FONT_FUNCS`` environment
    variable to the name of the desired font-functions.
    """
def font_set_parent(font: font_t, parent: font_t) -> None:
    """
    Sets the parent font of ``font``.
    """
def font_set_ppem(font: font_t, x_ppem: int, y_ppem: int) -> None:
    """
    Sets the horizontal and vertical pixels-per-em (PPEM) of a font.

    These values are used for pixel-size-specific adjustment to
    shaping and draw results, though for the most part they are
    unused and can be left unset.
    """
def font_set_ptem(font: font_t, ptem: float) -> None:
    """
    Sets the "point size" of a font. Set to zero to unset.
    Used in CoreText to implement optical sizing.

    <note>Note: There are 72 points in an inch.</note>
    """
def font_set_scale(font: font_t, x_scale: int, y_scale: int) -> None:
    """
    Sets the horizontal and vertical scale of a font.

    The font scale is a number related to, but not the same as,
    font size. Typically the client establishes a scale factor
    to be used between the two. For example, 64, or 256, which
    would be the fractional-precision part of the font scale.
    This is necessary because #hb_position_t values are integer
    types and you need to leave room for fractional values
    in there.

    For example, to set the font size to 20, with 64
    levels of fractional precision you would call
    :func:`HarfBuzz.font_set_scale` ``(font, 20 * 64, 20 * 64)``.

    In the example above, even what font size 20 means is up to
    you. It might be 20 pixels, or 20 points, or 20 millimeters.
    HarfBuzz does not care about that.  You can set the point
    size of the font using :func:`HarfBuzz.font_set_ptem`, and the pixel
    size using :func:`HarfBuzz.font_set_ppem`.

    The choice of scale is yours but needs to be consistent between
    what you set here, and what you expect out of #hb_position_t
    as well has draw / paint API output values.

    Fonts default to a scale equal to the UPEM value of their face.
    A font with this setting is sometimes called an "unscaled" font.
    """
def font_set_synthetic_bold(font: font_t, x_embolden: float, y_embolden: float, in_place: bool_t) -> None:
    """
    Sets the "synthetic boldness" of a font.

    Positive values for ``x_embolden`` / ``y_embolden`` make a font
    bolder, negative values thinner. Typical values are in the
    0.01 to 0.05 range. The default value is zero.

    Synthetic boldness is applied by offsetting the contour
    points of the glyph shape.

    Synthetic boldness is applied when rendering a glyph via
    :func:`HarfBuzz.font_draw_glyph_or_fail`.

    If ``in_place`` is ``false``, then glyph advance-widths are also
    adjusted, otherwise they are not.  The in-place mode is
    useful for simulating `font grading <https://fonts.google.com/knowledge/glossary/grade>`_.
    """
def font_set_synthetic_slant(font: font_t, slant: float) -> None:
    """
    Sets the "synthetic slant" of a font.  By default is zero.
    Synthetic slant is the graphical skew applied to the font
    at rendering time.

    HarfBuzz needs to know this value to adjust shaping results,
    metrics, and style values to match the slanted rendering.

    <note>Note: The glyph shape fetched via the :func:`HarfBuzz.font_draw_glyph_or_fail`
    function is slanted to reflect this value as well.</note>

    <note>Note: The slant value is a ratio.  For example, a
    20% slant would be represented as a 0.2 value.</note>
    """
def font_set_var_coords_design(font: font_t, coords: list[float]) -> None:
    """
    Applies a list of variation coordinates (in design-space units)
    to a font.

    Note that this overrides all existing variations set on ``font``.
    Axes not included in ``coords`` will be effectively set to their
    default values.
    """
def font_set_var_coords_normalized(font: font_t, coords: list[int]) -> None:
    """
    Applies a list of variation coordinates (in normalized units)
    to a font.

    Note that this overrides all existing variations set on ``font``.
    Axes not included in ``coords`` will be effectively set to their
    default values.

    <note>Note: Coordinates should be normalized to 2.14.</note>
    """
def font_set_var_named_instance(font: font_t, instance_index: int) -> None:
    """
    Sets design coords of a font from a named-instance index.
    """
def font_set_variation(font: font_t, tag: tag_t, value: float) -> None:
    """
    Change the value of one variation axis on the font.

    Note: This function is expensive to be called repeatedly.
      If you want to set multiple variation axes at the same time,
      use :func:`HarfBuzz.font_set_variations` instead.
    """
def font_set_variations(font: font_t, variations: list[variation_t]) -> None:
    """
    Applies a list of font-variation settings to a font.

    Note that this overrides all existing variations set on ``font``.
    Axes not included in ``variations`` will be effectively set to their
    default values.
    """
def font_subtract_glyph_origin_for_direction(font: font_t, glyph: codepoint_t, direction: direction_t | int, x: position_t, y: position_t) -> tuple[position_t, position_t]:
    """
    Subtracts the origin coordinates from an (X,Y) point coordinate,
    in the specified glyph ID in the specified font.

    Calls the appropriate direction-specific variant (horizontal
    or vertical) depending on the value of ``direction``.
    """
def free(ptr: int | None = ...) -> None:
    """
    Frees the memory pointed to by ``ptr``, using the allocator set at
    compile-time. Typically just free().
    """
def ft_face_create_cached(ft_face: freetype2.Face) -> face_t:
    """
    Creates an :class:`HarfBuzz.face_t` face object from the specified FT_Face.

    Note that this is using the FT_Face object just to get at the underlying
    font data, and fonts created from the returned :class:`HarfBuzz.face_t` will use the native
    HarfBuzz font implementation, unless you call :func:`HarfBuzz.ft_font_set_funcs` on them.

    This variant of the function caches the newly created :class:`HarfBuzz.face_t`
    face object, using the ``generic`` pointer of ``ft_face``. Subsequent function
    calls that are passed the same ``ft_face`` parameter will have the same
    :class:`HarfBuzz.face_t` returned to them, and that :class:`HarfBuzz.face_t` will be correctly
    reference counted.

    However, client programs are still responsible for destroying
    ``ft_face`` after the last :class:`HarfBuzz.face_t` face object has been destroyed.
    """
def ft_face_create_from_blob_or_fail(blob: blob_t, index: int) -> face_t:
    """
    Creates an :class:`HarfBuzz.face_t` face object from the specified
    font blob and face index.

    This is similar in functionality to hb_face_create_from_blob_or_fail(),
    but uses the FreeType library for loading the font blob. This can
    be useful, for example, to load WOFF and WOFF2 font data.
    """
def ft_face_create_from_file_or_fail(file_name: str, index: int) -> face_t:
    """
    Creates an :class:`HarfBuzz.face_t` face object from the specified
    font file and face index.

    This is similar in functionality to :func:`HarfBuzz.face_create_from_file_or_fail`,
    but uses the FreeType library for loading the font file. This can
    be useful, for example, to load WOFF and WOFF2 font data.
    """
def ft_face_create_referenced(ft_face: freetype2.Face) -> face_t:
    """
    Creates an :class:`HarfBuzz.face_t` face object from the specified FT_Face.

    Note that this is using the FT_Face object just to get at the underlying
    font data, and fonts created from the returned :class:`HarfBuzz.face_t` will use the native
    HarfBuzz font implementation, unless you call :func:`HarfBuzz.ft_font_set_funcs` on them.

    This is the preferred variant of the hb_ft_face_create*
    function family, because it calls FT_Reference_Face() on ``ft_face``,
    ensuring that ``ft_face`` remains alive as long as the resulting
    :class:`HarfBuzz.face_t` face object remains alive. Also calls FT_Done_Face()
    when the :class:`HarfBuzz.face_t` face object is destroyed.

    Use this version unless you know you have good reasons not to.
    """
def ft_font_changed(font: font_t) -> None:
    """
    Refreshes the state of ``font`` when the underlying FT_Face has changed.
    This function should be called after changing the size or
    variation-axis settings on the FT_Face.
    """
def ft_font_create_referenced(ft_face: freetype2.Face) -> font_t:
    """
    Creates an :class:`HarfBuzz.font_t` font object from the specified FT_Face.

    <note>Note: You must set the face size on ``ft_face`` before calling
    :func:`HarfBuzz.ft_font_create_referenced` on it. HarfBuzz assumes size is always set
    and will access ``size`` member of FT_Face unconditionally.</note>

    This is the preferred variant of the hb_ft_font_create*
    function family, because it calls FT_Reference_Face() on ``ft_face``,
    ensuring that ``ft_face`` remains alive as long as the resulting
    :class:`HarfBuzz.font_t` font object remains alive.

    Use this version unless you know you have good reasons not to.
    """
def ft_font_get_load_flags(font: font_t) -> int:
    """
    Fetches the FT_Load_Glyph load flags of the specified :class:`HarfBuzz.font_t`.

    For more information, see
    <https://freetype.org/freetype2/docs/reference/ft2-glyph_retrieval.html#ft_load_xxx>

    This function works with :class:`HarfBuzz.font_t` objects created by
    hb_ft_font_create() or :func:`HarfBuzz.ft_font_create_referenced`.
    """
def ft_font_set_funcs(font: font_t) -> None:
    """
    Configures the font-functions structure of the specified
    :class:`HarfBuzz.font_t` font object to use FreeType font functions.

    In particular, you can use this function to configure an
    existing :class:`HarfBuzz.face_t` face object for use with FreeType font
    functions even if that :class:`HarfBuzz.face_t` face object was initially
    created with :func:`HarfBuzz.face_create`, and therefore was not
    initially configured to use FreeType font functions.

    An :class:`HarfBuzz.font_t` object created with hb_ft_font_create()
    is preconfigured for FreeType font functions and does not
    require this function to be used.

    Note that if you modify the underlying :class:`HarfBuzz.font_t` after
    calling this function, you need to call :func:`HarfBuzz.ft_hb_font_changed`
    to update the underlying FT_Face.

    <note>Note: Internally, this function creates an FT_Face.
    </note>
    """
def ft_font_set_load_flags(font: font_t, load_flags: int) -> None:
    """
    Sets the FT_Load_Glyph load flags for the specified :class:`HarfBuzz.font_t`.

    For more information, see
    <https://freetype.org/freetype2/docs/reference/ft2-glyph_retrieval.html#ft_load_xxx>

    This function works with :class:`HarfBuzz.font_t` objects created by
    hb_ft_font_create() or :func:`HarfBuzz.ft_font_create_referenced`.
    """
def ft_hb_font_changed(font: font_t) -> bool_t:
    """
    Refreshes the state of the underlying FT_Face of ``font`` when the :class:`HarfBuzz.font_t`
    ``font`` has changed.
    This function should be called after changing the size or
    variation-axis settings on the ``font``.
    This call is fast if nothing has changed on ``font``.

    Note that as of version 11.0.0, calling this function is not necessary,
    as HarfBuzz will automatically detect changes to the font and update
    the underlying FT_Face as needed.
    """
def glib_blob_create(gbytes: bytes) -> blob_t:
    """
    Creates an :class:`HarfBuzz.blob_t` blob from the specified
    GBytes data structure.
    """
def glib_get_unicode_funcs() -> unicode_funcs_t:
    """
    Fetches a Unicode-functions structure that is populated
    with the appropriate GLib function for each method.
    """
def glib_script_from_script(script: script_t | int) -> GLib.UnicodeScript:
    """
    Fetches the GUnicodeScript identifier that corresponds to the
    specified :class:`HarfBuzz.script_t` script.
    """
def glib_script_to_script(script: GLib.UnicodeScript | int) -> script_t:
    """
    Fetches the :class:`HarfBuzz.script_t` script that corresponds to the
    specified GUnicodeScript identifier.
    """
def glyph_info_get_glyph_flags(info: glyph_info_t) -> glyph_flags_t:
    """
    Returns glyph flags encoded within a :class:`HarfBuzz.glyph_info_t`.
    """
def language_from_string(str: list[int]) -> language_t:
    """
    Converts ``str`` representing a BCP 47 language tag to the corresponding
    :class:`HarfBuzz.language_t`.
    """
def language_get_default() -> language_t:
    """
    Fetch the default language from current locale.

    <note>Note that the first time this function is called, it calls
    "setlocale (LC_CTYPE, nullptr)" to fetch current locale.  The underlying
    setlocale function is, in many implementations, NOT threadsafe.  To avoid
    problems, call this function once before multiple threads can call it.
    This function is only used from :func:`HarfBuzz.buffer_guess_segment_properties` by
    HarfBuzz itself.</note>
    """
def language_matches(language: language_t, specific: language_t) -> bool_t:
    """
    Check whether a second language tag is the same or a more
    specific version of the provided language tag.  For example,
    "fa_IR.utf8" is a more specific tag for "fa" or for "fa_IR".
    """
def language_to_string(language: language_t) -> str:
    """
    Converts an :class:`HarfBuzz.language_t` to a string.
    """
def malloc(size: int) -> int | None:
    """
    Allocates ``size`` bytes of memory, using the allocator set at
    compile-time. Typically just malloc().
    """
def map_allocation_successful(map: map_t) -> bool_t:
    """
    Tests whether memory allocation for a set was successful.
    """
def map_clear(map: map_t) -> None:
    """
    Clears out the contents of ``map``.
    """
def map_copy(map: map_t) -> map_t:
    """
    Allocate a copy of ``map``.
    """
def map_create() -> map_t:
    """
    Creates a new, initially empty map.
    """
def map_del(map: map_t, key: codepoint_t) -> None:
    """
    Removes ``key`` and its stored value from ``map``.
    """
def map_get(map: map_t, key: codepoint_t) -> codepoint_t:
    """
    Fetches the value stored for ``key`` in ``map``.
    """
def map_get_empty() -> map_t:
    """
    Fetches the singleton empty :class:`HarfBuzz.map_t`.
    """
def map_get_population(map: map_t) -> int:
    """
    Returns the number of key-value pairs in the map.
    """
def map_has(map: map_t, key: codepoint_t) -> bool_t:
    """
    Tests whether ``key`` is an element of ``map``.
    """
def map_hash(map: map_t) -> int:
    """
    Creates a hash representing ``map``.
    """
def map_is_empty(map: map_t) -> bool_t:
    """
    Tests whether ``map`` is empty (contains no elements).
    """
def map_is_equal(map: map_t, other: map_t) -> bool_t:
    """
    Tests whether ``map`` and ``other`` are equal (contain the same
    elements).
    """
def map_keys(map: map_t, keys: set_t) -> None:
    """
    Add the keys of ``map`` to ``keys``.
    """
def map_next(map: map_t, idx: int) -> tuple[bool_t, int, codepoint_t, codepoint_t]:
    """
    Fetches the next key/value pair in ``map``.

    Set ``idx`` to -1 to get started.

    If the map is modified during iteration, the behavior is undefined.

    The order in which the key/values are returned is undefined.
    """
def map_set(map: map_t, key: codepoint_t, value: codepoint_t) -> None:
    """
    Stores ``key``:``value`` in the map.
    """
def map_update(map: map_t, other: map_t) -> None:
    """
    Add the contents of ``other`` to ``map``.
    """
def map_values(map: map_t, values: set_t) -> None:
    """
    Add the values of ``map`` to ``values``.
    """
def ot_color_glyph_get_layers(face: face_t, glyph: codepoint_t, start_offset: int) -> tuple[int, list[ot_color_layer_t]]:
    """
    Fetches a list of all color layers for the specified glyph index in the specified
    face. The list returned will begin at the offset provided.
    """
def ot_color_glyph_has_paint(face: face_t, glyph: codepoint_t) -> bool_t:
    """
    Tests where a face includes COLRv1 paint
    data for ``glyph``.
    """
def ot_color_glyph_reference_png(font: font_t, glyph: codepoint_t) -> blob_t:
    """
    Fetches the PNG image for a glyph. This function takes a font object, not a face object,
    as input. To get an optimally sized PNG blob, the PPEM values must be set on the ``font``
    object. If PPEM is unset, the blob returned will be the largest PNG available.

    If the glyph has no PNG image, the singleton empty blob is returned.
    """
def ot_color_glyph_reference_svg(face: face_t, glyph: codepoint_t) -> blob_t:
    """
    Fetches the SVG document for a glyph. The blob may be either plain text or gzip-encoded.

    If the glyph has no SVG document, the singleton empty blob is returned.
    """
def ot_color_has_layers(face: face_t) -> bool_t:
    """
    Tests whether a face includes a ``COLR`` table
    with data according to COLRv0.
    """
def ot_color_has_paint(face: face_t) -> bool_t:
    """
    Tests where a face includes a ``COLR`` table
    with data according to COLRv1.
    """
def ot_color_has_palettes(face: face_t) -> bool_t:
    """
    Tests whether a face includes a ``CPAL`` color-palette table.
    """
def ot_color_has_png(face: face_t) -> bool_t:
    """
    Tests whether a face has PNG glyph images (either in ``CBDT`` or ``sbix`` tables).
    """
def ot_color_has_svg(face: face_t) -> bool_t:
    """
    Tests whether a face includes any ``SVG`` glyph images.
    """
def ot_color_palette_color_get_name_id(face: face_t, color_index: int) -> ot_name_id_t:
    """
    Fetches the ``name`` table Name ID that provides display names for
    the specified color in a face's ``CPAL`` color palette.

    Display names can be generic (e.g., "Background") or specific
    (e.g., "Eye color").
    """
def ot_color_palette_get_colors(face: face_t, palette_index: int, start_offset: int) -> tuple[int, list[color_t]]:
    """
    Fetches a list of the colors in a color palette.

    After calling this function, ``colors`` will be filled with the palette
    colors. If ``colors`` is NULL, the function will just return the number
    of total colors without storing any actual colors; this can be used
    for allocating a buffer of suitable size before calling
    :func:`HarfBuzz.ot_color_palette_get_colors` a second time.

    The RGBA values in the palette are unpremultiplied. See the
    OpenType spec `CPAL <https://learn.microsoft.com/en-us/typography/opentype/spec/cpal>`_
    section for details.
    """
def ot_color_palette_get_count(face: face_t) -> int:
    """
    Fetches the number of color palettes in a face.
    """
def ot_color_palette_get_flags(face: face_t, palette_index: int) -> ot_color_palette_flags_t:
    """
    Fetches the flags defined for a color palette.
    """
def ot_color_palette_get_name_id(face: face_t, palette_index: int) -> ot_name_id_t:
    """
    Fetches the ``name`` table Name ID that provides display names for
    a ``CPAL`` color palette.

    Palette display names can be generic (e.g., "Default") or provide
    specific, themed names (e.g., "Spring", "Summer", "Fall", and "Winter").
    """
def ot_font_set_funcs(font: font_t) -> None:
    """
    Sets the font functions to use when working with ``font`` to
    the HarfBuzz's native implementation. This is the default
    for fonts newly created.
    """
def ot_layout_collect_features(face: face_t, table_tag: tag_t, scripts: list[tag_t] | None = ..., languages: list[tag_t] | None = ..., features: list[tag_t] | None = ...) -> set_t:
    """
    Fetches a list of all feature indexes in the specified face's GSUB table
    or GPOS table, underneath the specified scripts, languages, and features.
    If no list of scripts is provided, all scripts will be queried. If no list
    of languages is provided, all languages will be queried. If no list of
    features is provided, all features will be queried.
    """
def ot_layout_collect_features_map(face: face_t, table_tag: tag_t, script_index: int, language_index: int) -> map_t:
    """
    Fetches the mapping from feature tags to feature indexes for
    the specified script and language.
    """
def ot_layout_collect_lookups(face: face_t, table_tag: tag_t, scripts: list[tag_t] | None = ..., languages: list[tag_t] | None = ..., features: list[tag_t] | None = ...) -> set_t:
    """
    Fetches a list of all feature-lookup indexes in the specified face's GSUB
    table or GPOS table, underneath the specified scripts, languages, and
    features. If no list of scripts is provided, all scripts will be queried.
    If no list of languages is provided, all languages will be queried. If no
    list of features is provided, all features will be queried.
    """
def ot_layout_feature_get_characters(face: face_t, table_tag: tag_t, feature_index: int, start_offset: int) -> tuple[int, list[codepoint_t]]:
    """
    Fetches a list of the characters defined as having a variant under the specified
    "Character Variant" ("cvXX") feature tag.
    """
def ot_layout_feature_get_lookups(face: face_t, table_tag: tag_t, feature_index: int, start_offset: int) -> tuple[int, list[int]]:
    """
    Fetches a list of all lookups enumerated for the specified feature, in
    the specified face's GSUB table or GPOS table. The list returned will
    begin at the offset provided.
    """
def ot_layout_feature_get_name_ids(face: face_t, table_tag: tag_t, feature_index: int) -> tuple[bool_t, ot_name_id_t, ot_name_id_t, ot_name_id_t, int, ot_name_id_t]:
    """
    Fetches name indices from feature parameters for "Stylistic Set" ('ssXX') or
    "Character Variant" ('cvXX') features.
    """
def ot_layout_feature_with_variations_get_lookups(face: face_t, table_tag: tag_t, feature_index: int, variations_index: int, start_offset: int) -> tuple[int, list[int]]:
    """
    Fetches a list of all lookups enumerated for the specified feature, in
    the specified face's GSUB table or GPOS table, enabled at the specified
    variations index. The list returned will begin at the offset provided.
    """
def ot_layout_get_attach_points(face: face_t, glyph: codepoint_t, start_offset: int) -> tuple[int, list[int]]:
    """
    Fetches a list of all attachment points for the specified glyph in the GDEF
    table of the face. The list returned will begin at the offset provided.

    Useful if the client program wishes to cache the list.
    """
def ot_layout_get_baseline(font: font_t, baseline_tag: ot_layout_baseline_tag_t | int, direction: direction_t | int, script_tag: tag_t, language_tag: tag_t) -> tuple[bool_t, position_t]:
    """
    Fetches a baseline value from the face.
    """
def ot_layout_get_baseline2(font: font_t, baseline_tag: ot_layout_baseline_tag_t | int, direction: direction_t | int, script: script_t | int, language: language_t | None = ...) -> tuple[bool_t, position_t]:
    """
    Fetches a baseline value from the face.

    This function is like :func:`HarfBuzz.ot_layout_get_baseline` but takes
    :class:`HarfBuzz.script_t` and :class:`HarfBuzz.language_t` instead of OpenType #hb_tag_t.
    """
def ot_layout_get_baseline_with_fallback(font: font_t, baseline_tag: ot_layout_baseline_tag_t | int, direction: direction_t | int, script_tag: tag_t, language_tag: tag_t) -> position_t:
    """
    Fetches a baseline value from the face, and synthesizes
    it if the font does not have it.
    """
def ot_layout_get_baseline_with_fallback2(font: font_t, baseline_tag: ot_layout_baseline_tag_t | int, direction: direction_t | int, script: script_t | int, language: language_t | None = ...) -> position_t:
    """
    Fetches a baseline value from the face, and synthesizes
    it if the font does not have it.

    This function is like :func:`HarfBuzz.ot_layout_get_baseline_with_fallback` but takes
    :class:`HarfBuzz.script_t` and :class:`HarfBuzz.language_t` instead of OpenType #hb_tag_t.
    """
def ot_layout_get_font_extents(font: font_t, direction: direction_t | int, script_tag: tag_t, language_tag: tag_t) -> tuple[bool_t, font_extents_t]:
    """
    Fetches script/language-specific font extents.  These values are
    looked up in the ``BASE`` table's ``MinMax`` records.

    If no such extents are found, the default extents for the font are
    fetched. As such, the return value of this function can for the
    most part be ignored.  Note that the per-script/language extents
    do not have a line-gap value, and the line-gap is set to zero in
    that case.
    """
def ot_layout_get_font_extents2(font: font_t, direction: direction_t | int, script: script_t | int, language: language_t | None = ...) -> tuple[bool_t, font_extents_t]:
    """
    Fetches script/language-specific font extents.  These values are
    looked up in the ``BASE`` table's ``MinMax`` records.

    If no such extents are found, the default extents for the font are
    fetched. As such, the return value of this function can for the
    most part be ignored.  Note that the per-script/language extents
    do not have a line-gap value, and the line-gap is set to zero in
    that case.

    This function is like :func:`HarfBuzz.ot_layout_get_font_extents` but takes
    :class:`HarfBuzz.script_t` and :class:`HarfBuzz.language_t` instead of OpenType #hb_tag_t.
    """
def ot_layout_get_glyph_class(face: face_t, glyph: codepoint_t) -> ot_layout_glyph_class_t:
    """
    Fetches the GDEF class of the requested glyph in the specified face.
    """
def ot_layout_get_glyphs_in_class(face: face_t, klass: ot_layout_glyph_class_t | int) -> set_t:
    """
    Retrieves the set of all glyphs from the face that belong to the requested
    glyph class in the face's GDEF table.
    """
def ot_layout_get_horizontal_baseline_tag_for_script(script: script_t | int) -> ot_layout_baseline_tag_t:
    """
    Fetches the dominant horizontal baseline tag used by ``script``.
    """
def ot_layout_get_ligature_carets(font: font_t, direction: direction_t | int, glyph: codepoint_t, start_offset: int) -> tuple[int, list[position_t]]:
    """
    Fetches a list of the caret positions defined for a ligature glyph in the GDEF
    table of the font. The list returned will begin at the offset provided.

    Note that a ligature that is formed from n characters will have n-1
    caret positions. The first character is not represented in the array,
    since its caret position is the glyph position.

    The positions returned by this function are 'unshaped', and will have to
    be fixed up for kerning that may be applied to the ligature glyph.
    """
def ot_layout_get_size_params(face: face_t) -> tuple[bool_t, int, int, ot_name_id_t, int, int]:
    """
    Fetches optical-size feature data (i.e., the ``size`` feature from GPOS). Note that
    the subfamily_id and the subfamily name string (accessible via the subfamily_name_id)
    as used here are defined as pertaining only to fonts within a font family that differ
    specifically in their respective size ranges; other ways to differentiate fonts within
    a subfamily are not covered by the ``size`` feature.

    For more information on this distinction, see the [``size`` feature documentation](
    https://docs.microsoft.com/en-us/typography/opentype/spec/features_pt#tag-size).
    """
def ot_layout_has_glyph_classes(face: face_t) -> bool_t:
    """
    Tests whether a face has any glyph classes defined in its GDEF table.
    """
def ot_layout_has_positioning(face: face_t) -> bool_t:
    """
    Tests whether the specified face includes any GPOS positioning.
    """
def ot_layout_has_substitution(face: face_t) -> bool_t:
    """
    Tests whether the specified face includes any GSUB substitutions.
    """
def ot_layout_language_find_feature(face: face_t, table_tag: tag_t, script_index: int, language_index: int, feature_tag: tag_t) -> tuple[bool_t, int]:
    """
    Fetches the index of a given feature tag in the specified face's GSUB table
    or GPOS table, underneath the specified script and language.
    """
def ot_layout_language_get_feature_indexes(face: face_t, table_tag: tag_t, script_index: int, language_index: int, start_offset: int) -> tuple[int, list[int]]:
    """
    Fetches a list of all features in the specified face's GSUB table
    or GPOS table, underneath the specified script and language. The list
    returned will begin at the offset provided.
    """
def ot_layout_language_get_feature_tags(face: face_t, table_tag: tag_t, script_index: int, language_index: int, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches a list of all features in the specified face's GSUB table
    or GPOS table, underneath the specified script and language. The list
    returned will begin at the offset provided.
    """
def ot_layout_language_get_required_feature(face: face_t, table_tag: tag_t, script_index: int, language_index: int) -> tuple[bool_t, int, tag_t]:
    """
    Fetches the tag of a requested feature index in the given face's GSUB or GPOS table,
    underneath the specified script and language.
    """
def ot_layout_language_get_required_feature_index(face: face_t, table_tag: tag_t, script_index: int, language_index: int) -> tuple[bool_t, int]:
    """
    Fetches the index of a requested feature in the given face's GSUB or GPOS table,
    underneath the specified script and language.
    """
def ot_layout_lookup_collect_glyph_alternates(face: face_t, lookup_index: int, alternate_count: map_t, alternate_glyphs: map_t) -> tuple[bool_t, map_t, map_t]:
    """
    Collects alternates of glyphs from a given GSUB lookup index.

    For one-to-one GSUB glyph substitutions, this function collects the
    substituted glyph.

    For lookups that assign multiple alternates to a glyph, all alternate glyphs are collected.

    For other lookup types, nothing is performed and ``false`` is returned.

    The ``alternate_count`` mapping will contain the number of alternates for each glyph id.
    Upon entry, this mapping should contain the glyph ids as keys, and the number of alternates
    currently known for each glyph id as values.

    The ``alternate_glyphs`` mapping will contain the alternate glyph ids for each glyph id.
    The mapping is encoded in the following way, upon entry and after processing:
    If G is the glyph id, and A0, A1, ..., A(n-1) are the alternate glyph ids,
    the mapping will contain the following entries: (G + (i << 24)) -> A(i)
    for i = 0, 1, ..., n-1 where n is the number of alternates for G as per ``alternate_count``.
    """
def ot_layout_lookup_collect_glyphs(face: face_t, table_tag: tag_t, lookup_index: int) -> tuple[set_t, set_t, set_t, set_t]:
    """
    Fetches a list of all glyphs affected by the specified lookup in the
    specified face's GSUB table or GPOS table.
    """
def ot_layout_lookup_get_glyph_alternates(face: face_t, lookup_index: int, glyph: codepoint_t, start_offset: int) -> tuple[int, list[codepoint_t]]:
    """
    Fetches alternates of a glyph from a given GSUB lookup index. Note that for one-to-one GSUB
    glyph substitutions, this function fetches the substituted glyph.
    """
def ot_layout_lookup_get_optical_bound(font: font_t, lookup_index: int, direction: direction_t | int, glyph: codepoint_t) -> position_t:
    """
    Fetches the optical bound of a glyph positioned at the margin of text.
    The direction identifies which edge of the glyph to query.
    """
def ot_layout_lookup_substitute_closure(face: face_t, lookup_index: int) -> set_t:
    """
    Compute the transitive closure of glyphs needed for a
    specified lookup.
    """
def ot_layout_lookup_would_substitute(face: face_t, lookup_index: int, glyphs: codepoint_t, glyphs_length: int, zero_context: bool_t) -> bool_t:
    """
    Tests whether a specified lookup in the specified face would
    trigger a substitution on the given glyph sequence.
    """
def ot_layout_lookups_substitute_closure(face: face_t, lookups: set_t) -> set_t:
    """
    Compute the transitive closure of glyphs needed for all of the
    provided lookups.
    """
@deprecated("Deprecated since 2.0.0.")
def ot_layout_script_find_language(face: face_t, table_tag: tag_t, script_index: int, language_tag: tag_t, language_index: int) -> bool_t:
    """
    Fetches the index of a given language tag in the specified face's GSUB table
    or GPOS table, underneath the specified script tag.
    """
def ot_layout_script_get_language_tags(face: face_t, table_tag: tag_t, script_index: int, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches a list of language tags in the given face's GSUB or GPOS table, underneath
    the specified script index. The list returned will begin at the offset provided.
    """
def ot_layout_script_select_language(face: face_t, table_tag: tag_t, script_index: int, language_count: int, language_tags: tag_t) -> tuple[bool_t, int]:
    """
    Fetches the index of the first language tag fom ``language_tags`` that is present
    in the specified face's GSUB or GPOS table, underneath the specified script
    index.

    If none of the given language tags is found, ``false`` is returned and
    ``language_index`` is set to the default language index.
    """
def ot_layout_script_select_language2(face: face_t, table_tag: tag_t, script_index: int, language_count: int, language_tags: tag_t) -> tuple[bool_t, int, tag_t]:
    """
    Fetches the index of the first language tag fom ``language_tags`` that is present
    in the specified face's GSUB or GPOS table, underneath the specified script
    index.

    If none of the given language tags is found, ``false`` is returned and
    ``language_index`` is set to :data:`HarfBuzz.OT_LAYOUT_DEFAULT_LANGUAGE_INDEX` and
    ``chosen_language`` is set to :class:`HB_TAG_NONE.`
    """
def ot_layout_table_choose_script(face: face_t, table_tag: tag_t, script_tags: tag_t) -> tuple[bool_t, int, tag_t]:
    """
    Deprecated since 2.0.0
    """
def ot_layout_table_find_feature_variations(face: face_t, table_tag: tag_t, coords: int, num_coords: int) -> tuple[bool_t, int]:
    """
    Fetches a list of feature variations in the specified face's GSUB table
    or GPOS table, at the specified variation coordinates.
    """
def ot_layout_table_find_script(face: face_t, table_tag: tag_t, script_tag: tag_t) -> tuple[bool_t, int]:
    """
    Fetches the index if a given script tag in the specified face's GSUB table
    or GPOS table.
    """
def ot_layout_table_get_feature_tags(face: face_t, table_tag: tag_t, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches a list of all feature tags in the given face's GSUB or GPOS table.
    Note that there might be duplicate feature tags, belonging to different
    script/language-system pairs of the table.
    """
def ot_layout_table_get_lookup_count(face: face_t, table_tag: tag_t) -> int:
    """
    Fetches the total number of lookups enumerated in the specified
    face's GSUB table or GPOS table.
    """
def ot_layout_table_get_script_tags(face: face_t, table_tag: tag_t, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches a list of all scripts enumerated in the specified face's GSUB table
    or GPOS table. The list returned will begin at the offset provided.
    """
def ot_layout_table_select_script(face: face_t, table_tag: tag_t, script_count: int, script_tags: tag_t) -> tuple[bool_t, int, tag_t]:
    """
    Selects an OpenType script for ``table_tag`` from the ``script_tags`` array.

    If the table does not have any of the requested scripts, then ``DFLT``,
    ``dflt``, and ``latn`` tags are tried in that order. If the table still does not
    have any of these scripts, ``script_index`` is set to
    :data:`HarfBuzz.OT_LAYOUT_NO_SCRIPT_INDEX` and ``chosen_script`` is set to :class:`HB_TAG_NONE.`
    """
def ot_math_get_constant(font: font_t, constant: ot_math_constant_t | int) -> position_t:
    """
    Fetches the specified math constant. For most constants, the value returned
    is an #hb_position_t.

    However, if the requested constant is :data:`HarfBuzz.ot_math_constant_t.SCRIPT_PERCENT_SCALE_DOWN`,
    :data:`HarfBuzz.ot_math_constant_t.SCRIPT_SCRIPT_PERCENT_SCALE_DOWN` or
    :data:`HarfBuzz.ot_math_constant_t.RADICAL_DEGREE_BOTTOM_RAISE_PERCENT`, then the return value is
    an integer between 0 and 100 representing that percentage.
    """
def ot_math_get_glyph_assembly(font: font_t, glyph: codepoint_t, direction: direction_t | int, start_offset: int) -> tuple[int, list[ot_math_glyph_part_t], position_t]:
    """
    Fetches the GlyphAssembly for the specified font, glyph index, and direction.
    Returned are a list of :class:`HarfBuzz.ot_math_glyph_part_t` glyph parts that can be
    used to draw the glyph and an italics-correction value (if one is defined
    in the font).

    <note>The ``direction`` parameter is only used to select between horizontal
    or vertical directions for the construction. Even though all :class:`HarfBuzz.direction_t`
    values are accepted, only the result of :class:`HB_DIRECTION_IS_HORIZONTAL` is
    considered.</note>
    """
def ot_math_get_glyph_italics_correction(font: font_t, glyph: codepoint_t) -> position_t:
    """
    Fetches an italics-correction value (if one exists) for the specified
    glyph index.
    """
def ot_math_get_glyph_kerning(font: font_t, glyph: codepoint_t, kern: ot_math_kern_t | int, correction_height: position_t) -> position_t:
    """
    Fetches the math kerning (cut-ins) value for the specified font, glyph index, and
    ``kern``.

    If the MathKern table is found, the function examines it to find a height
    value that is greater or equal to ``correction_height``. If such a height
    value is found, corresponding kerning value from the table is returned. If
    no such height value is found, the last kerning value is returned.
    """
def ot_math_get_glyph_kernings(font: font_t, glyph: codepoint_t, kern: ot_math_kern_t | int, start_offset: int) -> tuple[int, list[ot_math_kern_entry_t]]:
    """
    Fetches the raw MathKern (cut-in) data for the specified font, glyph index,
    and ``kern``. The corresponding list of kern values and correction heights is
    returned as a list of :class:`HarfBuzz.ot_math_kern_entry_t` structs.

    See also #:func:`HarfBuzz.ot_math_get_glyph_kerning`, which handles selecting the
    appropriate kern value for a given correction height.

    <note>For a glyph with ``n`` defined kern values (where ``n`` > 0), there are only
    ``n``−1 defined correction heights, as each correction height defines a boundary
    past which the next kern value should be selected. Therefore, only the
    :class:`HarfBuzz.ot_math_kern_entry_t`.kern_value of the uppermost :class:`HarfBuzz.ot_math_kern_entry_t`
    actually comes from the font; its corresponding
    :class:`HarfBuzz.ot_math_kern_entry_t`.max_correction_height is always set to
    <code>INT32_MAX</code>.</note>
    """
def ot_math_get_glyph_top_accent_attachment(font: font_t, glyph: codepoint_t) -> position_t:
    """
    Fetches a top-accent-attachment value (if one exists) for the specified
    glyph index.

    For any glyph that does not have a top-accent-attachment value - that is,
    a glyph not covered by the ``MathTopAccentAttachment`` table (or, when
    ``font`` has no ``MathTopAccentAttachment`` table or no ``MATH`` table, any
    glyph) - the function synthesizes a value, returning the position at
    one-half the glyph's advance width.
    """
def ot_math_get_glyph_variants(font: font_t, glyph: codepoint_t, direction: direction_t | int, start_offset: int) -> tuple[int, list[ot_math_glyph_variant_t]]:
    """
    Fetches the MathGlyphConstruction for the specified font, glyph index, and
    direction. The corresponding list of size variants is returned as a list of
    :class:`HarfBuzz.ot_math_glyph_variant_t` structs.

    <note>The ``direction`` parameter is only used to select between horizontal
    or vertical directions for the construction. Even though all :class:`HarfBuzz.direction_t`
    values are accepted, only the result of :class:`HB_DIRECTION_IS_HORIZONTAL` is
    considered.</note>
    """
def ot_math_get_min_connector_overlap(font: font_t, direction: direction_t | int) -> position_t:
    """
    Fetches the MathVariants table for the specified font and returns the
    minimum overlap of connecting glyphs that are required to draw a glyph
    assembly in the specified direction.

    <note>The ``direction`` parameter is only used to select between horizontal
    or vertical directions for the construction. Even though all :class:`HarfBuzz.direction_t`
    values are accepted, only the result of :class:`HB_DIRECTION_IS_HORIZONTAL` is
    considered.</note>
    """
def ot_math_has_data(face: face_t) -> bool_t:
    """
    Tests whether a face has a ``MATH`` table.
    """
def ot_math_is_glyph_extended_shape(face: face_t, glyph: codepoint_t) -> bool_t:
    """
    Tests whether the given glyph index is an extended shape in the face.
    """
def ot_meta_get_entry_tags(face: face_t, start_offset: int) -> tuple[int, list[ot_meta_tag_t | int]]:
    """
    Fetches all available feature types.
    """
def ot_meta_reference_entry(face: face_t, meta_tag: ot_meta_tag_t | int) -> blob_t:
    """
    It fetches metadata entry of a given tag from a font.
    """
def ot_metrics_get_position(font: font_t, metrics_tag: ot_metrics_tag_t | int) -> tuple[bool_t, position_t]:
    """
    Fetches metrics value corresponding to ``metrics_tag`` from ``font``.
    """
def ot_metrics_get_position_with_fallback(font: font_t, metrics_tag: ot_metrics_tag_t | int) -> position_t:
    """
    Fetches metrics value corresponding to ``metrics_tag`` from ``font``,
    and synthesizes a value if it the value is missing in the font.
    """
def ot_metrics_get_variation(font: font_t, metrics_tag: ot_metrics_tag_t | int) -> float:
    """
    Fetches metrics value corresponding to ``metrics_tag`` from ``font`` with the
    current font variation settings applied.
    """
def ot_metrics_get_x_variation(font: font_t, metrics_tag: ot_metrics_tag_t | int) -> position_t:
    """
    Fetches horizontal metrics value corresponding to ``metrics_tag`` from ``font``
    with the current font variation settings applied.
    """
def ot_metrics_get_y_variation(font: font_t, metrics_tag: ot_metrics_tag_t | int) -> position_t:
    """
    Fetches vertical metrics value corresponding to ``metrics_tag`` from ``font`` with
    the current font variation settings applied.
    """
def ot_name_get_utf16(face: face_t, name_id: ot_name_id_t, language: language_t) -> tuple[int, list[int]]:
    """
    Fetches a font name from the OpenType 'name' table.
    If ``language`` is :data:`HarfBuzz.LANGUAGE_INVALID`, English ("en") is assumed.
    Returns string in UTF-16 encoding. A NUL terminator is always written
    for convenience, and isn't included in the output ``text_size``.
    """
def ot_name_get_utf32(face: face_t, name_id: ot_name_id_t, language: language_t) -> tuple[int, list[int]]:
    """
    Fetches a font name from the OpenType 'name' table.
    If ``language`` is :data:`HarfBuzz.LANGUAGE_INVALID`, English ("en") is assumed.
    Returns string in UTF-32 encoding. A NUL terminator is always written
    for convenience, and isn't included in the output ``text_size``.
    """
def ot_name_get_utf8(face: face_t, name_id: ot_name_id_t, language: language_t) -> tuple[int, list[str]]:
    """
    Fetches a font name from the OpenType 'name' table.
    If ``language`` is :data:`HarfBuzz.LANGUAGE_INVALID`, English ("en") is assumed.
    Returns string in UTF-8 encoding. A NUL terminator is always written
    for convenience, and isn't included in the output ``text_size``.
    """
def ot_name_list_names(face: face_t) -> list[ot_name_entry_t]:
    """
    Enumerates all available name IDs and language combinations. Returned
    array is owned by the ``face`` and should not be modified.  It can be
    used as long as ``face`` is alive.
    """
def ot_shape_glyphs_closure(font: font_t, buffer: buffer_t, features: list[feature_t]) -> set_t:
    """
    Computes the transitive closure of glyphs needed for a specified
    input buffer under the given font and feature list. The closure is
    computed as a set, not as a list.
    """
def ot_shape_plan_collect_lookups(shape_plan: shape_plan_t, table_tag: tag_t) -> set_t:
    """
    Computes the complete set of GSUB or GPOS lookups that are applicable
    under a given ``shape_plan``.
    """
def ot_shape_plan_get_feature_tags(shape_plan: shape_plan_t, start_offset: int) -> tuple[int, list[tag_t]]:
    """
    Fetches the list of OpenType feature tags enabled for a shaping plan, if possible.
    """
@deprecated("Deprecated since 2.0.0.")
def ot_tag_from_language(language: language_t) -> tag_t:
    """
    Converts an :class:`HarfBuzz.language_t` to an #hb_tag_t.
    """
def ot_tag_to_language(tag: tag_t) -> language_t | None:
    """
    Converts a language tag to an :class:`HarfBuzz.language_t`.
    """
def ot_tag_to_script(tag: tag_t) -> script_t:
    """
    Converts a script tag to an :class:`HarfBuzz.script_t`.
    """
@deprecated("Deprecated since 2.0.0.")
def ot_tags_from_script(script: script_t | int) -> tuple[tag_t, tag_t]:
    """
    Converts an :class:`HarfBuzz.script_t` to script tags.
    """
def ot_tags_from_script_and_language(script: script_t | int, language: language_t | None, script_count: int, language_count: int) -> tuple[int, tag_t, int, tag_t]:
    """
    Converts an :class:`HarfBuzz.script_t` and an :class:`HarfBuzz.language_t` to script and language tags.
    """
def ot_tags_to_script_and_language(script_tag: tag_t, language_tag: tag_t) -> tuple[script_t | int, language_t]:
    """
    Converts a script tag and a language tag to an :class:`HarfBuzz.script_t` and an
    :class:`HarfBuzz.language_t`.
    """
@deprecated("Deprecated since 2.2.0.")
def ot_var_find_axis(face: face_t, axis_tag: tag_t, axis_index: int) -> tuple[bool_t, ot_var_axis_t]:
    """
    Fetches the variation-axis information corresponding to the specified axis tag
    in the specified face.
    """
def ot_var_find_axis_info(face: face_t, axis_tag: tag_t) -> tuple[bool_t, ot_var_axis_info_t]:
    """
    Fetches the variation-axis information corresponding to the specified axis tag
    in the specified face.
    """
@deprecated("Deprecated since 2.2.0.")
def ot_var_get_axes(face: face_t, start_offset: int) -> tuple[int, list[ot_var_axis_t]]:
    """
    Fetches a list of all variation axes in the specified face. The list returned will begin
    at the offset provided.
    """
def ot_var_get_axis_count(face: face_t) -> int:
    """
    Fetches the number of OpenType variation axes included in the face.
    """
def ot_var_get_axis_infos(face: face_t, start_offset: int) -> tuple[int, list[ot_var_axis_info_t]]:
    """
    Fetches a list of all variation axes in the specified face. The list returned will begin
    at the offset provided.
    """
def ot_var_get_named_instance_count(face: face_t) -> int:
    """
    Fetches the number of named instances included in the face.
    """
def ot_var_has_data(face: face_t) -> bool_t:
    """
    Tests whether a face includes any OpenType variation data in the ``fvar`` table.
    """
def ot_var_named_instance_get_design_coords(face: face_t, instance_index: int) -> tuple[int, list[float]]:
    """
    Fetches the design-space coordinates corresponding to the given
    named instance in the face.
    """
def ot_var_named_instance_get_postscript_name_id(face: face_t, instance_index: int) -> ot_name_id_t:
    """
    Fetches the ``name`` table Name ID that provides display names for
    the "PostScript name" defined for the given named instance in the face.
    """
def ot_var_named_instance_get_subfamily_name_id(face: face_t, instance_index: int) -> ot_name_id_t:
    """
    Fetches the ``name`` table Name ID that provides display names for
    the "Subfamily name" defined for the given named instance in the face.
    """
def ot_var_normalize_coords(face: face_t, coords_length: int, design_coords: float) -> int:
    """
    Normalizes the given design-space coordinates. The minimum and maximum
    values for the axis are mapped to the interval [-1,1], with the default
    axis value mapped to 0.

    The normalized values have 14 bits of fixed-point sub-integer precision as per
    OpenType specification.

    Any additional scaling defined in the face's ``avar`` table is also
    applied, as described at https://docs.microsoft.com/en-us/typography/opentype/spec/avar

    Note: ``coords_length`` must be the same as the number of axes in the face, as
    for example returned by :func:`HarfBuzz.ot_var_get_axis_count`.
    Otherwise, the behavior is undefined.
    """
def ot_var_normalize_variations(face: face_t, variations: variation_t, variations_length: int) -> list[int]:
    """
    Normalizes all of the coordinates in the given list of variation axes.
    """
def paint_color(funcs: paint_funcs_t, paint_data: int | None, is_foreground: bool_t, color: color_t) -> None:
    """
    Perform a "color" paint operation.
    """
def paint_color_glyph(funcs: paint_funcs_t, paint_data: int | None, glyph: codepoint_t, font: font_t) -> bool_t:
    """
    Perform a "color-glyph" paint operation.
    """
def paint_custom_palette_color(funcs: paint_funcs_t, paint_data: int | None, color_index: int) -> tuple[bool_t, color_t]:
    """
    Gets the custom palette color for ``color_index``.
    """
def paint_funcs_create() -> paint_funcs_t:
    """
    Creates a new :class:`HarfBuzz.paint_funcs_t` structure of paint functions.

    The initial reference count of 1 should be released with hb_paint_funcs_destroy()
    when you are done using the :class:`HarfBuzz.paint_funcs_t`. This function never returns
    ``None``. If memory cannot be allocated, a special singleton :class:`HarfBuzz.paint_funcs_t`
    object will be returned.
    """
def paint_funcs_get_empty() -> paint_funcs_t:
    """
    Fetches the singleton empty paint-functions structure.
    """
def paint_funcs_is_immutable(funcs: paint_funcs_t) -> bool_t:
    """
    Tests whether a paint-functions structure is immutable.
    """
def paint_funcs_make_immutable(funcs: paint_funcs_t) -> None:
    """
    Makes a paint-functions structure immutable.

    After this call, all attempts to set one of the callbacks
    on ``funcs`` will fail.
    """
def paint_funcs_set_color_func(funcs: paint_funcs_t, func: paint_color_func_t) -> None:
    """
    Sets the paint-color callback on the paint functions struct.
    """
def paint_funcs_set_color_glyph_func(funcs: paint_funcs_t, func: paint_color_glyph_func_t) -> None:
    """
    Sets the color-glyph callback on the paint functions struct.
    """
def paint_funcs_set_custom_palette_color_func(funcs: paint_funcs_t, func: paint_custom_palette_color_func_t) -> None:
    """
    Sets the custom-palette-color callback on the paint functions struct.
    """
def paint_funcs_set_image_func(funcs: paint_funcs_t, func: paint_image_func_t) -> None:
    """
    Sets the paint-image callback on the paint functions struct.
    """
def paint_funcs_set_linear_gradient_func(funcs: paint_funcs_t, func: paint_linear_gradient_func_t) -> None:
    """
    Sets the linear-gradient callback on the paint functions struct.
    """
def paint_funcs_set_pop_clip_func(funcs: paint_funcs_t, func: paint_pop_clip_func_t) -> None:
    """
    Sets the pop-clip callback on the paint functions struct.
    """
def paint_funcs_set_pop_group_func(funcs: paint_funcs_t, func: paint_pop_group_func_t) -> None:
    """
    Sets the pop-group callback on the paint functions struct.
    """
def paint_funcs_set_pop_transform_func(funcs: paint_funcs_t, func: paint_pop_transform_func_t) -> None:
    """
    Sets the pop-transform callback on the paint functions struct.
    """
def paint_funcs_set_push_clip_glyph_func(funcs: paint_funcs_t, func: paint_push_clip_glyph_func_t) -> None:
    """
    Sets the push-clip-glyph callback on the paint functions struct.
    """
def paint_funcs_set_push_clip_rectangle_func(funcs: paint_funcs_t, func: paint_push_clip_rectangle_func_t) -> None:
    """
    Sets the push-clip-rect callback on the paint functions struct.
    """
def paint_funcs_set_push_group_func(funcs: paint_funcs_t, func: paint_push_group_func_t) -> None:
    """
    Sets the push-group callback on the paint functions struct.
    """
def paint_funcs_set_push_transform_func(funcs: paint_funcs_t, func: paint_push_transform_func_t) -> None:
    """
    Sets the push-transform callback on the paint functions struct.
    """
def paint_funcs_set_radial_gradient_func(funcs: paint_funcs_t, func: paint_radial_gradient_func_t) -> None:
    """
    Sets the radial-gradient callback on the paint functions struct.
    """
def paint_funcs_set_sweep_gradient_func(funcs: paint_funcs_t, func: paint_sweep_gradient_func_t) -> None:
    """
    Sets the sweep-gradient callback on the paint functions struct.
    """
def paint_image(funcs: paint_funcs_t, paint_data: int | None, image: blob_t, width: int, height: int, format: tag_t, slant: float, extents: glyph_extents_t | None = ...) -> None:
    """
    Perform a "image" paint operation.
    """
def paint_linear_gradient(funcs: paint_funcs_t, paint_data: int | None, color_line: color_line_t, x0: float, y0: float, x1: float, y1: float, x2: float, y2: float) -> None:
    """
    Perform a "linear-gradient" paint operation.
    """
def paint_pop_clip(funcs: paint_funcs_t, paint_data: int | None = ...) -> None:
    """
    Perform a "pop-clip" paint operation.
    """
def paint_pop_group(funcs: paint_funcs_t, paint_data: int | None, mode: paint_composite_mode_t | int) -> None:
    """
    Perform a "pop-group" paint operation.
    """
def paint_pop_transform(funcs: paint_funcs_t, paint_data: int | None = ...) -> None:
    """
    Perform a "pop-transform" paint operation.
    """
def paint_push_clip_glyph(funcs: paint_funcs_t, paint_data: int | None, glyph: codepoint_t, font: font_t) -> None:
    """
    Perform a "push-clip-glyph" paint operation.
    """
def paint_push_clip_rectangle(funcs: paint_funcs_t, paint_data: int | None, xmin: float, ymin: float, xmax: float, ymax: float) -> None:
    """
    Perform a "push-clip-rect" paint operation.
    """
def paint_push_font_transform(funcs: paint_funcs_t, paint_data: int | None, font: font_t) -> None:
    """
    Push the transform reflecting the font's scale and slant
    settings onto the paint functions.
    """
def paint_push_group(funcs: paint_funcs_t, paint_data: int | None = ...) -> None:
    """
    Perform a "push-group" paint operation.
    """
def paint_push_inverse_font_transform(funcs: paint_funcs_t, paint_data: int | None, font: font_t) -> None:
    """
    Push the inverse of the transform reflecting the font's
    scale and slant settings onto the paint functions.
    """
def paint_push_transform(funcs: paint_funcs_t, paint_data: int | None, xx: float, yx: float, xy: float, yy: float, dx: float, dy: float) -> None:
    """
    Perform a "push-transform" paint operation.
    """
def paint_radial_gradient(funcs: paint_funcs_t, paint_data: int | None, color_line: color_line_t, x0: float, y0: float, r0: float, x1: float, y1: float, r1: float) -> None:
    """
    Perform a "radial-gradient" paint operation.
    """
def paint_sweep_gradient(funcs: paint_funcs_t, paint_data: int | None, color_line: color_line_t, x0: float, y0: float, start_angle: float, end_angle: float) -> None:
    """
    Perform a "sweep-gradient" paint operation.
    """
def realloc(ptr: int | None, size: int) -> int | None:
    """
    Reallocates the memory pointed to by ``ptr`` to ``size`` bytes, using the
    allocator set at compile-time. Typically just realloc().
    """
def script_from_iso15924_tag(tag: tag_t) -> script_t:
    """
    Converts an ISO 15924 script tag to a corresponding :class:`HarfBuzz.script_t`.
    """
def script_from_string(str: list[int]) -> script_t:
    """
    Converts a string ``str`` representing an ISO 15924 script tag to a
    corresponding :class:`HarfBuzz.script_t`. Shorthand for :func:`HarfBuzz.tag_from_string` then
    :func:`HarfBuzz.script_from_iso15924_tag`.
    """
def script_get_horizontal_direction(script: script_t | int) -> direction_t:
    """
    Fetches the :class:`HarfBuzz.direction_t` of a script when it is
    set horizontally. All right-to-left scripts will return
    :data:`HarfBuzz.direction_t.RTL`. All left-to-right scripts will return
    :data:`HarfBuzz.direction_t.LTR`.

    Scripts that can be written either right-to-left or
    left-to-right will return :data:`HarfBuzz.direction_t.INVALID`.

    Unknown scripts will return :data:`HarfBuzz.direction_t.LTR`.
    """
def script_to_iso15924_tag(script: script_t | int) -> tag_t:
    """
    Converts an :class:`HarfBuzz.script_t` to a corresponding ISO 15924 script tag.
    """
def segment_properties_equal(a: segment_properties_t, b: segment_properties_t) -> bool_t:
    """
    Checks the equality of two :class:`HarfBuzz.segment_properties_t`'s.
    """
def segment_properties_hash(p: segment_properties_t) -> int:
    """
    Creates a hash representing ``p``.
    """
def segment_properties_overlay(p: segment_properties_t, src: segment_properties_t) -> None:
    """
    Fills in missing fields of ``p`` from ``src`` in a considered manner.

    First, if ``p`` does not have direction set, direction is copied from ``src``.

    Next, if ``p`` and ``src`` have the same direction (which can be unset), if ``p``
    does not have script set, script is copied from ``src``.

    Finally, if ``p`` and ``src`` have the same direction and script (which either
    can be unset), if ``p`` does not have language set, language is copied from
    ``src``.
    """
def set_add(set: set_t, codepoint: codepoint_t) -> None:
    """
    Adds ``codepoint`` to ``set``.
    """
def set_add_range(set: set_t, first: codepoint_t, last: codepoint_t) -> None:
    """
    Adds all of the elements from ``first`` to ``last``
    (inclusive) to ``set``.
    """
def set_add_sorted_array(set: set_t, sorted_codepoints: list[codepoint_t]) -> None:
    """
    Adds ``num_codepoints`` codepoints to a set at once.
    The codepoints array must be in increasing order,
    with size at least ``num_codepoints``.
    """
def set_allocation_successful(set: set_t) -> bool_t:
    """
    Tests whether memory allocation for a set was successful.
    """
def set_clear(set: set_t) -> None:
    """
    Clears out the contents of a set.
    """
def set_copy(set: set_t) -> set_t:
    """
    Allocate a copy of ``set``.
    """
def set_create() -> set_t:
    """
    Creates a new, initially empty set.
    """
def set_del(set: set_t, codepoint: codepoint_t) -> None:
    """
    Removes ``codepoint`` from ``set``.
    """
def set_del_range(set: set_t, first: codepoint_t, last: codepoint_t) -> None:
    """
    Removes all of the elements from ``first`` to ``last``
    (inclusive) from ``set``.

    If ``last`` is :class:`HB_SET_VALUE_INVALID`, then all values
    greater than or equal to ``first`` are removed.
    """
def set_get_empty() -> set_t:
    """
    Fetches the singleton empty :class:`HarfBuzz.set_t`.
    """
def set_get_max(set: set_t) -> codepoint_t:
    """
    Finds the largest element in the set.
    """
def set_get_min(set: set_t) -> codepoint_t:
    """
    Finds the smallest element in the set.
    """
def set_get_population(set: set_t) -> int:
    """
    Returns the number of elements in the set.
    """
def set_has(set: set_t, codepoint: codepoint_t) -> bool_t:
    """
    Tests whether ``codepoint`` belongs to ``set``.
    """
def set_hash(set: set_t) -> int:
    """
    Creates a hash representing ``set``.
    """
def set_intersect(set: set_t, other: set_t) -> None:
    """
    Makes ``set`` the intersection of ``set`` and ``other``.
    """
def set_invert(set: set_t) -> None:
    """
    Inverts the contents of ``set``.
    """
def set_is_empty(set: set_t) -> bool_t:
    """
    Tests whether a set is empty (contains no elements).
    """
def set_is_equal(set: set_t, other: set_t) -> bool_t:
    """
    Tests whether ``set`` and ``other`` are equal (contain the same
    elements).
    """
def set_is_inverted(set: set_t) -> bool_t:
    """
    Returns whether the set is inverted.
    """
def set_is_subset(set: set_t, larger_set: set_t) -> bool_t:
    """
    Tests whether ``set`` is a subset of ``larger_set``.
    """
def set_next(set: set_t, codepoint: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the next element in ``set`` that is greater than current value of ``codepoint``.

    Set ``codepoint`` to :class:`HB_SET_VALUE_INVALID` to get started.
    """
def set_next_many(set: set_t, codepoint: codepoint_t, out: list[codepoint_t]) -> int:
    """
    Finds the next element in ``set`` that is greater than ``codepoint``. Writes out
    codepoints to ``out``, until either the set runs out of elements, or ``size``
    codepoints are written, whichever comes first.
    """
def set_next_range(set: set_t, last: codepoint_t) -> tuple[bool_t, codepoint_t, codepoint_t]:
    """
    Fetches the next consecutive range of elements in ``set`` that
    are greater than current value of ``last``.

    Set ``last`` to :class:`HB_SET_VALUE_INVALID` to get started.
    """
def set_previous(set: set_t, codepoint: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the previous element in ``set`` that is lower than current value of ``codepoint``.

    Set ``codepoint`` to :class:`HB_SET_VALUE_INVALID` to get started.
    """
def set_previous_range(set: set_t, first: codepoint_t) -> tuple[bool_t, codepoint_t, codepoint_t]:
    """
    Fetches the previous consecutive range of elements in ``set`` that
    are greater than current value of ``last``.

    Set ``first`` to :class:`HB_SET_VALUE_INVALID` to get started.
    """
def set_set(set: set_t, other: set_t) -> None:
    """
    Makes the contents of ``set`` equal to the contents of ``other``.
    """
def set_subtract(set: set_t, other: set_t) -> None:
    """
    Subtracts the contents of ``other`` from ``set``.
    """
def set_symmetric_difference(set: set_t, other: set_t) -> None:
    """
    Makes ``set`` the symmetric difference of ``set``
    and ``other``.
    """
def set_union(set: set_t, other: set_t) -> None:
    """
    Makes ``set`` the union of ``set`` and ``other``.
    """
def shape(font: font_t, buffer: buffer_t, features: list[feature_t] | None = ...) -> None:
    """
    Shapes ``buffer`` using ``font`` turning its Unicode characters content to
    positioned glyphs. If ``features`` is not ``None``, it will be used to control the
    features applied during shaping. If two ``features`` have the same tag but
    overlapping ranges the value of the feature with the higher index takes
    precedence.
    """
def shape_full(font: font_t, buffer: buffer_t, features: list[feature_t] | None = ..., shaper_list: list[str] | None = ...) -> bool_t:
    """
    See :func:`HarfBuzz.shape` for details. If ``shaper_list`` is not ``None``, the specified
    shapers will be used in the given order, otherwise the default shapers list
    will be used.
    """
def shape_list_shapers() -> list[str]:
    """
    Retrieves the list of shapers supported by HarfBuzz.
    """
def shape_plan_create(face: face_t, props: segment_properties_t, user_features: list[feature_t], shaper_list: list[str]) -> shape_plan_t:
    """
    Constructs a shaping plan for a combination of ``face``, ``user_features``, ``props``,
    and ``shaper_list``.
    """
def shape_plan_create2(face: face_t, props: segment_properties_t, user_features: list[feature_t], coords: list[int], shaper_list: list[str]) -> shape_plan_t:
    """
    The variable-font version of #:func:`HarfBuzz.shape_plan_create`.
    Constructs a shaping plan for a combination of ``face``, ``user_features``, ``props``,
    and ``shaper_list``, plus the variation-space coordinates ``coords``.
    """
def shape_plan_create_cached(face: face_t, props: segment_properties_t, user_features: list[feature_t], shaper_list: list[str]) -> shape_plan_t:
    """
    Creates a cached shaping plan suitable for reuse, for a combination
    of ``face``, ``user_features``, ``props``, and ``shaper_list``.
    """
def shape_plan_create_cached2(face: face_t, props: segment_properties_t, user_features: list[feature_t], coords: list[int], shaper_list: list[str]) -> shape_plan_t:
    """
    The variable-font version of #:func:`HarfBuzz.shape_plan_create_cached`.
    Creates a cached shaping plan suitable for reuse, for a combination
    of ``face``, ``user_features``, ``props``, and ``shaper_list``, plus the
    variation-space coordinates ``coords``.
    """
def shape_plan_execute(shape_plan: shape_plan_t, font: font_t, buffer: buffer_t, features: list[feature_t]) -> bool_t:
    """
    Executes the given shaping plan on the specified buffer, using
    the given ``font`` and ``features``.
    """
def shape_plan_get_empty() -> shape_plan_t:
    """
    Fetches the singleton empty shaping plan.
    """
def shape_plan_get_shaper(shape_plan: shape_plan_t) -> str:
    """
    Fetches the shaper from a given shaping plan.
    """
def style_get_value(font: font_t, style_tag: style_tag_t | int) -> float:
    """
    Searches variation axes of a :class:`HarfBuzz.font_t` object for a specific axis first,
    if not set, first tries to get default style values in ``STAT`` table
    then tries to polyfill from different tables of the font.
    """
def tag_from_string(str: list[int]) -> tag_t:
    """
    Converts a string into an #hb_tag_t. Valid tags
    are four characters. Shorter input strings will be
    padded with spaces. Longer input strings will be
    truncated.
    """
def tag_to_string(tag: tag_t) -> list[int]:
    """
    Converts an #hb_tag_t to a string and returns it in ``buf``.
    Strings will be four characters long.
    """
def unicode_combining_class(ufuncs: unicode_funcs_t, unicode: codepoint_t) -> unicode_combining_class_t:
    """
    Retrieves the Canonical Combining Class (ccc) property
    of code point ``unicode``.
    """
def unicode_compose(ufuncs: unicode_funcs_t, a: codepoint_t, b: codepoint_t) -> tuple[bool_t, codepoint_t]:
    """
    Fetches the composition of a sequence of two Unicode
    code points.

    Calls the composition function of the specified
    Unicode-functions structure ``ufuncs``.
    """
def unicode_decompose(ufuncs: unicode_funcs_t, ab: codepoint_t) -> tuple[bool_t, codepoint_t, codepoint_t]:
    """
    Fetches the decomposition of a Unicode code point.

    Calls the decomposition function of the specified
    Unicode-functions structure ``ufuncs``.
    """
@deprecated("Deprecated since 2.0.0.")
def unicode_decompose_compatibility(ufuncs: unicode_funcs_t, u: codepoint_t) -> tuple[int, codepoint_t]:
    """
    Fetches the compatibility decomposition of a Unicode
    code point. Deprecated.
    """
@deprecated("Deprecated since 2.0.0.")
def unicode_eastasian_width(ufuncs: unicode_funcs_t, unicode: codepoint_t) -> int:
    """
    Don't use. Not used by HarfBuzz.
    """
def unicode_funcs_create(parent: unicode_funcs_t | None = ...) -> unicode_funcs_t:
    """
    Creates a new :class:`HarfBuzz.unicode_funcs_t` structure of Unicode functions.
    """
def unicode_funcs_get_default() -> unicode_funcs_t:
    """
    Fetches a pointer to the default Unicode-functions structure that is used
    when no functions are explicitly set on :class:`HarfBuzz.buffer_t`.
    """
def unicode_funcs_get_empty() -> unicode_funcs_t:
    """
    Fetches the singleton empty Unicode-functions structure.
    """
def unicode_funcs_get_parent(ufuncs: unicode_funcs_t) -> unicode_funcs_t:
    """
    Fetches the parent of the Unicode-functions structure
    ``ufuncs``.
    """
def unicode_funcs_is_immutable(ufuncs: unicode_funcs_t) -> bool_t:
    """
    Tests whether the specified Unicode-functions structure
    is immutable.
    """
def unicode_funcs_make_immutable(ufuncs: unicode_funcs_t) -> None:
    """
    Makes the specified Unicode-functions structure
    immutable.
    """
def unicode_funcs_set_combining_class_func(ufuncs: unicode_funcs_t, func: unicode_combining_class_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_combining_class_func_t.
    """
def unicode_funcs_set_compose_func(ufuncs: unicode_funcs_t, func: unicode_compose_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_compose_func_t.
    """
@deprecated("Deprecated since 2.0.0.")
def unicode_funcs_set_decompose_compatibility_func(ufuncs: unicode_funcs_t, func: unicode_decompose_compatibility_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_decompose_compatibility_func_t.
    """
def unicode_funcs_set_decompose_func(ufuncs: unicode_funcs_t, func: unicode_decompose_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_decompose_func_t.
    """
@deprecated("Deprecated since 2.0.0.")
def unicode_funcs_set_eastasian_width_func(ufuncs: unicode_funcs_t, func: unicode_eastasian_width_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_eastasian_width_func_t.
    """
def unicode_funcs_set_general_category_func(ufuncs: unicode_funcs_t, func: unicode_general_category_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_general_category_func_t.
    """
def unicode_funcs_set_mirroring_func(ufuncs: unicode_funcs_t, func: unicode_mirroring_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_mirroring_func_t.
    """
def unicode_funcs_set_script_func(ufuncs: unicode_funcs_t, func: unicode_script_func_t) -> None:
    """
    Sets the implementation function for #hb_unicode_script_func_t.
    """
def unicode_general_category(ufuncs: unicode_funcs_t, unicode: codepoint_t) -> unicode_general_category_t:
    """
    Retrieves the General Category (gc) property
    of code point ``unicode``.
    """
def unicode_mirroring(ufuncs: unicode_funcs_t, unicode: codepoint_t) -> codepoint_t:
    """
    Retrieves the Bi-directional Mirroring Glyph code
    point defined for code point ``unicode``.
    """
def unicode_script(ufuncs: unicode_funcs_t, unicode: codepoint_t) -> script_t:
    """
    Retrieves the :class:`HarfBuzz.script_t` script to which code
    point ``unicode`` belongs.
    """
def variation_from_string(str: list[int]) -> tuple[bool_t, variation_t]:
    """
    Parses a string into a :class:`HarfBuzz.variation_t`.

    The format for specifying variation settings follows. All valid CSS
    font-variation-settings values other than 'normal' and 'inherited' are also
    accepted, though, not documented below.

    The format is a tag, optionally followed by an equals sign, followed by a
    number. For example ``wght=500``, or ``slnt=-7.5``.
    """
def variation_to_string(variation: variation_t) -> list[str]:
    """
    Converts an :class:`HarfBuzz.variation_t` into a ``None``-terminated string in the format
    understood by :func:`HarfBuzz.variation_from_string`. The client in responsible for
    allocating big enough size for ``buf``, 128 bytes is more than enough.

    Note that the string won't include any whitespace.
    """
